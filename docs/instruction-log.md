# Instruction log

Every request the agent has issued to the user, in order, with outcomes —
the raw data for reducing human-in-the-loop load (see the "User request
protocol" in the agent-toolkit skills). Types: `decision` (choose or
approve), `execute` (run what the agent cannot), `verify` (judge reality
by eye), `provision` (supply a resource), `inform` (supply knowledge).

Requests R1–R15 were backfilled on 2026-07-28 when the protocol was
adopted mid-M3; wording is reconstructed, outcomes are exact.

## M1–M2 (pre-protocol, not reconstructed)

M1/M2 requests predate this log. Known shape: review-gate decisions
(plan B reorientation, architecture QA/QB/QC), live-verification
sessions (dump-vs-screen comparisons, UI panel calibration). See those
milestones' planning dirs.

## M3 — navigation

### P3 review gate (map generator blocked)

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R1 | decision | Resolve the generator blockage | Choose: supply vanilla 1.13c DLLs / keep stubbing PD2's DLL chain / ship M3 without generated maps | User supplied a better fourth option: SP maps are fixed per character+difficulty, so persist live-read grids instead (explored-map atlas). Generator deferred. |
| R2 | provision | Supply a vanilla 1.13c DLL set | Folder of clean 1.13c game DLLs for the generator | **Withdrawn** — superseded by R1's resolution. |

### Live verification (elevated terminal, user at the machine)

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R3 | execute | Gate test in three states | `navdemo gate` with ESC menu open, backgrounded, and `--focus` | PASS 3/3 — refusals correct, allowed state correct. Required a tool fix first (user caught that "game focused + press Enter" was impossible → `--focus` flag added). |
| R4 | execute | Click test, first run | `navdemo click-test --send` in open ground | Undershoot: error (−4, 1). Cause ambiguous (timer vs render scale). |
| R5 | execute | Click test, re-run after watch-until-stop fix | Same command | Same (−4, 1) — not the timer; scale factor confirmed as hypothesis. |
| R6 | execute | Projection calibration | `navdemo calibrate` (12 walks, 8 directions) in open ground | Fitted 20/10 px/subtile (ratio 2.000); bonus discovery: clicks >~320 px complete only 60–70% of distance → waypoint cap justified empirically. |
| R7 | execute | Click test, confirm calibration | `navdemo click-test --send` | PASS — error (0, 1). |
| R8 | execute | Collision dump at a landmark | `collision` beside a map-edge cliff | 0 rooms read — exposed a CollMap layout error (no pMapEnd in this build; grid stored inline at Coll+0x24). |
| R9 | execute | Collision chain diagnosis | `collision --debug` | Field-by-field dump identified the true layout; tile/subtile invariant adopted as the integrity check. |
| R10 | verify | Collision vs eyes | Re-run `collision`; compare walls/cliff against the screen | PASS — cliff reported 1 subtile screen-right, matching reality. |
| R11 | verify | Collision tracks movement | Walk ~20 subtiles, re-run, compare | PASS — terrain fixed in world coords across dumps (cliff at x=5953 in both), view followed player. |
| R12 | execute | Survey walk, first attempt | `navigate --survey` around Cold Plains waypoint | 247 rooms "recorded" — user's monster-collision hypothesis confirmed a churn bug (occupancy bits persisted); transient-mask fix added. |
| R13 | execute | Survey walk, post-fix | Delete `maps/`, re-survey same area | PASS — 34 rooms. |
| R14 | execute | Atlas persistence checks | `--survey` standing still; `Get-ChildItem maps`; save-exit → new game → `--survey` | PASS — 0 standing still; same seed folder after full game cycle; 125 re-records in fresh game were byte-identical churn (file size unchanged), benign, now instrumented. |
| R15 | execute | Acceptance demo, first attempt | `navigate --demo` | FAIL — demo aimed at a fixed offset in unsurveyed ground; demo rewritten to pick known/walkable/reachable targets. |
| R16 | execute | Survey current spot, then demo ×5 | `--survey` loop where standing, then `navigate --demo` five times | **PASS 5/5** (survey read 0 — ground already known from R15's walk). 10 walks of ~60 subtiles, all arrived; 2.9–6.7 s each; re-click ladder fired 7 times incl. absorbing the user touching the mouse mid-demo-1; demo 3 showed a full stuck→re-plan→arrive cycle. M3 acceptance criterion met. |

### M3 closeout

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R17 | execute | Ground-item live check (M2 leftover) | Drop an item, `dump -v`, confirm listed, pick up, confirm gone | **Found a real bug** — a *carried* item reported as on the floor "at (5, 0)" (its inventory grid slot), unchanged by pickup. Cause: ItemData's location byte (BH header, 0x45) does not mean what the header says in this build. Filter switched to the unit MODE (onGround=3/dropping=5, kolbot sdk); regression test added. Third header-vs-reality correction of M3. |
| R19 | execute | Ground-item re-check after mode filter | Same procedure: drop, dump, pick up, dump | False positive gone (0 items with nothing dropped) but **false negative**: a dropped potion was not detected. Mode constants verified correct against kolbot sdk, so the cause is elsewhere → raw-field diagnostic added (`dump --items`) rather than a third guess. |
| R20 | execute | Dump raw item-unit fields | `dump --items` with an item on the ground, then after picking it up | **Resolved by R23** (same session): the raw dump revealed ground items carry `loc@0x45=247`, the mode-based filter was confirmed, and the hash-table defects it exposed were fixed. *(Marked closed at the M5 closeout reconciliation, 2026-08-03.)* |
| R21 | inform | Mercenary/summons counted as monsters | User observed the dump reporting their Rogue merc as a monster and predicted summons would too; noted units under player control also do not collide | **Confirmed before testing** — the reported `type 271` is exactly kolbot's `sdk.mercs.Rogue`. D2 files mercs/summons/friendly NPCs as unit type 1; only stat 172 (Alignment==2) separates them. Scans now split `monsters` (hostile) from `allies`; 2 regression tests. Collision point noted: ally bits are outside the walkability mask, so no pathing impact. |
| R22 | verify | Confirm ally split live | Summon skeletons, run `dump -v`, check monsters vs "yours" | Partly — split works, but exposed R23's defects. |
| R23 | execute | Verify hash-table enumeration | `dump -v` and `dump --items` with skeletons out and an item dropped, then after pickup | **Ground items fixed and confirmed**: the same unit reads `mode=3` at world (5274,5725), then `mode=0` at inventory slot (7,3) after pickup. Also revealed why the original filter failed — ground items carry `loc@0x45=247`, not 255, so it rejected real drops while accepting belt potions. **New defect found**: the hash table is global (whole stash, other levels, expired summons), so the ally list showed phantoms (`summon 0`, `summon 6`) and the merc was missing. Added a perception-radius filter (80 subtiles) and a `--monsters` diagnostic. |
| R24 | execute | Verify locality filter; locate the merc | `dump -v` then `dump --monsters` with merc and skeletons out | **PASS** — `yours: 3 (summon, summon, rogue 8%) (+3 dead)`. Merc found. But the raw dump showed heavy duplication (49 rows for ~13 units): units are reachable from several bucket heads, so the iterator now de-duplicates by unit id. |
| R25 | execute | Confirm de-duplication | `dump --monsters` and `dump -v` | **PASS** — 49 → 14 rows, each unit once; merc explicit as `271 … MERC rogue, ally` at 128/1620 hp (7.9%, matching the 8% summary); 6 allies + 8 dead hostiles = 14, fully consistent with the `-v` view. Perception verified end to end. |

### Post-milestone housekeeping

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R26 | decision | Relocate agent-toolkit to `C:\dev\agent-toolkit`? | Move the cross-project toolkit out of the pd2 workspace folder, or leave it | **Move it.** Moved; repo intact; `install.sh` verified from the new home; path pointers updated in `~/.claude/CLAUDE.md` and memory. Same conversation also established: Windows-only development (Mac framing removed from project docs), toolkit = single source of truth with pull-first editing, and the multi-machine update model documented in the toolkit README (`287f82a`). |
| R18 | decision | Commit M3? | Approve the single conventional commit of the milestone (repo) and a separate commit of the agent-toolkit protocol changes | **Yes to both.** Project: `fa81028` on main (43 files, +6002/−89). agent-toolkit: `d585250`. Both worktrees clean; neither pushed. |

## M4 — game cycle

### Planning discovery

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R27 | decision | M4 planning confirmations (Q1–Q8) | Batched table: OOG perception via D2Win control list, separate MenuInput guard, chicken scope, merc chicken deferral, door deferral, corpse retrieval, difficulty-from-memory, focus-loss policy | Q1–Q5, Q8 **yes**. Q6 **no — scope change**: on death the bot stops completely and awaits a human; corpse retrieval/recovery deferred (low priority). Q7 **no** (Hell-only project), with caveat: blind-fallback path adds a difficulty sanity read. User also supplied the survival toolkit (blood warp, potions, bone shield ≥75%, ESC-exit, walk-away, 3 revives) → designed as M5's survival layer; robustness-before-live-runs is a standing M5 constraint. |
| R28 | decision | How to live-test death handling | Choose: throwaway low-level char in Normal / accept one deliberate Hell death on MaqiuDoubing / simulation only | **Withdrawn** — superseded by R27/Q6: death→full-stop has no recovery sequence to live-test; simulation covers detection→halt. |
| R29 | decision | Approve the M4 phase table | Five phases: OOG perception / menu input / game-cycle FSM / safety monitor / docs+teach | **Approved** (`lgtm`), with one amendment: user convinced by the map-poisoning argument — the post-join difficulty read becomes an **unconditional** guard on every game entry (was: only if the blind fallback landed). |

### M4 live-session setup

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R30 | execute | Open an elevated terminal, verify attach | Run-as-administrator PowerShell; `cd` to repo; `python -m pd2bot.dump` against client at char select | First run failed on an **agent error**: bare `python` resolves to a stale system 3.8 without pymem — the canonical env is the venv at `~\.venvs\pd2bot` (documented in README; agent should have checked before writing the command). Corrected command reissued with the venv's full path. Superseded before re-run: user proposed giving the agent direct elevated access instead (→ R31), exactly the reduction this log's observations predicted. |
| R31 | decision | Choose the agent's elevated-access mechanism | Options: (A) elevated command-runner bridge the agent drives, (B) resume session in an elevated `claude` CLI, (C) run the desktop app itself as administrator | **A — the bridge** (agent recommendation accepted). Trust trade acknowledged: agent commands execute elevated without per-command human review while the loop runs. Script: `tools/elevated-bridge.ps1`; queue at `%LOCALAPPDATA%\pd2bot-bridge`. |
| R32 | execute | Start the elevated bridge | One command in the existing elevated window; leave it running for the whole session | **Up.** Elevation check through the bridge: True, correct user/cwd, PS 5.1. Found one defect: exit codes never populated (Start-Process + timed WaitForExit quirk) — fixed in the script, restart needed (→ R33). |
| R33 | execute | Restart the bridge (exit-code fix) | Ctrl+C in the bridge window, re-run the same start command | **Done** (needed a re-ask — the first "restart" turned out to be the old instance still running; the fresh banner is the tell). First fix (extra WaitForExit) was insufficient (local repro); real fix: exit code in-band via wrapper shell + sidecar file. Verified through the bridge: probe `exit: 7`; then `pd2bot.dump` → `exit: 0`, attached pid 24996, correctly reported not-in-a-game at char select. **R30's original goal achieved agent-side; the elevated round-trip era ends here** — from R34 on, live checks are agent-driven and user requests should drop to game-side actions only (be at the machine, keep the game foreground, judge by eye). |

### M4 implementation — P1 (OOG perception)

Bridge era: commands run agent-side (bridge ids in parentheses); user
requests cover game-side actions only. First live dump (006, char
select) passed outright: screen classified, 26 controls, button texts
clean, kolbot-table coordinates exact, state semantics confirmed
(disabled CONVERT TO reads state 4). Control-list strategy confirmed —
no fallback needed.

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R34 | execute | Walk the menu screens for the OOG watcher | With the transition watcher running (bridge 007/009/010/011): save-and-exit → menu screens → re-enter a Hell game | **PASS** (4th attempt; the loop working as designed). Attempt 1: agent defect (script run from %TEMP%, PYTHONPATH fix). Attempt 2: instructions read as gameplay, user played instead — bonus: `in_game` stayed rock-stable through waypoint travel and combat. Attempt 3: user called away, window expired. Attempt 4 (011): **every transition classified, zero unknowns** — in_game→loading→main_menu→char_select→difficulty→char_select→main_menu→char_select→difficulty→in_game(2). **User-found discovery: save-and-exit lands at MAIN MENU in offline SP**, not char select as kolbot's (multiplayer-lore) flow implies — P3's leave sequence updated to expect it. Alt-tabbing irrelevant to memory reads, confirmed. ERROR_POPUP not safely triggerable live; covered by kolbot fingerprint + unit test only. One-shot 008 verified difficulty=2 (Hell) in-game. |

### M4 implementation — P2 (menu input)

Complement-guard refusal verified live agent-side (bridge 013): in a
game, no ESC menu → `REFUSED … GatedInput's job`. (Bridge 012 was lost
to PowerShell quote-mangling of inline `python -c`; bridge commands now
always write a temp .py first.)

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R35 | execute | ESC-menu probe, then hands-off click test | Bridge 014–017 across three reruns: probe the ESC menu's control visibility; hands-off click test at char select | Took three rounds — attempts 1–2 lost to probe windows racing the user's reading speed (fixed: single long-window state-driven scripts). Results: **ESC menu is NOT in the control list** (P3 needs calibrated fixed coordinates); first bot menu click **missed 140 px right** of OK — naive stretch model wrong. |
| R36 | execute+verify | Hover the OK button to confirm the corrected projection | Steady 2 s hover on OK's center; probe compares against the pillarbox model and only clicks if within 6 px | **PASS** — client 1536×864; menus are aspect-fit 1.44× with 192 px pillarbars (the config's mysterious 1.44 from M3, now explained: it is the MENU scale, not the world scale). Hover delta (−1, +2). Auto click test: OK clicked at (1187,799), difficulty popup appeared, bot's own ESC returned to char_select. User confirmed visually. |

### M4 implementation — P3 (game cycle)

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R37 | execute | Hover "Save and Exit Game" for calibration | Enter game, ESC, steady 2 s hover on the button, don't click (bridge 020) | **Done** — (751, 366) at 1536×864 → client-rect fractions (0.4889, 0.4236); stored in CycleConfig (ESC menu is not a readable control, R35). |
| R38 | execute | Hands off for the first autonomous cycle | Focus the game, hands off; bot does ESC → Save and Exit → menus → OK → HELL (bridge 021 expired unused while user away; rerun 022) | **PASS** — leave → main_menu → create → difficulty 2, fresh seed. First fully autonomous game cycle; user watched it live ("worked perfectly to my eye"). |
| R39 | execute | Acceptance run: 3 unattended cycles | Focus the game, hands off ~3 min; `python -m pd2bot.cycle --games 3 --dwell 10` (bridge 023) | **PASS 3/3** — `[CVRL]` × 3: created, Hell-verified, dwelled as MaqiuDoubing, left cleanly. No retries, no focus interventions. M4 acceptance criterion #1 met. |

| R40 | decision | P3 review gate | Approve the cycle (3/3 acceptance, one skipped live test: deliberate wrong-difficulty entry judged all-risk-no-information) and proceed to P4 | **Approved** (`ok`). |

### M4 implementation — P4 (safety monitor) + chat channel

Mid-milestone addition at user request (before R41 ran): an in-game
chat channel (`pd2bot/chat.py`) so live-test instructions appear in the
game instead of requiring alt-tabs. Guard: nothing is typed until the
chat console (UI panel 0x05) is verified open — an unopened console
would turn text into hotkey presses; re-verified per character.

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R41 | execute | Chicken demo + inert run (pre-chat version) | Focus game, cast until mana dips; then hands off for 2 normal cycles (bridge 024/025) | **Superseded** before running (windows expired during the chat-channel detour) — reissued as R43 with in-game chat instructions. |
| R42 | execute | Chat probe | Enter a Hell game, focus, hands off; bot posts a message to the in-game chat (bridge 026) | **PASS** — message posted; the alt-tab-free instruction channel exists. |
| R43 | execute | Chicken demo with in-game instructions, then inert run | Bot chats the instructions; user casts until mana dips; bot flees on its own; then 2 hands-off default-threshold cycles (bridge 027–029) | **PASS both.** Chicken: tripped at `mana 340/378 (90%) <= 99%`, announced in chat, left the game autonomously — full chicken pipeline live at zero risk. Inert: 2/2 `[CVRL]`, monitor silent at defaults. (One focus-timeout rerun in between; the chat channel removed the alt-tab problem it was built for.) User flagged the 90%-vs-99% gap; diagnosis: not a defect — 0.4 s sampling + burst mana costs (and possibly a sub-100% start) mean the first at-or-below observation can sit well past the threshold. That floor-trigger semantic is the production behavior we want for life chicken (a hard hit crosses 50% mid-frame; the monitor reacts to the crossing within one tick). |

### M4 closeout

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R45 | inform | Carried-vitals chicken loop risk | User flagged post-acceptance: PD2 does not heal between games, so below-threshold vitals at entry → chicken → new game → chicken, ad infinitum; also explains the demo's "instant" trip (mana spent in the *previous* game carried in) | **Confirmed real** (M4's dwell loop was safe only via town suppression; M5's real runs were exposed). Fixed before commit: `max_consecutive_chickens` backstop (default 2) halts the loop loudly, streak resets on any clean run; +2 tests (195 total). Durable fix assigned to M5: town-heal preamble (healer NPC before leaving town, kolbot-style) — recorded in `_DONE.md` follow-ups and game-cycle.md. |
| R44 | decision | Commit M4? | Approve one conventional commit of the milestone (code + docs + archived plan; now 195 tests green incl. the R45 backstop) | **Yes, commit and push.** Milestone: `e6dabec` on main (35 files, +4360/−19), followed by a docs chore commit stamping the SHA into the archived plan; both pushed to origin/main. (The first SHA-stamping attempt mangled UTF-8 via PowerShell's ANSI default — restored from the commit and redone with Python; lesson consistent with the bridge's quoting defect: text transforms on this repo go through Python, not PowerShell string ops.) |

## M5 — trial run (Cold Plains)

### Planning discovery

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R46 | decision | M5 planning confirmations (Q1–Q8) | Batched table: on-foot route (no WP menu), clearance definition, trial-pickit scope, stash via PD2 ctrl+click, town preamble order, hotkey-based skill casting with read-back verify, reflex ladder above the untouched SafetyMonitor, staged acceptance ladder | Q2–Q8 **yes** with amendments: clearance radius 150; pickit format must be human-readable/easily updatable; full-stash guardrails; preamble gains conditional merc resurrect (dead + gold > 49,999); combat layer class-agnostic by architecture; chicken threshold to be discussed (→ R49). Q1 **no — scope change**: waypoint usage is in scope (key to all future runs); route becomes town WP → Cold Plains WP teleport, cross-area walking deferred to M6. |
| R47 | inform | Character kit questionnaire | The necro's actual skills/levels, manual pack-fighting rotation, blood warp + desecrate/revive mechanics, bone armor recast rule, belt layout, hotkey bindings, PD2 QoL confirmations, honest Hell Blood Moor/Cold Plains danger assessment | **Answered in full** — left skill permanently Poison Strike; right-skill hotkeys F1–F6 (bone armor/blood warp/TP/bone wall/desecrate/revive); skirmish pattern (wait for revives → dash in → left-click → retreat); bone armor recast <75% absorb (fallback: recast when hit); blood warp cost 10 mana + max(12% HP, 12); desecrate→revive to 3, not in town, ~300 s; belt 4×4 (heal/rejuv/mana/heal), drink rules 100%/50%/25%; stash = **shift+right-click**; danger "fairly dangerous — idle kills, groups stun-lock". Full detail in the M5 planning notes.md. |
| R48 | decision | Belt refill strategy for M5 | Vendor bulk-buy QoL vs ground-potion refill + loud halt vs full vendor shopping UI | **(b)** — ground-potion refill + loud halt in town when below minimum; no vendor UI in M5 (user preference). |
| R49 | decision | Reflex-ladder thresholds (incl. chicken) | Proposed defaults for the full rung ladder: chicken 35%, rejuv 50%, blood-warp surround trigger, heal/mana drink rules, upkeep rungs — all config, tunable during staged acceptance | **Approved with one change**: mana potion cooldown 15 s (proposed 5 s). Chicken 35% accepted. |
| R50 | decision | Approve the M5 phase table | Six phases: perception extensions / input paths / town layer + waypoint / behavior engine + ladder / combat + pickit / staged acceptance + docs | **Approved** (`lgtm`); execution strictly sequential (no P3/P4 overlap). |

### M5 implementation — P1 (perception extensions)

Code first, sim-tested (211 tests, was 195), then live verification.
New reads: active skills, corpses, objects, carried items/belt, merc/
revive counts; all offsets cited (BH + d2bs + kolbot sdk), all marked
unverified until the drills below.

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R51 | execute | Start the bridge + enter a Hell town game | Run `tools/elevated-bridge.ps1` in an elevated PowerShell; launch PD2 via PD2Launcher, enter a Hell game with MaqiuDoubing, stay in town | **Done** — bridge up, attach confirmed on the first dump (bridge 030). |
| R52 | execute | P1 live-verification drills (in-town) | Guided by in-game chat: F1–F6 skill cycling (skill-id capture), Bone Armor cast-and-diff (stat 132/133), potion shuffle inventory↔belt↔stash (`--carried --watch`), walk past Akara/Kashya (NPC kinds), stand by waypoint + stash (object kinds); out-of-town corpse check at user's discretion | **PASS, all drills** (bridge 030–035); corpse kill unnecessary (town corpses read mode 12). Captured: all 7 skill ids (Blood Warp 367, Desecrate 83 are PD2's); bone-armor stats 132/133 fixed-point; container classification verified at every hop incl. the belt-column cascade; NPCs 147/148/150/154/155/265; objects 119/267. Surprises: cursor-held items leave the inventory chain (pCursorItem read added); **PD2 renumbered potions** (healing 610/611, rejuv 530/531, mana 606; classic tables discarded); alignment-statless townsfolk read as monsters in town; user's merc at 8% hp with 0 carried gold (P3 resurrect-condition datum). |
| R53 | inform | Name the PD2 potion kinds | Hover the tooltips: what are kinds 610 vs 611 (healing tiers), 530 vs 531 (rejuv vs full), and 606 — and why does 606 sit in belt column 4, which R47.6 called a healing column? (Layout deviation or misattribution?) | **Answered with a scope change**: the belt had been shuffled; the layout seen in drill C is now the *permanent* configuration — **key 1 = mana, key 2 = rejuv, key 3 = health, key 4 = health** (supersedes R47.6's heal/rejuv/mana/heal). Tier differences within a type are irrelevant for our purposes. Kind→type attribution flips accordingly (610/611 likely mana, 606 healing) — confirmed empirically via R54 rather than tooltips. |
| R54 | execute | Potion-type proof drill | In-game, chat-guided: cast Bone Armor 3–4× to drain mana, then press key 1; the watcher reads which kind leaves the belt and which orb refills — self-verifying kind→type attribution, one potion spent | **PASS** (bridge 038–039; one retry — the first chat attempt caught the alt-tab focus transition mid-typing and the guard stopped it exactly as designed). Four casts drained 378→240 mana; key 1 consumed kind 611 from column 0; mana refilled 246→378. **Proven: 610/611 mana, 606 healing, 530/531 rejuv.** Tables and tests updated (211 green, ruff clean). |


### M5 implementation — P2 (input extensions)

Sim work first: PanelInput (third gate), stand-still shift click,
verified skill switch, belt keys — 232 tests green. Live finding
during test design: UI_WPMENU's BH enum slot (0x14) is in the
always-on filter, so the waypoint panel's real slot needs live
calibration (drill phase 1).

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R55 | execute | P2 live drills (town, chat-guided) | Four phases: panel-slot calibration (user toggles waypoint/stash/NPC-menu/inventory over a raw UI-array differ), bot-driven verified skill switching, bot-driven Bone Armor cast + belt drink (first autonomous keypresses/right-click), PanelInput refusal demonstrations. Positive panel click deferred to P3 post-calibration (blind panel clicks violate the never-click-unnamed policy) | **PASS, all four phases** (bridge 044; two earlier launches 041/042 expired waiting for game foreground across the overnight pause). Calibration: waypoint = 0x14 toggling cleanly (M2's always-on note was a misobservation — slot restored as a panel, kept fail-safe by adding it to the BLOCKING set; P3 must verify the 0->1 edge), stash = 0x19 raising ONLY itself (inventory slot stays 0 — stash added to the blocking set, closing a real can_act hole), npc_menu = 0x08 confirmed. Verified switch: desecrate 83 and bone armor 68 both read back. Cast: right-click sent, mana 378->343 (the 35-mana bone armor cost). Drink: belt col 0 1->0, mana back to 378 — col 0 is now EMPTY of mana potions. Refusals: no-panel and wrong-belief both refused with correct messages. 234 tests, ruff clean. |

| R56 | inform | Kashya's menu is state-dependent | User-supplied, unprompted: a "Resurrect MERCNAME: $GOLDPRICE" line appears in Kashya's menu ONLY while the hireling is dead, shifting the other rows | **Incorporated into P3**: the resurrect row cannot be calibrated while the merc lives (it does not exist); NPC-menu row positions are per-menu-state. Flow hardened: confirm merc dead via perception before using the dead-state calibration; verify merc-alive + gold-decreased after; calibration itself deferred to the first real dead-merc occurrence (the phase file already leaned that way). |

### M5 implementation — P3 (town layer + waypoint)

Sim work first: `waypoint.py` (edge-verified panel, calibrated
rows, arrival-by-area proof) + `town.py` (heal/stash/refill/merc
with guardrails) — 259 tests green.

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R57 | execute | P3 hover calibration (5 targets, chat-guided) | Rest the cursor on: inventory top-left cell, bottom-right cell, top-left again with the stash open (consistency), the Rogue Encampment waypoint row, the Cold Plains row. No clicks sent | **FAILED — agent tool defect** (bridge 046). Captures 1 and 2 returned the identical pixel (803, 374): the script had no re-arm requirement, so capture 2 fired instantly on the cursor still resting from capture 1, desyncing the sequence (cell pitch computed as 0.0000, stash point 453 px away). Fixed in-band (cursor must move >40 px from the last capture before the next arms; clearer stash-grid-vs-inventory-grid wording) and reissued as R58. Consistent with the M3 pattern: first-run instrumentation defects cost a round trip. |
| R58 | execute | P3 hover calibration, re-run after the re-arm fix | Same five targets, with the fixed capture script | **Superseded before running** — the user asked, before starting, whether the reference cells being *occupied by items* invalidated the exercise. Correct instinct: occupancy cannot move the cursor, but a multi-cell item icon hides the cell boundaries and pulls the aim to the item's centre, and that bias lands in the ORIGIN, which offsets every later click. Reissued as R59 with a better method. |
| R59 | execute | P3 calibration with memory-identified anchors | User places a 1x1 item in the top-left and bottom-right inventory cells; the script reads both grid coordinates from memory before any hover | **Cancelled mid-flight — user caught a design flaw.** PD2 has a **charm inventory** below the main grid, permanently off-limits to the bot; if those slots share the inventory container, the script's "maximum occupied cell" anchor would be a charm cell and skew the whole grid. Worse, the same blind spot would have bitten P3's `deposit_to_stash`, which walks every inventory-container item: a charm in locked space gets a computed pixel, fails its transfer verification, and trips the full-stash halt for nothing. Reissued as R60 with a user-designed replacement. |
| R60 | execute | Four-corner inventory calibration (user-designed) | User drags a 1x1 potion from the belt into each of the four corners of the USABLE inventory, one at a time, hovering each 3 s; the script watches for each item to appear and reads the landed cell from memory. Closes with a charm-slot probe | **PASS, and it found something bigger than the geometry** (bridge 049). Geometry: usable grid confirmed **10x4 at cells (0,0)..(9,3)**, origin fraction (0.5257, 0.4404), cell pitch (0.0273, 0.0467) = 42.0 x 40.3 px, worst corner residual 3.5 px. **The charm finding**: the probe item landed at cell (4,4) reading `game_location=3, node_page=1, mode=0, container='inventory'` — byte-for-byte identical to ordinary inventory. Charm space shares the container; ONLY the cell coordinate separates it. And the baseline dump showed **all 24 of the character's existing inventory-container items sit at y 4..7** (the main grid was empty), so the pre-R60 `deposit_to_stash` would have walked two dozen untouchable items, failed every transfer, and tripped the full-stash halt over a stash that was never full. Exclusion implemented (`INVENTORY_COLS/ROWS`, `main_inventory` vs `charm_inventory`, pixel maths refuses out-of-grid cells); 264 tests green. |
| R61 | execute | Snake-sweep inventory calibration (T11, dense pass) | User snakes one potion through every usable cell; the script samples the cursor at each pick-up/put-down click, averages per cell, fits over the whole grid | **PASS — full coverage** (bridge 051; the first launch, bridge 050, died on a launch-path defect — a drill run as a bare file could not see the pd2bot package; fixed with a path bootstrap + module invocation, and it was also T-era protocol's first outing: header/instructions/TEST LIVE/TEST CONCLUDED all delivered in-game). All 40 cells, 78 samples. Fit: origin (0.5301, 0.4385), cell (0.0268, 0.0461) = 41.1 x 39.8 px pitch; worst per-cell residual 11.5 px with random scatter, **no row/column drift — the grid is uniform, no gutter**. Agreement with T10's four-corner fit within 7 px origin / 0.8 px-per-cell pitch; T11's denser click-derived numbers adopted as TownConfig defaults. 274 tests green. Post-run user correction: the sweep ends at the bottom-LEFT, not bottom-right — an even row count exits on the starting side; the drill's instruction text said otherwise and is fixed for future re-runs. |

| R62 | decision | Approve the bot-control town drills (T12-T14) | First drills where the bot drives the town layer for real around the user's live character and stash: walk to and click Akara, open the stash and shift+right-click planted junk into it, press belt keys. All in town, all behind the verify-everything logic | **Approved** (`OK, I'm ready`). Drills written on the harness as one suite (`drills/t12_t14_town_steps.py`) so the user sets up once; `navigate._live_navigator` promoted to public `live_navigator` (drills need it now, P4's engine will too). |
| R63 | execute | Run the T12-T14 town-step suite | Setup: spend mana; plant junk in the usable grid; potions in the grid; belt short | **T14 PASS, T12/T13 FAILED — both failures were real gaps in the shipping code, which is what the drills are for** (bridge 056). T14: bot opened the inventory, shift-clicked 3 potions into the belt using the T11 grid calibration, verified the count after each, stopped exactly at minimums — the calibration, PanelInput and verify-everything logic all proven on the live character. T12 FAILED `Akara not in perception range`: perception reaches 80 subtiles and a town is bigger than that, so asking perception and giving up was simply wrong. Fixed: configured NPC approach positions (SP maps are fixed per character — the atlas premise), walk-then-look, re-read after arrival since NPCs pace. T13 FAILED `NavigationError: a blocking panel is open (stash)`: a panel left open from setup made the very first walk illegal, and the step died 10 s later in the navigator. Fixed: every step opens with `_begin_step()` closing panels — which the user's R63 note then confirmed is *semantically* required, not just hygienic. 6 regression tests pin both; 278 green. |
| R64 | inform | PD2 shift+right-click semantics and the materials tab | User-supplied mid-run: shift+right-click means **inventory<->stash while the stash is OPEN**, but **inventory->belt while it is CLOSED**; and the stash has a **materials tab** reached by an X button, holding many more items | **Incorporated.** The gesture's meaning depending on panel state independently justifies `_begin_step()` (a refill attempted with the stash up would quietly stash the potion instead of belting it) — documented in town.py's contract. Materials tab: new drills T15 (find a verifiable tab-state signal, calibrate the X button) and T16 (bot toggles and verifies), built discovery-first because a toggle we cannot verify is a click we are not allowed to make. Which items belong there is deferred to P5's pickit, per the user. |
| R65 | execute | Next battery: T12/T13 re-run + T15/T16 materials tab | Re-run the two fixed town steps, then the materials-tab discovery and toggle | Pending. |

| R66 | inform | T12 walked to Kashya, not Akara | User watched the re-run: the bot ran to the merc-resurrector and looped talking to her ("rather charming"), asked that the two be differentiated | **Root cause is a verification failure, not a coding slip.** R52 recorded the NPC kind table as "verified live" on evidence that did not support it: the user was asked to walk to Akara and kind 148 entered perception range en route, which proves only that *some* NPC came into range during a walk north — anyone standing near Akara gives the identical signature. Acting on it sent the bot to kind 148 expecting a healer. Corrections: `offsets.NPC_KINDS` demoted to UNVERIFIED with the history written in; `heal_at_akara` now names the kind and position it opened a dialog with, so a wrong identity accuses itself instead of looping silently; new drill **T17** identifies NPCs by proximity (stand beside one, read the nearest non-merc ally), yielding both the true kind mapping and accurate approach positions. Nothing walks to a named NPC until T17 lands. |

| R67 | inform | The Horadric Cube cannot be shift-clicked | User diagnosed T13's halt: the cube was in the inventory, and it has special behaviour — right-clicking OPENS it, so the shift+right-click that transfers every other item does something else entirely | **Confirmed and fixed.** The failing item was kind 564, the single such item in the inventory — cube identified. `offsets.UNMOVABLE_KINDS` added (extensible; quest items are the obvious future members), `CarriedItem.is_movable`, and `deposit_to_stash` filters unmovables **itself** rather than trusting the caller's keep-predicate — a pickit that said "keep" would otherwise halt the preamble on the cube every single game. The guardrail behaved correctly throughout: two attempts, then a loud halt rather than clicking forever. |
| R68 | execute | Battery: T17 NPC identification, then materials tab, then T12/T13 re-run | T17 (stand beside each named NPC, no clicking) settles the kind table that R66 questioned | **T17 PASS — and it EXONERATED the kind table** (bridge 058). Measured by standing on top of each NPC: Akara 148 (d=1), Kashya 150 (d=4), Charsi 154 (d=3), Gheed 147 (d=5), every rival candidate 14+ subtiles away. kolbot's table was right and the configured positions were right. **So R66's diagnosis was wrong and the real cause is worse**: the navigator moves by *clicking toward* the destination, and a travel click that lands on a bystander opens their dialog instead of moving. The bot was correctly aimed at Akara and got waylaid by Kashya en route; the dialog then blocked all world input and the walk died. Fixed with `_walk_guarded`: a NavigationError *accompanied by an open panel* closes it and resumes (progress is real each attempt, since the walk restarts from where it stopped), while any other NavigationError propagates untouched as a genuine pathing failure. NPC_KINDS promoted back to verified with the proper evidence; positions updated to T17's measurements; 3 tests; 285 green. **Carried to P4/P5**: this is not a town problem — a travel click landing on any interactive thing does the wrong thing, and in combat left-clicking a monster attacks. The engine needs this at navigator level, not per-caller. |
| R69 | execute | Re-run T12/T13, then the materials tab suite | T12 with the waylay recovery and standoff; T13 with the cube skipped | **T12 PASS** (bridge 059): mana 207 -> 378, heal verified from memory — the waylay recovery and standoff fixed it. **T13 FAILED** on the same class of bug one layer over: `stash object not in perception range`, because after the heal the character stands at Akara and the client only keeps NEARBY ROOMS loaded — the stash was not merely far, it was absent from the unit table. Fixed with `_approach_object` (configured object positions, walk-then-look), mirroring the NPC path. |
| R70 | inform | Add repairing to the town chores | User request: run to Charsi, click her, select trade/repair, click "repair all equipment" once. Also offered to have the merc killed so the resurrection path can be drilled for real | **Built.** `repair_at_charsi` added to the preamble after the heal (both NPC visits); `read_equipped_durability` reads wear off worn items via their stat lists — deliberately separate from `read_carried_items`, since folding a stat read into the general enumeration would tax every snapshot on a character carrying 400+ items to serve one step that runs once a game. Two clicks deep into NPC UI, so both positions are hover-calibrated and verified by effect (R56's lesson: menu rows shift with NPC state). Drills T18 (calibrate) / T19 (bot repairs and verifies). Merc drill: yes please — see R72. |
| R71 | decision | Repair every game rather than on a durability threshold | User: "durability reading may not be necessary - simply repairing every single time at charsi ought to be enough" | **Adopted for the trigger, with one part kept.** Threshold dropped (a number that needs tuning is one that will eventually be wrong); the step now goes for any wear at all, skipping only pristine gear, where the walk would buy nothing and could not be verified either way. The durability *read* stays as the success proof, not the trigger: without it, a repair-all click landing where the button used to be is indistinguishable from success, and the first symptom is gear breaking mid-run in Hell. One memory read per visit. |

| R72 | execute | Pending battery: T13 re-run, T18/T19 repair, T15/T16 materials | Queued back to back on the bridge; each drill announces its own setup | **T13 PASS, T18 PASS, T15 PASS, T16 PASS; T19 ABORTED (clean).** T13 (bridge 060) exercised every fix at once: stash found via configured position, 7 of 8 deposited with per-item verification, cube skipped by rule, **charm space untouched at 24 items**. T18 (bridge 061) captured trade row (0.5879, 0.2072) and repair-all (0.4707, 0.7523), and verified two unknowns as a side effect: **UI_NPCSHOP 0x0C is really the trade screen**, and **durability stats 72/73 are right** — exactly 7 worn items carried a maximum (helm/armour/weapon/shield/boots/gloves/belt, with rings/amulet/charms correctly absent). T19 aborted on its own setup check: gear is at 100%, and a repair with nothing to repair proves nothing. T15/T16 (bridge 062): X button at (0.2188, 0.8426), toggled by the bot twice and verified both ways. **Two findings that outlive the drill**: the materials tab makes the ordinary stash read EMPTY (18 items -> 0), so a bare stash count cannot distinguish an empty stash from the wrong tab; and stash contents populate PROGRESSIVELY (a read caught 10 of 18 mid-flight), so any future count must let the panel settle. Both written into town.py's contract. |
| R74 | execute | T19 needs worn gear | The repair path is calibrated and coded but unproven: gear reads 100%, so the drill correctly refuses to pretend. Take some durability damage (a minute of hitting things in the Blood Moor) and re-run T19 | Pending. |
| R73 | decision | Merc resurrection drills built (T20/T21) | Written while the battery ran. T20 calibrates the resurrect row (only exists while the merc is dead, R56); T21 clicks it and verifies BOTH merc-alive AND gold-spent — either proof alone lies, since a never-dead merc reads alive without a click and gold moves for other reasons. Needs: merc dead, and >50k gold CARRIED (NPCs are paid from the person; this character habitually carries none) | **Resolved 2026-07-30**: T20 calibrated the resurrect row twice (the row moves per menu state — run 2's (0.5716, 0.2569) is the live number); T21 run 1 correctly ABORTED on a live merc (the R56 guard working), run 2 **PASS** — resurrected, verified by BOTH merc-alive and gold 878725 → 828725. *(Marked closed at the M5 closeout reconciliation, 2026-08-03.)* |

| R75 | decision | Inventory-management loop design | User-designed protocol: potions to belt then drink excess; deposit everything into MATERIALS; toggle; deposit everything into REGULAR; halt on overflow. The bot never learns materials-vs-loot — the game classifies | **Agreed, with four refinements accepted.** Q1 rejuvs: not drunk, and they are **materials**, so they need no rule at all — they simply fall through to the materials phase (never the regular tab; no potion ever goes there). Q2: drinking always works even at full health, so no fallback needed. Q3: "didn't move" is EXPECTED in the materials phase, an error only in the final one — the halt is phase-aware. Q4: fast-fail timing in the materials phase (one attempt, short verify), since ~10 expected failures at 2-attempts/3s would cost a minute per run. Q5 **overruled**: the user requires reliable tab identification rather than accepting ambiguity — T22 built to find it. |
| R76 | decision | Gold reserve policy (raised by the agent) | Repairs and merc resurrection are both paid from CARRIED gold (>50k for the merc), so an inventory loop that deposits all gold would strand both — the character would bank its way out of being able to fix its own equipment | Pending — needs a reserve figure, or a rule for withdrawing when short. |
| R77 | execute | Two new probes: T22 stash-tab signal, T23 gold deposit | T22 reads the inventory STORE array (per-grid width/height/item chain) plus location bytes while the user toggles tabs, with the UI array as a control — T15 already proved the UI array does not move, so a deeper read is needed. Setup matters: put an item IN the materials tab first, since an empty tab is what made the last attempt ambiguous. T23 calibrates the gold button and the amount dialog's confirm, and reports whether that dialog raises any panel flag at all | Pending. |

| R78 | inform | T19 never clicked Charsi; T21 looped chatting to Kashya | User watched both: the repair bot never clicked the smith, and the merc drill appeared to click resurrect but then cycled opening Kashya's dialog, selecting introduction, and closing | **One root cause, and it was a bad constant of mine.** The NPC standoff was 5 subtiles — INSIDE D2's click-selection radius — so the navigator's own travel clicks landed on the NPC and opened her dialog, and `_walk_guarded` spent its recoveries closing it and walking again. Proximity was never needed in the first place: clicking an NPC from across the screen makes the character walk over and interact by itself. Standoff raised to 10 and interact range to 12, chosen against the projection maths (a diagonal target beyond ~14 subtiles falls behind the HUD strip and becomes unclickable). Second finding, from the same session: repair had ONE dialog attempt where heal had three, and repair was the one that failed — so all three NPC steps now share a single `open_npc_dialog`, with a test that fails if any of them hand-rolls its own again. Failures now name the click position and the player's position. 295 tests. |

| R79 | execute | Cancel the running drills | User asked to pause, then cancel, all current tests | **Partly possible, and the gap is now closed.** The agent could stop its own polling but NOT the elevated drill: the bridge runs one elevated child at a time and the agent's shell is not elevated, so the only instant stop was Ctrl+C in the bridge window (which also kills the bridge). Fixed in the harness — drills check a cancel file at every wait and abort within a tick; `tools/drill-cancel.ps1` requests or withdraws it. Two failure modes of the fix were closed with it: a stale request must not kill tomorrow's first drill (cleared once per process), and within a suite the cancel is deliberately STICKY, since stopping means stopping rather than skipping to the next test. Also hardened the runner so an abort during the window-in wait — the likeliest moment — no longer escapes it. 298 tests. |

| R80 | inform | T19 still never opened the trade screen; T21 resurrected the merc then looped | User watched both: repair reached Charsi but the trade/repair screen never opened; the merc drill **succeeded** at resurrecting, then cycled clicking Kashya and her dialogs | **T20 PASS** (resurrect row captured at (0.5716, 0.2569)) and the resurrect itself worked — the loop came after. Three causes found, all mine: (1) **clicks landing during a panel's open animation are lost** — the flag goes up before the panel is clickable, the same phenomenon as T15's progressively-populating stash; every in-panel click now settles first, retries bounded, and reports what WAS on screen when it fails. (2) **The dialog wait covered a journey, not a frame**: clicking an NPC from a distance makes the character WALK there first, and 6 s was too short — the retry then re-clicked mid-walk and restarted the approach. Raised to 15 s. (3) **Re-approaching with the dialog already open cannot work**: the walk is refused while a panel blocks input, so `_walk_guarded` closed the dialog, walked, and the next click reopened it — the visible loop. Now an already-open dialog returns immediately. Fourth, and the one that mattered most to the user in the moment: **a bot looping inside the town layer was unstoppable**, because the harness could only cancel its own waits. Every retry ladder now honours an outside veto wired to the same cancel file, so `tools/drill-cancel.ps1` stops a running bot, not just a waiting drill. 303 tests. |

| R81 | execute | Restart the bridge | Given as `& "<repo>	ools\elevated-bridge.ps1"` | **Agent error** — that form is blocked by this machine's execution policy (`PSSecurityException`), and the user reasonably suspected their machine had changed. The working form spawns a child process: `powershell -ExecutionPolicy Bypass -File ...`. Reissued as R82; the memory note now records the failure mode, not just the command. |
| R82 | execute | Restart the bridge (corrected command) | `powershell -ExecutionPolicy Bypass -File <repo>	ools\elevated-bridge.ps1` | **Done** — bridge running. |
| R83 | inform | Conversation handoff | User migrating to a fresh conversation; asked for the state to be recorded and an opening prompt suggested | **`docs/plans/2026-07-29-m5-trial-run/RESUME.md` written**: phase state, the two drills awaiting re-run with their exact bridge commands, the still-missing waypoint calibration (the last big P3 item), every measured calibration, the live facts that are easy to lose, the designed-but-unbuilt inventory loop, and a suggested opening prompt. T20's resurrect row baked into `TownConfig` first, so T21 now runs standalone rather than needing its calibration re-captured. |
| R84 | verify | Window in for T19 (repair) with worn gear | Fresh conversation; user reported gear is worn and asked for the T19 re-run against the R80 fixes | **Half the R80 fixes proved out, and the failure moved one step deeper.** Charsi's dialog opened on the first attempt — the shared `open_npc_dialog` and the 15 s walk-inclusive wait did their job, and "Charsi's dialog never opened" is gone. What failed was the next click: the stored trade/repair row, clicked three times at (903, 179), had no effect, and afterwards NO panel was open at all — so the clicks dismissed her menu rather than opening the shop. Gear read 3 worn items, lowest 92%, so the trigger was real. |
| R85 | inform | Name the test in the in-game failure line; T19's two causes as watched | User asked that failure announcements carry the test number ("Test 19 failed"), and reported watching two distinct problems: the character started near a waypoint that a travel click opened accidentally, and then the same trade/repair screen failure as before | **Both actioned.** (1) The chat conclusion now reads `TEST T19 CONCLUDED — FAILED`; a suite reports several conclusions into one chat window and a bare status leaves the watcher to work out which test it belonged to. (2) The waypoint sighting exposed a real defect, not a nuisance: `uistate` blocks world input on **eleven** panels, but `town.py` hand-listed **three** (npc menu, stash, inventory). A travel click that opens anything else — the waypoint being exactly the case — left the layer unable to recognise or close it, so the walk died as a bare `NavigationError` with no diagnosis and no recovery. Both the recovery check and the ESC-close loop now read the list from `uistate.blocking_panels()`, `_walk_guarded` names the panels that interrupted it, and a test asserts the two lists cannot drift apart again. The fake's `press_escape` was also making the old bug invisible by only ever closing those same three. 308 tests. |
| R86 | verify | Run the T24 Charsi battery (user's suggestion) | User proposed a fresh human-calibration battery for opening Charsi's dialog and clicking through it | **Found the defect behind three failed live runs, in the harness rather than the game.** `capture_hover(last_point=None)` meant "armed immediately", so the FIRST capture of a drill fired on the hand still resting where the user had just clicked the NPC. T18 and T20 both used it and both recorded a click point as if it were a menu row. None now means "arm against wherever the cursor is", so every capture demands a deliberate move onto the target. The poisoned values were deleted rather than adjusted. |
| R87 | decision | Redesign the calibration: full human control, and transferable abstractions | User: the calibration must run with no bot input at all (keeping human-calibration vs bot-control a real distinction in the log), and the steps should be built from abstract elements — opening an NPC dialog is one task regardless of the NPC, clicking a line is one task regardless of the line | **Both built.** `pd2bot/uipoints.py` makes a click target data — `UIPoint(name, panel, fraction, opens)` — with one `TownLayer.click_point()` for every in-panel click, defaulting to the point's expected panel or taking a better proof (durability, live merc + gold spent). Points resolve BEFORE the walk, so an uncalibrated step fails instantly rather than after crossing town. The four separate config fields and their drifting consumers are gone. T25 is the generic calibration drill: a battery is a list of targets, not new code, so Charsi's rows, Kashya's row and the waypoint destinations all calibrate through it. T18/T20 were split out of the bot-control drills entirely. |
| R88 | verify | Run T25 charsi | First run of the rebuilt battery | **Aborted mid-run by the user** — but not before capturing the trade/repair row and proving it by effect (click settled to npc_menu+npc_shop). It also exposed the next defect, below. |
| R89 | inform | Chat into an open NPC dialog selects a dialog option | User diagnosed the recurring interference: the agent chatting in-game while a dialog is open presses Enter, which the dialog takes as choosing an option. Asked for the calibration to be briefed up front and then run silently | **A real input-safety hole, of the M1 "Save and Exit" class, open since M4.** `Chat` guarded the *typing* but treated the opening Enter as harmless — and Enter is not harmless with a panel up. Worse, the console then never opens, `say` raises, and the drill harness retries once a second for a minute, choosing a dialog option every time. `Chat` now refuses while any blocking panel is open, `DrillRun.wait_until` gives drills a silent wait, and T25 delivers its whole script before anything is open and then says nothing until the panels close. Third finding: **`drill-cancel.ps1` was itself broken** — an em dash inside a string, and PowerShell 5.1 reads a BOM-less .ps1 as ANSI, so the stop button died on a parse error at the moment it was needed. All three tool scripts are ASCII-only now and cancel/clear were exercised both ways. The silent re-run then PASSED, and its row sits 46 px from run 1's — because run 1 had chatted into the dialog between the hover and the click, which is the same bug measured from the other end. |
| R90 | decision | Waypoint calibration in Hell Cold Plains: go or hold? | Agent flagged that the battery puts the character in Hell Cold Plains under manual control, standing still 3 s at a time, in an area the user had called "fairly dangerous" | **Go — and it PASSED**, both rows measured and each proved by arriving: `waypoint.cold_plains (0.3249, 0.2928)` -> area 3, `waypoint.rogue_encampment (0.3197, 0.2384)` -> area 1. The classic-D2 area ids were confirmed live in passing. Pairing the calibration with the trip collapsed P3's supervised round trip and two calibrations into one sitting. |
| R90a | verify | Run T26, the bot-driven waypoint round trip | Agent flagged that the bot has no combat/reflexes/chicken in this drill and takes the return leg immediately | **PASS first attempt.** Both legs' 0->1 panel edges were attributable to our own click (M2's stuck-at-1 suspicion did not recur), 2 object clicks for 2 legs so the standoff approach landed in range first try, and the Cold Plains waypoint was in perception at (5269, 5714) on arrival — the missing configured position for that area never mattered, because you land on it. P3's last big capability. |
| R91 | execute | Calibrate the resurrect row in the dead-merc window | User was staging state for a full-routine test and about to kill the merc; `kashya.resurrect` was still uncalibrated after T20's value was discarded | **Blocked on a dead bridge, not yet run** — see R92. Flagged in advance that calibrating *resurrects* the merc (the click is the proof), so the merc must be killed twice: once for the calibration, once for the routine run. |
| R92 | execute | Restart the elevated bridge | Drill did not start when the user tabbed in; the queued `091-t25-kashya.cmd.ps1` was still unclaimed while every earlier command had been renamed `.done.ps1`, and no bridge process existed (it died after T26 completed at 17:59) | *pending* — and note the bridge quarantines stale `*.cmd.ps1` at startup by design, so a restart does not rescue the queued command; it must be resubmitted. |
| R93 | execute | Stage the world for T27 and run the full routine | User asked for a drill exercising the whole town routine before the P3 gate; agent built T27 on the production `run_preamble` with a per-step coverage table judged from the world, not from the steps' own reports | **Two runs, two real defects, both one-shot sends.** Run 1 died at the heal: `close_panels` pressed ESC once and gave up after 3 s, and Akara's dialog is closed *immediately* after being opened, so the ESC arrived mid-animation and was swallowed. Run 2 reached the repair and died on the row click. Both fixed by `send_until` — one path for "send, verify, retry bounded", now shared by ESC, the inventory toggle and panel clicks, because clicks, keys and ESC are the same problem and three similar answers would drift. |
| R94 | inform | Announce test conclusions in-game | User: when a test ends or aborts, say so in PD2 chat so they can stop waiting | **A real hole, and the log showed it precisely**: T27's conclusion came out `(never delivered)` because a failing drill ends mid-panel and `Chat` now refuses while a panel is open — the message failed exactly when it was most needed. The harness now clears the way first (ESC if the drill was already sending input, waiting if read-only, never after a death) and failures chat their reason too. |
| R95–R103 | inform | The calibration arc: NPC dialogs cannot be located by position | User diagnosed that NPC dialog boxes are drawn relative to the NPC, and NPCs wander; later corrected the agent's row-pitch premise (three rows, ~14 px font, not 180 px apart) | **Four positional approaches, all doomed for one reason.** Three screen fractions were stored for Charsi's trade row, each verified by effect when taken and each stale by the next opening. T29 then measured a genuine NPC-anchored offset (-211 px, holding to 5 px while her screen position moved 60+), which T30 scored only 4/5. The agent's supporting premise — a 180 px row pitch from T28 — was wrong and had gone unchallenged across three drills; the user caught it by eye. |
| R104 | inform | NPC dialogs are keyboard-navigable | User discovered arrows move the highlight and Enter selects, and proposed selecting rows by ORDINAL — transferable to any NPC, including Kashya's state-dependent resurrect row | **This retired the entire problem.** T34 mapped Charsi's menu exactly: highlight opens on row 1, Down advances, wraps at the end (0 downs = Talk, 1 = Trade, 2 = Cancel, 3 = back to Talk). `UIPoint` gained a keyboard-row kind, `PanelInput` gained a guarded `press_key` (neither existing gate could send those keys), and retries REOPEN the dialog rather than pressing on from an unknown highlight — which at Kashya would cost 50,000 gold. **T19 then passed first time after failing four times**: 6 worn items, 12 durability missing -> 0. The row's pixels move when the NPC paces; its index does not. |
| R105 | inform | Read Kashya's dead-merc menu aloud | With the merc dead: which rows, in order? | **TALK / RESURRECT <name>: 50000 / HIRE / CANCEL** — so resurrect is row 2 of 4. Read rather than probed: a wrong row at Kashya costs 50,000 gold and consumes the dead-merc state being measured. Baked in; **T21 then passed first time** (merc alive, exactly 50,000 spent). The reading also exposed the hazard concretely: with the merc ALIVE row 2 is HIRE — same index, different action (R56) — so the step re-confirms the merc is dead immediately before selecting, and a test pins that no Enter is sent if it revived in between. |
| R106 | inform | T35 run 2: dialog opened en route killed the stash step | Bot clicked Warriv on the way to the stash; `InputRefused` killed the deliberate click | **The object path never had the NPC path's protection.** A travel click landing on a bystander on the walk's LAST click leaves their dialog open — `_walk_guarded` only recovers when the navigator fails, and an arriving walk succeeds. `open_object_panel` now clears blocking panels before and after the approach; regression test added. |
| R107 | inform | T35: the belt did not fill before drinking | User: shift-click potions into the belt BEFORE drinking excess | **Design gap confirmed.** `refill_belt` implements R48 (top up to minimums), but R75's loop needs the belt FULL so what remains is genuinely excess — otherwise the bot drinks potions it had room to carry. New `fill_belt` moves every potion the belt will take, tracking fullness by TYPE (the belt routes by column, so a full healing column is full for every healing tier); minimums stay as the loud-halt backstop. The test fake gained belt capacity — without it, `fill_belt` could never learn the belt was full. |
| R108 | inform | T35 run 3: 'cannot identify the stash tab' with 29 items in it | User had fetched a rune, which left the stash on the materials tab — the ambiguous state | **A one-shot probe turned one lost click into a false diagnosis.** The tab probe clicked once, read once, concluded 'unreadable'. It now retries before drawing that conclusion — the same lesson as every other send (R94), reintroduced and re-fixed. |
| R109 | decision | Detect the stash tab directly rather than by toggling? | User: the bot should know which mode the stash is in | Led to T36 + R110. |
| R110 | verify | Two stash reads, one per tab | User held the stash on materials, then regular, while the agent read the store array | **No direct signal exists.** Every store is byte-identical between tabs except the stash store's item-chain head, which the game nulls while materials shows — same 10x15 shape, same grid pointer, no separate materials store. Also found `dwStoresCount` reads 258 (garbage), so T22's reader always bailed; the store array itself is readable and confirms known geometry (belt 4x4, inventory 10x8, stash 10x15). Conclusion recorded in town.py: toggle-and-look is the only tab signal, and an empty regular stash is genuinely unresolvable (the loop refuses, by design). |
| R111 | inform | T27: bot stuck in a loop misclicking the waypoint | User watched the bot open the waypoint menu repeatedly en route to Akara; suggested moving the mouse aside + ESC | **Fixed at navigator level after two failed mitigations** (a 7-subtile sidestep, then a 14-subtile detour around the named obstacle — both insufficient: the waypoint sits at the same y as Akara's approach point, dead on the route). The real defect: travel clicks land on whatever occupies the clicked subtile, and every retry re-projects the same point. `Navigator` now nudges any travel click aimed within 4 subtiles of a known interactive unit (objects AND NPCs, read fresh per click) to land 6 clear — the mouse-pointer observation ('it never moves') was the tell: the bot only moves the cursor when it clicks, so the fix had to move the CLICK, not the pointer. Monsters deliberately not avoided (a click near one is at worst an attack). 345 tests. |
| R112 | inform | What is the item stuck in the inventory (kind 534)? | T27 halted: one item would not enter the regular stash | User identified it directly: a **Tome of Identify**, and the bot plain-right-clicked it — which CAST it — rather than shift+right-clicking. → R113. |
| R113 | inform | The shift race | User-observed: the tome was used, not stashed | **Shift and click were sent in the same frame**, in both send paths — a race the game can resolve as an unmodified click. Invisible until now because an unshifted right-click on a potion just drinks it; on a tome it casts, and the identify cursor then ate the retry. `_MODIFIER_SETTLE_S` (one 25fps frame) now sits on both sides of every modified click, in PanelInput and in GatedInput's stand-still path (M5 combat would have met the identical bug). Ordering + settle pinned by test. 346 tests. |
| R114 | decision | The P3 review gate | Approve P4 (behaviour engine, sim-only) with P3 complete: every town step, the waypoint round trip, and the R75 loop live-proven through the production `run_preamble`; full coverage demonstrated across the T27 halt-and-resume pair | **Approved** — user closed P3. Teach explainer written (`docs/learning/2026-07-31-when-the-tools-lie.md`, +5 glossary terms), committed as cb63b67 on `m5-trial-run`, pushed to origin. P4 begins in a fresh conversation. |

### M5 implementation — P4 (behavior engine, sim-only)

| ID | Type | Title | What was asked | Outcome |
|---|---|---|---|---|
| R115 | decision | IdleBail wiring at the frozen cycle boundary | P4 shipped `IdleBail` as a `ChickenExit` subclass so the unmodified M4 cycle leaves the game; the runner counts idles separately (2 consecutive → loud `IdleLoopHalt`). Wrinkle: the cycle's own chicken counter also counts an idle bail, so a mixed chicken-then-idle pair can halt with the vitals message (the per-game `chicken:` line still names the idle bail). Options: (a) accept as-is, (b) authorize a one-line cycle.py change so idle bails bypass the chicken counter. | *pending — carried to the P5 gate (R116)* |

### M5 implementation — P5 (combat + pickit, sim-only)

| ID | Type | Title | What was asked | Outcome |
|---|---|---|---|---|
| R116 | decision | **The P5 review gate — go/no-go for live** | Present the sim decision trace (`p5-sim-trace.md`), the pickit file, and the final `config/necro.toml` numbers; ask whether to begin P6's staged live acceptance and whether any threshold changes are wanted first. Also carries R115's IdleBail question. | *deferred — at the gate check-in the user supplied the real pickup spec (R117), which reshapes the pickit before live acceptance makes sense* |
| R117 | inform | The real pickup spec, the potion-cap town rule, and the inventory cleanse | User check-in at the gate: (1) a full pickup whitelist — potion caps (2 heal / 2 mana / 4 rejuv), flawless+perfect gems/skulls, all runes, unique charms, rare+unique jewels/rings/amulets, named unique/set/socketed bases, and ~30 PD2 quest-and-map items — with `filters/local/eqN-All-In-One.filter` as the display/vocabulary reference and "easily amendable by a human" as the format bar; (2) the town loop should drink only potions in excess of 2 per type, keep up to 2 of each in inventory, and never stash potions; (3) a new accidental-pickup recovery: CTRL+right-click drops items, an "inventory cleanse" whitelist-check runs only when no enemies are near, queued after a failed pickup. | Recorded; clarifications batched as R118. Supersedes parts of R46 Q3 (simple pickit) and R75 (drink-all-excess, rejuvs-to-materials). |
| R118 | decision | Pickup-spec confirmation batch (Q1–Q5) | Q1 potion-cap semantics · Q2 rejuv end-state · Q3 rule format · Q4 discovery drills for item ids + socket stat · Q5 cleanse details (town handling, drop-at-feet) | **All five answered.** Q1: belt first (4 rejuvs), then up to 2 of EACH type in inventory, all excess drunk "just like other potions". Q2: never stash potions, drink all excess including rejuvs — retires R75's rejuvs-to-materials. Q3: TOML stays. Q4: approved. Q5: yes — dropping junk is always advisable as overflow insurance; the town preamble drops rather than stashes non-whitelisted items. Built same-day as phase 05b (sim-side); T38/T39 drills pending → R119. |
| R119 | execute | Run the T38 + T39 discovery drills (bridge) | Start the elevated bridge, then run T38 (socket stat) and T39 (item ids). Both read-only; the bot sends nothing but its own chat. | **T38 PASS** (see R124). T39 not yet run — redesigned first around the chat return channel (R120–R123), which landed between the request and the run. |
| R130 | decision | GitHub rejected the push: email privacy | Four commits (110514f, 17f7d83, fe19d88, 4d54f31) are ready but `git push` is declined — the author email `james.stonelunde@gmail.com` is protected by GitHub's email-privacy setting. Note the remote already holds `084ba70` with that SAME email, so the setting was enabled after that push. Options: (a) turn the protection off at github.com/settings/emails and push unchanged; (b) switch to the GitHub `...@users.noreply.github.com` address and rewrite the four unpushed commits (safe — none are public). | **Resolved as (b), and it needed no decision from the user after all** — the rewrite and the push both went through in the same session the request was written; only this row was left saying otherwise. Every commit from `544418b` on carries `45543733+Anthoussae@users.noreply.github.com`, `git config user.email` is set to it locally so new commits inherit it, and `origin/m5-trial-run` has had them since. GitHub's email-privacy protection stays ON, which is the privacy-preserving end state and the one the user had chosen. **Worth noting for the record**: the three commits BEFORE the rewrite (`084ba70`, `cb63b67`, `b6d7fd3`) are public with the gmail address, and scrubbing those would mean rewriting already-pushed history — not proposed, and the user's call if it ever matters. Lesson for the log's own hygiene: a request resolved by action rather than by an answer is the kind that gets left `*pending*`, and a stale `*pending*` cost real time here (it propagated into RESUME.md and blocked the next session on a decision that had already been made). |
| R131 | inform | "We can try again — just make sure to ctrl+right click" | User, on T43's abort: a plain right-click drinks a potion, so check the modifier is really being held. | **Right instinct, wrong culprit — and the retry proved it.** T44 tried four ctrl spellings on a GEM (whose unmodified right-click is a no-op, so a failure would cost nothing and be visible, unlike a potion where both hypotheses end in a drunk potion). The FIRST variant worked — the original `VK_CONTROL` 0x11 with a one-frame settle — and a ground read then found T43's "lost" antidote lying at (5890, 5735). It had dropped correctly all along. **T43's verification was the defect**: it read the ground ONCE, immediately, and a just-dropped item takes a moment to enter the unit table. Fixed by polling; the speculative `VK_LCONTROL` is kept only as a documented alternative with a note not to reach for it. Same shape as the P3 calibration crisis (R86): the instrument was wrong, not the game. |
| R132 | decision | Cleanse strictness + fullness handling | User asked whether the cleanse is too conservative and proposed a conservative/ruthless toggle, plus routines for full inventory and full stash. Findings: the only real over-keep is the socket-rule gap (`CarriedItem` has no `sockets` field, so permissive mode keeps every necro head / archon plate); full-inventory handling already exists (suppress + queued cleanse + next preamble); `StashFull` escapes the engine like 002's `InputRefused`. Proposed: (a) add `CarriedItem.sockets` (read live-verified by T38) — closes the gap exactly, no toggle needed; (b) optionally add the strict/permissive toggle anyway; (c) fold StashFull clean-stop into the 002 fix; (d) stash headroom warning; placement: P6 wiring checklist + 002, before stage B. | **(a) — the sockets fix, no toggle.** The cleanse becomes exactly as ruthless as the pickit rather than gaining a second strictness axis; a toggle would have been dormant config today and actively dangerous without the sockets field (it would drop the 3-socket bases the bot collected). Placement as proposed. |
| R148 | verify | Stage B runs 6-9, and four behavioural notes | Four supervised runs with the user watching. Their notes: (1) no 3 revives before approaching; (2) too passive, standing still, "the best defense is a good offense"; (3) dithering after running to an NPC/stash; (4) bone armor has a slow cast animation that following commands interrupt. | **The full run completed twice (`[CVRL]`) and fought once.** Note 1 fixed by gating the dash on the revive wall (with a release so it cannot deadlock when no corpse can be raised). Note 2 by dropping `restrike_s` 6.0 -> 2.0 and drifting instead of freezing wherever the module used to return None with hostiles alive. Note 4 by a 0.4 s cast settle — which the review then flagged (002) as blocking the tick the survival ladder needs. Note 3 got a halved poll interval, which review 003 argues made it WORSE, since town polling reads every inventory socket at 10 Hz. **The user then called the review**, correctly: three of the session's regressions were mine, and the last batch of changes never ran live at all. |
| R147 | inform | "2 screens in each direction around the waypoint" | User asked to expand the travel radius around the Cold Plains waypoint. | Radius 50 already IS ~2 screens (a screen is ~24 subtiles). The reason the bot does not roam is that `clear_radius` has no PATROL — it stands at the arrival point and waits for the radius to read clear, and since perception (80) exceeds the radius it can verify from a standstill. Ran the radius-150 file instead for more ground. A real patrol (visit sample points so perception sweeps the area) is a new feature and P6 puts features out of scope — **open decision**. |
| R146 | execute | Run T47, the hotkey audit | Offered after `SkillSwitchFailed` recurred; presses F1-F6 and reads back which skill each selects, to separate stale bindings from a keypress that never lands. | *not run* — the user chose to go straight to another stage-B attempt, reasonably, since the settle fix addressed the mechanism they had observed. It has now recurred in three runs on three different skill pairs and **T47 is still the fastest way to settle it**. |
| R143 | execute | Enter a game to read the inventory | To find why a 3-socket armour was picked up and a rejuv ignored — the pickup and cleanse evaluations appeared to disagree, which should be impossible. | **Superseded by a better answer.** The user named the item — a Wire Fleece — and the live code table settled it without any probe: kind 445 is Wire Fleece, and this vocabulary called 445 `kraken_shell`. Not a socket bug at all. Led directly to R144. |
| R134 | decision | The stash tab is unreadable on this character | Stage B (R133) died in the preamble: `ensure_materials_tab` refused because the regular stash lists nothing on either side of a toggle. Probe: **350 items in `game_location 8`** (PD2's expanded stash, first seen in T38) and **0 in location 7**, the container the whole P3 stash model is built on — so the toggle-and-count inference has no signal. Offered: (a) marker item, (b) teach the layer about location 8, (c) both in sequence. | **The user supplied a better mechanic than any of the three options, and it retires the problem rather than working around it**: shift+right-click on a MATERIAL routes it to the materials tab even when the regular tab is displayed, so the bot never needs to toggle to deposit. Counter-proposed and accepted: **toggle-on-REFUSAL** rather than toggle-on-inference — deposit onto whatever tab is up, and only if something refuses, toggle blindly once and retry just the refusals; still refused means genuinely full. That needs no tab signal at all (verification is the item leaving the inventory, which is tab-independent), needs no permanent marker item, self-heals if the stash opens on materials, and is correct whether or not the auto-route works — it merely gets faster when it does. User: "Let's do this robustly and properly, no point in kicking the can down the road." Chose (a) including the confirmation drill. |
| R137 | execute | Enter a game so the waypoint state can be read | Stage B attempt 2 cleared the preamble (the R134 fix worked) and then died on `waypoint still not visible after walking to (5884, 5709)` — impossible on its face at a 10-subtile standoff against an 80-subtile radius. A read-only probe printed both sides of the locality filter to separate "in the unit table but dropped by perception" from "never loaded". | **It was a perception bug, and a much bigger one than the waypoint.** The probe found the waypoint present at exactly (5884, 5709), d=24, while `snap.objects` listed objects at d=31/35/46/57 and omitted it. Cause: `scan_units` deduped through ONE id set shared across the monster, item and object passes, but **D2 unit ids are unique only within a type** — the waypoint was object id 11 and a town monster was also id 11, monsters go first, and objects (enumerated last) lose every collision. On this character the fix takes the snapshot from **9 objects to 22**: thirteen objects had been silently missing from every snapshot the bot ever took, intermittently and seed-dependently. Dedup is now keyed by (type, id); 2 regression tests. |
| R135 | execute | Set up and run T45 (materials auto-route) | Open the stash on the REGULAR tab with a rune/gem and one ordinary item loose in the inventory; the bot shift+right-clicks one of each and reads where they land. | **PASS, and it found more than it asked for.** The material (kind 689, a gem) left the inventory while the regular stash stayed at 0 — **the user's auto-route observation confirmed**. The ordinary item (534, Tome of Town Portal) went to the regular stash, 0->1. The surprise was where the gem went: **neither** readable container. A follow-up probe found kind 689 absent from location 8 entirely (0 of 350), killing the stacking explanation — so **the materials tab is not enumerable through the player's inventory chain at all**, which retro-explains T15/T36 finding no store for it. Consequences: the deposit was rewritten to toggle-on-refusal (R134), `STORAGE_EXPANDED_STASH = 8` is now named, and the stash-pressure warning counts both readable containers instead of only the empty one. |
| R133 | verify | **P6 stage B — the first supervised fight** | Be at the machine with hands near the controls while the agent drives one game through the bridge: `--games 1 --chicken 50 --run runs/cold-plains-stage-b.toml`. Pre-flight read (T204): MaqiuDoubing 1234/1234 hp, in a town game on seed `0x4E52715F` (matches the atlas), can-act yes, 7 summons up, **merc at 8% hp**. The bot leaves the current game itself and creates a fresh Hell one. Watch: the wait-for-revives beat, dash/strike/retreat, bone-armor recasts, desecrate→revive, first potion drinks. Abort by taking the mouse (focus guard yields) or ESC (offline SP pauses). | *pending* |
| R129 | decision | P6 stage A — go | User: "OK, lets do P6 stage A. We leave B for tomorrow. After that, we do a yona review, and ideally get ready to push and finish for the night." | **PASSED** (via T43's audit + T44's verification). The ctrl+right-click drop is live-verified by effect, the last untested input path in the bot. T43's whitelist audit also did its job: of 12 carried items it would keep 8 and drop 4 — the two tomes, a misc item, and a **magic grand charm** — which is correct for an accidental pickup and would have been unwelcome for a charm, exactly the distinction the startup-baseline guard (R128) exists to protect. No combat, no travel, nothing lost. |
| R128 | decision | The R127 option-A review table | Presented all 53 remaining names with proposed code, live-read kind, confidence and evidence (`pickit-id-proposal.md`); asked for corrections on the 7 low-confidence elite armours and the 6 recipe-triangle inferences. | **"Looks right to me. The ones you didn't find likely can't drop (can only be crafted)."** All 44 proposals accepted; vocabulary now **105 names, zero pending**, and the inventory cleanse gate opened by itself. The craft-only remark did real work: those 9 names are retired rather than left pending (a pickup rule for an item that cannot drop can never fire, and leaving them pending would have held the cleanse disabled forever) — **and it exposed a hole**: an item that can only be crafted can never teach the whitelist its id from a live pickup, so one sitting in the inventory would look exactly like junk. Closed with a startup-baseline guard: the cleanse now refuses to drop anything the bot was already carrying when it started, because the feature exists to clean up the bot's OWN accidents. 584 tests. |
| R127 | decision | How to resolve the last ~45 names | (A) a table to eyeball, (B) extract the MPQ string tables, (C) leave pending and resolve opportunistically. | **A, with C as fallback.** A worked: the filter names items through their CUBE RECIPES — each rune's tooltip reads "3x Hel + Key = Io", and the quest items name each other ("Cube this item with: Black Soul Stone & Prime Evil Soul" identifies the third by elimination). 44 of 53 resolved from that plus block position; B never needed. |
| R126 | verify | T39 run 1 — ABORTED by the user, and worth more than a pass | User: the drill demanded "very precise nomenclature I'm not familiar with" — it rejected `hel rune`, `rune`, `hel_rune` and `Perfect Sapphire` — and they do not have many of the listed items to hand. Asked for a redesign, and offered to help if some information cannot be got from the game or filter files. | **The abort log caught a real bug in MY work, in its first 30 seconds.** The user's Hel rune read kind **713 = `r15s`** and their Perfect Sapphire **690 = `gpbs`**, while the R125 join had bound those names to the classic `r15` (639) and `gpb` (586). **PD2 carries two parallel item families** — the classic records and an `s`-suffixed set (`r01s`-`r33s` at 699-731, `gzvs`-`skzs` at 685-698) — and the `s` family is what drops. So ~49 of the 53 auto-resolved ids were wrong. This is exactly the systematic mis-pairing the spot-check existed to catch, and asking for a rune and a gem FIRST is why it cost seconds. Fixes: `ItemTable` now maps a name to a TUPLE of kinds (both records bound, since an item is one or the other); vocabulary regenerated with the two live ids asserted at generation time; regression tests added. Second defect, purely UX and also mine: the drill accepted only PENDING names, so a correct answer for an already-known item was rejected as "not on the wanted list" — the question was open but the answer space was not. |
| R125 | inform | The loot filter is a naming source — mine it | User, at the T39 hand-off: "dropped item display cosmetics should be readable in `eqN-All-In-One.filter`... ideally you should be able to retrieve all necessary information from there." | **Led somewhere better than the filter itself.** The filter names items by D2 *code* (`9kr`, `uap`), not by the numeric kind perception reads, so it cannot supply ids — and mining its `//` comments proved noisy (a "Troll Belt" comment matched code `rbe` off an unrelated token). But chasing the gap found the real Rosetta stone: **`dwTxtFileNo` is an index into the game's own item-record array**, which holds each item's code. Located by triangulation (T42) rather than an offset — 533/534 are adjacent so their distance in memory IS the record stride, and 564 must then land correctly too. Four bases fit (each record repeats the code as code/normcode/ubercode/ultracode); the true one is identified structurally, as the only column containing elite codes. Result: **kind → code for all 800 items**, corroborated by six independently-verified ids landing exactly right (index 0 = `hax`, 530/531 `rvs`/`rvl`, 606 `hp5`, 610/611 `mp4`/`mp5`). 53 of T39's ~100 names then resolved mechanically (all 33 runes as one contiguous block, the gem/skull blocks, charms, jewellery), cutting T39 roughly in half and turning the rest into a shorter conversation plus a spot-check. |
| R124 | verify | T38: confirm the socket read, by dropping the item | The bot stated every socket count it read (equipped 1/1/5/1/3; inventory kind 441 = 3) and asked the user to drop the socketed inventory item ONLY if all were right — an action as the answer, since the harness reads the game rather than a typed reply. Dropping = yes, cancelling = no. | **PASS, first run.** The item was dropped, and it read **3 sockets carried and 3 on the ground** — the ground path being the one the pickit uses. `STAT_NUM_SOCKETS = 194` is now live-verified and the socket-conditioned rules arm themselves. Two vocabulary freebies from the same read: **534 Tome of Identify** (72 charges; matches R112's cast-by-accident identification) and **533 Tome of Town Portal** (64). Also surfaced: a large unnamed container, `game_location = 8`, holding 25 socketed items at grid coords up to y=86 — almost certainly PD2's expanded stash. Harmless today (`main_inventory` filters on container), recorded so a future stash feature does not rediscover it. |
| R123 | execute | Run S2: the same exchange, solo-project framing, gated on a real submission | User feedback on S1: drop the team/deadline fiction (solo project, no other people), and *guarantee* it waits for the reply before confirming. | **Worked.** Heard `C` after exactly 1 submission, confirmation followed. The guarantee is structural, not a better filter: `ChatListener` now takes a `console_open` callable and releases a line only once a chat-console open→closed edge has been counted since the question, one submission spent per message. Nothing the bot says moves that flag, so self-answering is impossible even under drift. A new line seen before the edge is *held*, not dropped (the game writes the line and clears the flag in an order we do not control). 8 tests. |
| R122 | execute | Run S1: simulate a Claude coding-session exchange in-game | Immersive UX rehearsal, not a drill — no TEST banner, no pass/fail, re-runnable with identical wording. Numbered+titled request, 2-3 sentence statement, one multiple-choice question, then an acknowledgement. | **Run 1 answered its own question**; run 2 correct (heard `B`, confirmed after). Run 1 is the valuable one: it exposed that `CHAT_LAST_LINE` is a *window*, not a string start — the bot read its own line back shifted two bytes (`'laude] or (C) leave...'`), past the `startswith("[claude]")` filter. Fix: identity now matches against recently-spoken text within `DRIFT_SLACK`, never the prefix position. Also retro-explains T41 run 1's crash (a 0xFF colour byte at the read position). Offsets doc corrected against its earlier, more confident claim. |
| R121 | execute | Run T41: chat call-and-response (3 rounds) | Vocabulary (RED/GREEN/BLUE), entropy (a 4-digit number the user picks, so a lucky guess cannot explain a pass), then reliability (three words back to back, as fast as the user likes). Read-only. | **PASS on the second run — the channel works.** Heard `red`, then `2345`, then 3/3 rapid words (`hello`, `toda`, `bafh`) with the console-close counter independently agreeing 3 were sent. Run 1 FAILED before round 1 on a `UnicodeEncodeError`: the buffer held a non-ASCII byte and the bridge's stdout is cp1252, so *reporting* killed the drill. Fixed both ways — stdout uses `backslashreplace`, and `read_line` strips to printable ASCII (that text is echoed back through `Chat` one character at a time, so it must never carry a byte we have not thought about). Offset re-derived by scan at the top of each run: nonce landed at `D2Client.dll+0x1234DC` = `CHAT_LAST_LINE + len("[claude] ")`. Still one client process (pid 24996); NOT yet verified across a restart. |
| R120 | execute | Run T40: two marker rounds in the chat line | Out-of-band from P5 (an M5P3 follow-up): the user asked whether the bot can *read* what they type in chat, not just write to it. T40 has the user type a rare marker, scans the client's committed memory for it, and repeats with a second marker to separate a real buffer from a coincidence. Read-only; the bot sends only its own chat messages. | **PASS.** Scanner self-check first (character name found, so a "not found" would have meant something). Both markers landed at the SAME static address — `D2Client.dll+0x1234D3` (ASCII, NUL-terminated, holds the last chat line) — plus a heap line-buffer at `0x19CFA2` carrying the rendered line *with the sender name*. Caveat: the user posted before each dwell expired, so both scans were post-submit — the edit buffer (text before Enter) is still untested. Reading posted chat is feasible; T41 (call-and-response) is the next step. |

### Out-of-band — the game stopped launching (host environment, not the bot)

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R149 | execute | Reboot to reshuffle ASLR, and close the stuck `Game.exe` | After the 2026-07-31 22:13 restart, PD2 died on `UNHANDLED EXCEPTION: ACCESS_VIOLATION (c0000005)`, `eip=656c6f73`, before window creation. Diagnosed as a DLL base collision: this boot ASLR put `apphelp.dll` at `0x6F860000`+`0xA6000` (→`0x6F906000`), which covers `D2Win.dll`'s fixed preferred base `0x6F8E0000`, so D2Win relocated and D2's hardcoded absolute calls landed in apphelp's string data (`0x656C6F73` = `"sole"`). Bot fully exonerated: bare `Game.exe -w -ns`, no launcher and no PD2 args, crashes with byte-identical registers. Confirmed by boot history: boots at 7/25 11:56 then 7/31 22:14 and nothing between — the client started 7/28 00:17 and survived as one process for days (the pid-24996 note in R121), so 22:14 was the first fresh ASLR roll since it last started. **Fast Startup is ON (`HiberbootEnabled=1`), so "Shut down" resumes the saved kernel session and does NOT re-randomize — it must be "Restart".** | **Resolved by R150's probe**: after the true Restart the client launches again — attached to pid 16852, character alive in town, can-act yes. The ASLR-collision diagnosis and the Restart-not-Shutdown rule are recorded in memory for recurrence. *(Marked closed at the M5 closeout reconciliation, 2026-08-03.)* |

### M5 implementation — P6 staged acceptance (stages C–E)

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R210 | execute | STAGE E: 3 unattended games — the user said go | The M4-pattern real CLI via live-run.ps1: --games 3, radius 150, chicken from config (35, the R206/R208 decision). Clean = created, Hell-verified, preamble, waypoint, radius-150 clearance, pickit honored, left — monitor SILENT (no chicken, no idle-bail), zero human input. Any death: full stop for a conversation. Report to be captured into the M5 planning dir per the plan. | **PASS — 3/3 CLEAN: [CVRL] [CVRL] [CVRL], monitor SILENT (no chicken, no idle-bail, no death), zero human input, chicken 35 armed throughout.** Games 1-3: 426/355/364 ticks, 12/14/18 reflex fires, all five steps done each. Report captured to docs/plans/2026-07-29-m5-trial-run/stage-e-acceptance-report.txt. **STAGES A-E ALL PASSED — M5's live acceptance is COMPLETE.** Remaining: closeout items 1-10 (behavior.md, ADR accepted, README/CLAUDE.md/roadmap/pointers, teach step, sweep, instruction log, archive + _DONE.md), then the M6 Countess yona-plan pass. |
| R209 | execute | T66 (autonomous): find the ALT label-display flag in memory | T65 wrap: run 4's two 'hits' were GROUND-ITEM EXPIRY (user-sourced: floor items time out), run 5's 245 misses were honest — junk probing is retired until label state is controllable. T66: press ALT 4x, diff all module memory between presses, keep bytes alternating in exact lockstep (s0==s2==s4, s1==s3, s0!=s1). A readable flag kills the toggle-parity hazard and lets the bot choose its label perspective; then T65 is recreated on the user's fresh test items. | **PASS — a UNIQUE flag: BH.dll+0x14d2ca, the only byte in 117 modules alternating in lockstep with four ALT presses (1=showing).** Baked in as offsets.BH_LABEL_DISPLAY + units.label_display_on(); 864 tests green. T65 v4 then ran label-verified on the user's fresh stock: 6 dropped, 207 probes, 2 hits — BOTH at the label band (y=-48) as by-catch, while the tiny rune/gem/charm SPRITES survived 58-147 direct probes. CAMPAIGN CONCLUSIONS so far: (1) potions pick reliably at the sprite offsets (T63/T59, unchanged); (2) small classes are effectively LABEL-clicked — labels ON is mandatory for field pickup, and the (0..16,-40..-48) tail of the offset schedule covers a sparse drop's label; (3) pile-stacked labels displace unpredictably — dense-pile accuracy is bounded by the game, and accept-and-cleanse (R208) remains the answer there; (4) expiry, not clicks, explained the earlier phantom hits. NEXT SESSION: wire the label policy (ON to pick / OFF to travel, parity-safe via the flag), extend the schedule tail for label band, sparse-drop validation (T67), navigator sprite/label-band nudge. | **CAMPAIGN LANDING (this session): label policy WIRED into the executor** (flag read before every pickup sequence, ALT pressed only when provably off), schedule extended through the label band ((16,-40),(0,-48)), click budget 8 = schedule length. **T67 (sparse self-staged melange) run 1 PASS: 5 staged, the wanted rune — the label-clicked small class — ARRIVED on attempt 2, 0/4 junk taken.** Run 2 launched for sample size. Remaining before Stage E (user gate!): another T67 confirmation or two, optionally the navigator label-band nudge; Stage E only on the user's explicit go. |
| R208 | decision | Stage D — the full threshold review, closed | With Stage C's logs in hand: warp triggers, drink cadence, belt-short policy, by-catch policy (chicken 35% already decided at R206). | **All four resolved by the user, 2026-08-03 (~05:20), every one keeping the shipped behavior:** warp triggers "sound sane, conservative is right" (4 hostiles/8 subtiles/hp<60%, burst 25%/2s — unchanged); drink cadence "sounds good" (heal <100%/10s, mana <25%/15s, rejuv <50% — unchanged); belt-short = NOTICE AND CONTINUE (the T56-run-1 starvation is accepted as a restock-discipline matter, not a halt); by-catch = ACCEPT AND CLEANSE (the town/field cleanse remains the answer; the accuracy campaign may shrink the problem further). config/necro.toml unchanged — 35.0 was already the file's number; the 50-override retirement in live-run.ps1 was the only mechanical change. **STAGE D COMPLETE.** Remaining: Stage E (3 clean unattended games at 35), closeout items 1-10. |
| R207 | execute | Autonomous pickup-accuracy campaign (T65+): the game is at the agent's disposal | User mandate 2026-08-03 ~03:40: refine click accuracy, reduce by-catch, and reduce move/attack misclicks on ground items — designed, calibrated and iterated ENTIRELY autonomously (OK gates waived; inventory items except the Cube usable as test material; the user stays tabbed in). Campaign: T65 hitbox mapping (probe junk in town, rotated offset grids, arrival-verified) -> data-driven per-class aim offsets + clutter-aware aim (collect passes neighbor positions; executor picks the offset inside the target's hitbox farthest from every neighbor's) + screen-space sprite avoidance in the navigator's move-click nudge (attacks already SHIFT and cannot scoop) -> T66 self-dropped-pile validation to T64's criteria. | _(campaign PAUSED mid-flight, 2026-08-03 ~04:45: T65 v1 found an empty floor; v2 self-dropped 6 junk but probed from OUTSIDE pickup reach — walk-picks misread as misses, bridge-killed at 20 min; v3 fixed (in-reach standoff, 1.2 s arrival pacing, 600 s self-budget, partial results) but NOT STARTED — the user left the game. The 6-junk pile is still on the town floor; T65 v3 is armed and relaunches on the next 'go'. Implementation half (clutter-aware aim, navigator sprite-nudge, T66 validation) awaits the map.)_ **RESUMED and CONCLUDED the same night (see R209): T65 v3–v5 mapped the hitboxes (potions/armor pick at sprite offsets; small classes — runes, gems, charms — are effectively LABEL-clicked), T66 found the label-display flag (BH.dll+0x14d2ca), the label policy + extended offset schedule were wired into the executor, and T67 (sparse melange) passed 2/2 with zero junk taken. The mandate's remaining idea — the navigator label-band nudge — was left as optional polish, moot for M5.** |
| R206 | decision | Stage D, item 1: chicken back to 35% | Asked at Stage C completion; the user decided unprompted. | **"Yes, set chicken to 35%. I think we're safe to do so now."** config/necro.toml already carries 35.0 — the 50 was a per-run override, now retired: tools/live-run.ps1 -Chicken defaults to 0 = use the config. Stage E runs at 35. Remaining Stage D items (warp triggers, drink cadence, belt-short notice-vs-halt, by-catch policy) still open for the full 🔶 decision. |
| R205 | execute | T64 (after Stage C concludes): drop a junk/whitelist mix, GO — pickit accuracy | The user's peace-of-mind test, designed while T56 run 3 plays: they drop a MIX of junk and whitelisted items 2-6 steps out; the bot decides each with the SHIPPED pickit (loaded via the wiring's own loader — 16 rules, no pending names), announces the plan, takes ONLY the wanted (T63 sprite-aim schedule, arrival-verified), and leaves the junk. Accuracy judged on BOTH sides: every wanted item arrived AND every junk item still lying at the end (an accidental pickup fails the run even at 100% wanted). The ledger names the rule behind every verdict, so intent-vs-config disagreements read as config conversations. Launches after T56 run 3 concludes — one drill at a time through the bridge. | **Ran 2026-08-03 (T64 runs 1–2). Run 2 delivered the verdict: 5/5 wanted ARRIVED on the sprite schedule (attempts 1–3), but 5 of 7 junk items were scooped as BY-CATCH — none matched a pickit rule, so the clicks themselves took them.** That result launched the R207 autonomous accuracy campaign (T65–T67) and fed the Stage D by-catch decision (R208: accept-and-cleanse). By T67 the same test shape passed clean: 0 junk taken. |
| R204 | execute | Restock healing potions before the Stage C rerun | The belt is short (healing 4 short at last census; the staged 606 in the inventory is the whole reserve) and there is no vendor UI by design (R48) — Stage C run 3 must start with a full complement or the T56-game-2 starvation replays. Buy/collect healing potions (any tiers — hp1-hp5 are ALL recognized since the offsets fix) until the belt's two healing columns are full and a few spares sit in the inventory reserve, then say go for T56 run 3. | **Done, and T56 run 3 rode on it: PASS — STAGE C COMPLETE, 2/2 clean [CVRL], no chicken.** 1063 s for two radius-150 games (~530 s each — linear with area vs the ~220 s radius-96 steady state). Game 1 opened belt-FULL all types (the restock + tier fix verified together), field pickups counted the belt up live via the new confirmation telemetry (healing 2/8 -> 8/8, hp4 AND hp5 tiers arriving), 19 confirmed pickups on the sprite-aim schedule with zero click-miss and zero belt-full alerts. The deposit loop observed end to end: game 1's material kept "whitelisted by the pickit", game 2's preamble deposited 2 + gold, and the cleanse judged and dropped 3 accidental field scoops (kinds 333/167/140) — the whole town hygiene chain unprompted. Next: Stage D (🔶 thresholds incl. the belt-short notice-vs-halt policy), Stage E. |
| R203 | execute | T59 run 3: drops 3-5 steps out, DIFFERENT directions — the occlusion check | Same sprite-aim protocol; what changes is the drop geometry, because run 2's misses clustered behind the character. One potion above the character, one below, one to a side, each 3-5 steps out; the ledger now records each potion's direction relative to the player. A directional split in the arrivals convicts self-occlusion and points the runtime fix at approach angles; uniform arrivals clear the offsets for field use immediately. | **PASS — 3/3, the pickup investigation is CLOSED.** East (rel (8,1)) and south (rel (-1,8)) drops arrived on the FIRST click at (0, -28); the northwest drop (rel (-6,-6), behind the character's shoulder) took one extra attempt, arriving at (-16, -40) — occlusion is real but the offset schedule absorbs it within budget. All three re-belted (census restored exactly). The night's full root-cause chain, live-proven: potion tier blindness (hp1-hp4 invisible) fixed in offsets; sticky belt-full misdiagnosis fixed with live-count evidence; and the aim — clicks always landed on the ground TILE while the clickable sprite draws ~28-40 px above it — fixed with the T63-measured per-attempt offset schedule. Follow-up noted for field data: a step-aside retry if occluded misses show up in run ledgers. Next: restock healing potions (R204), then T56 run 3 (Stage C). |
| R202 | execute | T59 run 2: drop 2-3 potions at the feet, GO — the sprite-aim rematch | Run 1 (hover era) went 1/3 and launched the investigation; the rematch runs on the T63 answer: each attempt clicks the next point of the measured sprite-offset schedule, up to 6 per potion, arrival watched after every click. PASS = every dropped potion arrives. This is the gate back to the Stage C rerun: with pickup proven at the feet, the remaining field steps are restock + T56 run 3. | **FAILED 1/3 — and the miss pattern named a new suspect: self-occlusion.** All three drops sat in a row AT the character's feet (5865-5867, same y); the one arrival was the WESTMOST potion ((-16,-40), attempt 2), the two behind survived 12 aimed clicks, and the census confirms only the one item left the ground. T63's clean pickups were all STEPS AWAY from the character. Theory: the character's own sprite occludes items close behind/under it, and the upward-aimed clicks land on the body. Run 3 armed as the discriminator: drops 3-5 steps out in DIFFERENT directions, ledger carries each potion's relative direction — south/east arriving while north fails = occlusion confirmed, and the runtime fix is approach-angle/reposition-on-miss, not offsets. **Superseded by run 3 (R203): PASS 3/3 — occlusion real but absorbed by the offset schedule within budget; investigation closed.** |
| R201 | execute | T63: two rounds — labels ON then OFF, the click matrix | The geometry question, cheapest first (kolbot analysis + the user's ALT insight). Round 1: drop a potion, GO; the bot presses Alt (PD2: a TOGGLE of the label display — verified live by a user OK before anything trusts it, Drill Kit rule 4), then clicks a paced 15-offset matrix around the projection (biased upward), stopping at first arrival. Round 2: Alt again (labels off, user-confirmed), fresh potion, same matrix — the bare-sprite measurement. No hover reads anywhere; every click re-projects (misses are move commands and the character wanders). Outcome shapes the fix: labels-ON offset found -> the pickup protocol becomes Alt-on, click at measured offset, verify arrival, Alt-off; nothing picked either round -> position clicks are dead and the bookmarked T62 (slot mechanism) is the road. | **PASS — the five-drill hunt ends: POSITION CLICKS PICK ITEMS; the failure was always AIM.** Labels ON: picked at (-16, -40), 12th click. Labels OFF: picked at **(0, -28), 2nd click** — the bare sprite is clickable, no labels or hover state needed. Both potions auto-belted; the Alt toggle verified in both directions (and left available — `VK_MENU` documented — but NOT wired into the runtime: sprite clicks suffice and the toggle-state tracking risk buys nothing yet). Implemented the same hour: `PickUpItem.attempt` + `_PICKUP_OFFSETS` sprite-aim schedule in the executor (one measured offset per retry — the retry-that-differs rule), `pickup_click_attempts` 6 = schedule length (walk budget stays 3), hover machinery removed from the runtime pickup path (`hovered_item_id` retained as a diagnostic), T59 rewritten as the sprite-aim proof. 864 tests, ruff clean. |
| R200 | execute | T62: drop ONE potion, GO, hand off — the glide lawnmower | Stage A: a CONTINUOUS glide (relative deltas, hand-dialect) mows the ±64 px area around the potion's projection row by row, polling the RAW slot every step (no item filter, no reliance on the projection being on the sprite); first flip -> click that instant -> arrival + belt census. If it arrives, mechanism + aim delta + end-to-end pickup are proven in one gesture. Stage B (only if no flip): the by-hand control done right — user hovers and HOLDS STILL, the bot detects the hand from the slot itself (no typing, no cursor freedom), then clicks 100 px away: potion up = clicks obey the SLOT; down = clicks obey position. | **BOOKMARKED — paused at the gate by the user before OK** (drill-cancel dropped; logged ABORTED). The user called a step-back-and-consider on the whole "cannot click items" problem; analysis delivered (kolbot uses engine/packet calls — Packet.click(item) / clickMap(unit) — never screen clicks, nothing mechanically transferable); T63 (position-only click matrix, geometry question) proposed to run BEFORE resuming T62 (slot question). |
| R199 | execute | T61: drop TWO potions apart, GO — steering strategies + the far-click question | Two questions, one drill. Q1: can anything synthetic steer the pointer — (a) absolute jump (control), (b) a GLIDE of feedback-corrected relative deltas (the physical mouse's own dialect, new `GatedInput.glide_screen`), (c) teleport + 2px relative jiggle? Two potions so the drop-latch sits on the OTHER one and any flip to the target is real steering. Q2: with the pointer holding the potion (by strategy or one by-hand hover), click 100 px AWAY on empty ground — if the potion comes up, clicks resolve against the POINTER and cursor accuracy was never the requirement. | **FAILED — but the failure was the drill's, not the game's (probe `probe-hover-slots` decoded it).** All three strategies read None AND the by-hand control read "not target" — yet the probe shows the slot alive, same game, same address (player unit still 0x24fce00), currently holding a TYPE-1 unit (the merc). Three self-inflicted blindfolds: (1) `hovered_item_id` filters to items, so a slot holding the merc printed as None — the slot is really "unit under cursor", general; (2) the drill told the user "leave the cursor wherever you like after" the control hover — but the slot REPLACES on real motion, so their hand moving off overwrote the measurement; (3) every strategy aimed at the raw projection point, which T60 measured as up to ~50 px off the sprite — the glide walked to the wrong spot and read once. Bonus finding: the two ProjectDiablo.dll statics hold type-2 OBJECT units (last-interacted objects, kinds 37) — not hover slots. T62 armed: continuous glide lawnmower over ±64 px polling the RAW slot, click at first flip; by-hand hold-still control + far-click only if the mow never flips. | 
| R199b | — | _(probe, no user action)_ | `probe-hover-slots`: read the four T58 candidate addresses cold. | player+0xE8 == abs 0x24fcee8 (same game as T58) -> type-1 unit id 1 kind 271 (the merc/an NPC — the slot tracks ALL units, not items only); ProjectDiablo.dll+0x3e8058 -> type-2 id 19 kind 37; +0x3ead94 -> type-2 id 10 kind 37 (last-interacted objects, not hover). |
| R198 | execute | T60 run 3: three rounds, transition-click protocol | Same drop-and-GO routine, three rounds. The bot now clears the pointer at neutral parks, sweeps until the pointer FIRST flips to the target, and clicks that instant — the executor's own pickup gesture, validated end to end. Reports the transition offset per round (the aim data that sizes the executor's probe ring) and the belt census per arrival. PASS = 3/3 transitions found and arrived into the belt. | **FAILED — and with the user's eyewitness account it re-framed the whole problem.** Rounds 1-2: the pointer read the TARGET at neutral parks 100+ px away (drop-latch; empty ground does not clear it quickly even under SendInput moves) so the rounds refused themselves. Round 3: parks cleared, 421 probes swept visibly across the potion (user: "swept over it several times", label highlighting) — the pointer NEVER flipped. Combined with the user's report that runs 1-2 looked the same from the chair, the run-2 "regions" are best explained as the user's own hand micro-movements, not our sweeps. Conclusion: labels highlight off the POLLED cursor position, but the hover pointer (and likely click resolution) listens only to motion the client accepts — absolute synthetic moves (both flavors) are ignored; the untested dialect is RELATIVE deltas. Corroborating pattern: every pickup that ever worked (T59's single arrival, T60 run 1's centroid click) happened while the pointer was hand-latched — suggesting clicks resolve against the POINTER, not the position. T61 armed to answer both questions. |
| R197 | execute | T60 run 2: same two rounds, now with REAL mouse-move events | Identical protocol to run 1; what changed is underneath: every hover (and the pre-click positioning) is now a SendInput absolute move the game can hear, so the hover pointer should track the sweep live. Expected: a bounded confirming region in BOTH rounds (not 0, not all-cells), a measured centroid delta, and both verification clicks arriving into the belt. | **FAILED, decisively informative: the pointer now TRACKS the synthetic sweep** (bounded regions both rounds — the SendInput move fix works), but the protocol was wrong: confirmations TRAIL the cursor (the pointer sets crisply on first crossing the sprite, clears lazily), so both "regions" were retention smears, the centroid meant nothing, and both centroid clicks found the hover already lost. Real signal in the transitions: round 1 first-confirm ~+50 px BELOW the projection, round 2 ~-24 px — the aim error is real and direction-dependent, and the honest gesture is TRANSITION-CLICK: click at the instant the pointer first flips to the target. T60 reworked to exactly that (neutral-clear, sweep to first transition, click there immediately — the executor's own protocol) with 3 rounds for the aim data. |
| R196 | execute | T60: two rounds — drop one potion a few steps away, GO, hands off | The measurement the user's diagnosis calls for: the bot sweeps the cursor in a grid around the projected point of the dropped potion, reading the hover pointer after every move — mapping exactly which screen offsets the game confirms as "on it". Reports the region's bounds and its centroid's delta from the projection (the miscalibration, measured), then ONE verification click at the centroid: arrival plus a belt census settles the auto-belt question in the same round. Two rounds at different spots separate a constant offset from a direction-dependent one. Sweeps are cursor moves only; one click per round. | **FAILED — and the failure identified the real mechanism.** Round 1: ZERO confirms in 421 probes swept directly across the potion. Round 2: ALL 225 cells "confirmed" — including corners 56 px out — and the verification click at the (meaningless) centroid arrived, **belt gaining the 611: auto-belt confirmed, no cursor-lift**. A pointer that neither updates (round 1) nor clears (round 2) is not tracking our sweeps at all: `SetCursorPos` TELEPORTS the cursor without a real mouse-move event, so the game's hover state never re-evaluates — T58 tracked beautifully because the user's real hand made the moves; round 2's pointer was latched from the user's own drop. Also explains T59's do-nothing "verified" click: the game resolves clicks against its hover model, and ours was stale. Fixed the same hour: `_send_mouse_move` (SendInput absolute move over the virtual desktop) now carries every hover AND the pre-click positioning in `click_screen`; the executor's pickup gained the retention guard (park off-item, require the pointer NOT to read the target, only a fresh transition is believed; never-clears → labelled blind click). 865 tests, ruff clean. T60 rerun armed. |
| R195 | execute | T59: drop 2-3 potions at the character's feet, GO, hands off | The live proof of the T58 wiring: the bot picks the dropped potions up with hover-verified clicks (cursor settles ON the potion before every click — watch for it). Census before/after; per-click ledger (verified vs blind, which probe); arrival checked per potion. PASS = every potion arrives, every click hover-verified, zero blind fallbacks. Clicks only, no walking — drop them within a step or two. | **FAILED 1/3 — and the user's eyes named the root cause.** Three rejuvs dropped in a tight pile: item 358 never confirmed (3 blind clicks, no pickup); item 360 "hover-verified at probe (0,0)" yet the click did nothing — a RETENTION false positive (the pointer held 360 from the neighbouring item's probes; 40 ms + a single read is too trusting); item 359 arrived on a blind click (T57's luck rate again). Census: the arrived rejuv appeared in neither belt nor inventory. The user then identified the pattern from watching: **the clicks were close but OFF — a systematic aim miscalibration for ground items** — which also explains T57's 1-in-30, whitelisted runes clicked-near-but-never-taken, and likely some dither loops (T55 run 2's seam item). Movement projection is calibrated (R6); the ITEM clickable region's offset from the projection has never been measured. T60 armed to measure it with the hover pointer as ground truth. |
| R194 | execute | T58: drop a potion, then hover on my marks — the hover-pointer hunt | T57 proved the pickup failure is the blind click (30 decisions, 1 arrival, misses with belt room). T58 hunts the client's hovered-unit pointer with T40's scan protocol: self-check (player unit findable), user drops a potion (drill records its unit address), round A hover-it (full scan for pointers), round B empty-ground (real pointer must change), round C hover-again (must return), round D hover-the-merc (items-only or all units?). Read-only; ends on its own after D. With the pointer found, PickUpItem becomes hover-verified: read what the cursor is on, adjust, click only on confirmation. | **PASS — one stable pointer, and it is player-unit-relative.** Round A found 20 holders of the potion's unit address; one survived the change-away/come-back rounds: heap:0x24fcee8 — which is the player unit (0x24fce00) **+ 0xE8**, so no pointer chain is needed, ever: the player unit is found per game already. Round D measured the caveat: the pointer RETAINS the last item while the cursor sits on a living unit (it cleared over empty ground in B), so consumers must read fresh after a deliberate move and validate the target as an item unit. Implemented the same hour: `offsets.PLAYER_HOVER_ITEM`, `units.hovered_item_id` (validated, fails to None), gated `hover_screen`/`project_world` primitives, and the hover-verified `PickUpItem` (probe ring biased upward — D2 draws items above their tile — click only on confirmation, ONE labelled blind-click fallback so a broken offset degrades to pre-T58 behavior instead of breaking pickup). 864 tests, ruff clean. T59 armed as the live proof. |
| R193 | execute | T57 run 1: stage 1 healing potion, OK the gate — the potion supply chain | The user's directive after T56: prove the bot identifies healing potions on the ground and belts them as needed, mid-combat included. Three T56 causes fixed first: **(1)** the kind table listed ONE healing tier (hp5/606) and two mana tiers — extended to the full runs (602-606, 607-611) from the game's own T42 code table; the town refill, pickit, hygiene and pickup all key off these sets, so the "no loadable stock" alongside two inventory hp4s, and the risk of hygiene drinking real healing as "foreign", fall together. **(2)** "would not come up after 3 clicks" is only diagnosed belt-full when the live belt count agrees; with room it is a click miss — the ITEM is written off, the type stays wanted. **(3)** a belt-full mark now expires the moment drinking makes room (it was per-game sticky — the starvation amplifier). Plus arrival telemetry: every confirmed pickup narrates "came up — belt healing n/8, ...". 862 tests, ruff clean. T57: one patrol game, radius 96, chicken 50; user stages exactly 1 healing potion in the inventory, belt healing low; PASS = clean cycle AND the staged potion belted in town AND ≥1 ground healing potion confirmed into the belt. | **FAILED — and the instrumentation found the real defect.** The game itself was clean ([CVRL], 578 ticks) but the potion ledger read: **30 pickup decisions, 1 arrival** (a mana potion). ~25 "would not come up even though the belt has room" alerts — so the T56 "belt full" story is dead; the failure is the CLICK-TO-ARRIVAL hop itself. Two hard facts from the trace: (1) the same spot can fail 3 clicks, be written off, and LATER come up — (5178, 5619) failed at one pass and succeeded at another — so the failure is positional/temporal (click not landing on the item's clickable sprite), not a belt rule; (2) ground unit ids RE-ROLL as rooms reload (same positions re-alerted with fresh budgets all game), which resets every per-item budget and explains T56's "picked twice at the same coords". Also: the baseline census read the INVENTORY EMPTY (no staged healing potion — setup question for the user) and the belt stuffed with 8 rejuvs, so the town-load criterion was unfulfillable from the start. Diagnosis fixes verified working live: the one rejuv refusal was correctly called belt-full (8/4), misses were correctly NOT type-blacklisted. Next: measure the click itself (hover-verified pickup — find the hovered-unit offset, calibrate, then click only when the cursor provably rests on the target). |
| R192 | execute | T56 run 1: OK the gate — Stage C, the supervised full run | The Stage C shape from 06-staged-acceptance-closeout.md: TWO patrol games on the T55 flow at radius 150 (T55 ran 96), full pickit, chicken held at 50% until the Stage D decision, everything else the R49 config. Two games rather than one because the deposit of game 1's loot happens in game 2's town preamble — a single game can never show the whole loop. PASS = two clean [CVRL] cycles; timings reported against T55's ~220 s radius-96 steady state, not asserted (the circle covers ~2.4x the ground). Watch hands-off; abort = 'abort' in chat, ESC/Enter in the field (kill switch, still untriggered live), drill-cancel, or take the mouse. Any death stops the milestone for a conversation. | **FAILED by its own bar: 1 of 2 clean** (launch 1 expired NOT STARTED at 21:21; launch 2 ran to conclusion). Game 1 **[CVRL] clean at radius 150** — preamble 5 s (5 items + 91 037 gold deposited from prior loot), clearance 268 s, pickup 63 s with the ring skipped on a clean memo, zero churn, dead-zone point written off cleanly, one bounded could-not-walk write-off (nudge-vs-interactive corner, the T55-run-3 shape). Game 2 **[CV-L] CHICKEN at 47%** (597/1267) mid-clearance — the monitor left the game correctly; no death. Proximate cause: the belt went out short (healing short 3→4, "no loadable stock — continuing") so rungs 3/5 had nothing to drink — game 2's six reflex fires were all armor/upkeep. The FAILED announcement was never delivered (the chicken exit had already left to the menus), so the run looked clean from the chair. New smell: potions reported picked (kind 611 twice at the same coords) then written off as "belt full … would not come up after 3 attempts" while the belt was SHORT — the diagnosis contradicts the belt state; investigate before or with the rerun. Deposit loop half-proven: machinery worked (game 1's preamble), but game 1 dropped only belt-routed potions, so game 2 had nothing to deposit. |

### M5 implementation — P6 stage B review fixes

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R191 | execute | T55 run 4: OK the gate — confirm the trend | A fourth timed patrol on unchanged code: run 3's 217 s should reproduce within normal variance (monster density moves the clearance). Confirms the 1032 → 458 → 217 s trajectory is the new steady state, not a lucky map. | **PASS: 236 s (23%)** — clean [CVRL], same signature as run 3: seam point dropped at birth, both dead-zone points two-strike written off in two ticks, sweep ring skipped on a clean memo (34 s incl. collections), 5 reflex fires, zero churn. **Steady state confirmed: ~220 ± 20 s per patrol, ~4.5x faster than the pre-R185 baseline.** T55 retired as the optimization gate; the timed-patrol drill remains armed for future regression checks. |
| R190 | execute | T55 run 3: OK the gate — the livelock fixes and the kill switch, live | The same timed patrol carrying R189 a-c and the Enter/ESC kill switch. Things to watch: the seam item written off within 4 walks if it reappears; no ping-pong at the border; and the new stop — pressing ESC or Enter mid-field should stand the bot down within a tick, announced in the narrative. Note: a kill-switch stop reports as "no clean cycle" in the drill verdict (FAILED row) — the CONCLUDED reason names the operator stop, which is the honest reading. | **PASS: 217 s vs the 1032 s baseline (21%)** — clean [CVRL], preamble 5 s, clearance 178 s, sweep 5 s with the ring SKIPPED on a clean memo. Every R189 fix visible: 1 seam ring point dropped at birth (c); the two east dead-zone points asked twice then written off in two ticks each (the two-strike rule confirming genuine no-routes); 6 reflex fires all run (was 159), zero churn. One monster written off after a nudge-vs-unit corner spent its plan cycles — cost the target, not the run. The kill switch rode along untriggered. Three-run trajectory: 1032 → 458 → 217 s. |
| R189 | decision | The border livelock: approve the three fixes; and the Enter/ESC kill switch design | T55 run 2's final loop, decoded from the transcript: a wanted item at (5334, 5707) on the Cold Plains/Blood Moor seam; the collect walk arrives 5 subtiles short of it every time (pickup reach 4, the goal flanked by two ground items), clicks nothing, and therefore counts nothing — `collect`'s only budget is per-click attempts, so a walk that lands short is free forever. Two patrol legs interleave (the item drifts in/out of the opportunistic-pickup radius as the character yo-yos at the seam, where crossing also flips which area's grid plans the walk), and the occasional subtile of "improvement" keeps resetting the patrol's no-progress budget. Proposed: **(a)** collect gets the same no-progress budget as every other mover (per item: closest-distance tracking, `pickup_attempts` walks that get no closer -> written off as stuck); **(b)** patrol/survey progress must beat the best distance by a MARGIN (>=2 subtiles) to reset the budget, killing closest-creep loops generally; **(c)** ring points outside the area's own bounds are dropped at planning time (the seam points stop existing as targets). Also: the Enter/ESC operator kill switch investigated and designed (field-scoped, correlated against the bot's own recent ESC sends); implementation awaiting the user's separate go. | **All approved ("a-c yes, killswitch yes") and implemented (`28c9c6e`; 856 tests, ruff clean).** (a) collect walks pay a no-progress budget (the seam item dies in 4 walks); (b) `patrol_progress_margin` 2 — subtile wobble no longer resets budgets; (c) ring points outside the area's bounds (seam inset 4) dropped at birth; kill switch live — out of town, the operator's ESC or Enter stops the run within a tick (`StopRequested`), opening chat IS the abort, recorded in drill-kit.md. T55 run 3 armed as R190. |
| R188 | execute | T55 run 2: OK the gate — more data after the armor/no-route fixes | Same timed patrol, now carrying the armor-outranks-the-wall fix and the two-answer no-route rule (`069f15c`). Expected: clearance a shade faster (no spurious ring write-offs), armor up promptly under fire, comparable or better total. | **ABORTED by the user (manual intervention): a border livelock.** ~214 legs cycling at the Cold Plains/Blood Moor seam in a three-beat dance — a collect walk toward a wanted item at (5334, 5707) that always ARRIVES 5 subtiles short (pickup reach is 4), then two westward patrol legs, repeat. Every watchdog stayed fed because every walk SUCCEEDED: `collect` only counts attempts per CLICK, and a walk that lands short clicks nothing — the "acted but achieved nothing" shape again, this time in the pickup path. Diagnosis + fix proposals raised as R189; the operator-key kill-switch investigation (Enter/ESC = stand down) delivered alongside. |
| R187 | execute | T55 run 1: OK the gate — the timed patrol | One patrol game, T53's flow, after the R185/R186 work. PASS = a clean cycle; the result reports total and per-step durations against T54 run 4's 1032 s baseline, whether the sweep skipped its ring on a clean memo, and any churn narration (expected: none). | **PASS: 458 s vs the 1032 s baseline (44%)** — preamble 12 s (was 80; heal skipped at 100/100/100, no repair trip), waypoint 22 s, clearance 354 s (was 854; 27 reflex fires vs 159, zero churn narration), sweep 63 s (walked its ring — sightings pending). The user watched and reported: waypoint dither at the start (22 s — now a per-run narrative metric, watched), the bone armor recast dangerously late, and mild-but-much-improved wandering. Both live findings fixed the same hour (`069f15c`): armor recast pending now starves the combat upkeep (absorb had slid 67→54→5% behind CastInFlight collisions), and no-route write-offs need TWO answers over fresh grids (an offline atlas probe proved five of the sweep's "no route" points path in <70 ms — a torn live grid read; the two east dead-zone points are genuinely routeless). `route_service` no longer caches None. 849 tests, ruff clean. |
| R186 | decision | Dilly-dally review: which of T1-T4 to implement? | The requested pass over the town chores and patrol code for repeated-reassessment waste, priced against run 4's own narrative timings. Found: the loops all carry budgets from earlier reviews — the remaining waste is TRIPS AND RE-WALKS, not loops. Proposed: **T1** heal threshold (skip Akara when player vitals ~full and merc above its own threshold — run 4 paid 26 s to top up a sliver; numbers to be user-set); **T2** repair threshold (`repair_below_pct` exists and ships at 100.0, so ANY scratch buys a 33 s Charsi trip — set to ~70); **T3** sweep-on-evidence (the pickup step re-walks the full ring the clearance just patrolled; instead, remember wanted items SEEN but uncollected during the clearance — the per-run memo idea — and re-walk only when that memo is non-empty, else sweep from standstill); **T4** narrative cleanse line summarized (the per-item KEEP list stays in the micro-log). Estimated together with the R185 fixes: run 4's 1032 s patrol ≈ 4-5 min. | **All four approved and implemented (`8925977`; 846 tests, ruff clean).** T1 skip thresholds 95/90% player + 60% merc; T2 `repair_below_pct` 100 -> 70 (supersedes R71, priced by run 4); T3 the sightings memo (`RunServices.wanted_seen`) — the user's per-run scratch-note idea, verbatim — with the sweep re-walking only on pending evidence; T4 narrative preamble line drops per-item KEEP detail. T55 (timed patrol) created to price it live; launched as R187. |
| R185 | decision | The 854 s clearance: approve the upkeep-churn fixes | The run-4 narrative pinpointed the "inching around on seemingly nothing": clear_radius took 854 s, of which ~10.5 min was a silent stretch of `reflex upkeep: combat-module upkeep` fires — the revive-wall maintenance churning in a quiet field. Mechanism: revives expire (300 s) between fights; upkeep desecrates for corpses; the desecrate budget (`desecrate_rounds` = 2) RESETS whenever corpses exist — and desecrate is what creates corpses, so the bound feeds itself; every cast also blocks the next tick's movement (CastInFlight), so patrol legs interleave 1:1 with casts. Proposed: (A) quiet-field gate — the upkeep delegation only runs with a live hostile in perception (the wall rebuilds at next contact); (B) honest budget — reset `desecrate_rounds` only when the revive COUNT grows, not when corpses appear; (C) narrate upkeep churn so a future silent gap explains itself. Recommendation: all three. | **All three approved and implemented (`28b6d91`)**, plus the belt-bottom-row contract from the same message: rung 7.6 generalized (foreign potions drunk clear, duplicate bottoms drunk to work a buried missing type down, rejuv bottoms never spent, mana glut capped) — the user's central rule "one healing/mana/rejuv at column bottoms when contents allow" is now the rung's docstringed contract. 844 tests, ruff clean. |
| R184 | execute | T54 run 4: OK the gates in-game after the run-3 fixes | Run 3 (stage 1 PASS by ear — the Shift chord is delivery-proven; stage 2 FAILED) exposed three shipped defects, all fixed and pinned by tests (`8f3ec8f`): the merc's hp reads on the client 0-128 scale (a full rogue = "8%", fed all game — probed live, `Monster.life_pct`); belt type checks now read the column's BOTTOM occupant (mixed columns and antidotes are normal states — the user's fourth point); the engine polls the stop channel every tick (`StopRequested`), so chat abort lands within a tick anywhere in a run. The user's standing test preferences are recorded in docs/drill-kit.md. Asked: window in and OK the two gates of T54 run 4 (feed-by-ear, then the mixed-belt patrol). | **T54 run 4: PASS, both stages** — thank-you on chord 1 (Shift+key 1), then a clean [CVRL] cycle onto the mixed belt (1032 s; no belt halt; narrative log written and legible). The R182 live gate is met: all four cycle features are live-validated. Three riders fixed the same hour (`ea843a7`): the field panel-recovery was ESC-ing the chat console while the user typed abort (a lone console is now the OPERATOR'S and is left open); rung 7.6 added (user rule: >1 column with mana at the BOTTOM -> drink the glut down, squatters first); and the 854 s clearance was diagnosed from the narrative log — ~10.5 min of combat-module upkeep churn, proposals raised as R185. |
| R183 | verify | Which chord actually feeds the merc? Test by hand | T54 run 2 FAILED with a clean diagnosis: three Alt+key-4 chords each consumed a potion from the belt (3->2->1->0) and the merc never said thank you — the game read every press as a plain belt key and the PLAYER drank it. The Shift-chord machinery is live-proven (stand-still attacks), so the suspect is the BINDING, not the sequencing: Alt+NUM may not be PD2's give-to-merc chord at all (Alt is also the show-items toggle). Asked: with the merc standing next to you and sound on, try by hand (1) hold Shift, press a healing belt key; (2) hold Alt, press a healing belt key; (3) check Options -> Configure Controls for a dedicated give-potion binding — and report which one earns the thank-you. | **Answered: the chord is SHIFT+(1-4), not Alt.** Fixed throughout (`press_key_with_shift`, skills/actions/executor/reflex/TOML/tests/ADR; 823 tests, ruff clean). Two riders from the same message: (1) test endings must be unmistakable — every stage now announces its own PASS/FAIL loudly in chat, recorded as a Drill Kit convention (docs/drill-kit.md); (2) "can you examine the game's hotkeys yourself?" — yes, plausibly (the .key save files or the client's key-config table in memory); logged as an open follow-up in drill-kit.md and offered as a spin-off task. |
| R182 | decision | Potions + narrative-log cycle: end-of-phase stop — approve commit and say go for live | The R179/R181 plan is fully implemented and archived (`docs/archive/plans/2026-08-02-potions-and-narrative-log/`): P1 type-based belt (drink from any column, minimums by type, halt only on mechanical failure — the R178 shape is a regression test), P2 merc first aid (Alt+NUM below 50%, rung 7.5, paced), P3 narrative log (`logs/run-<stamp>.log`, coarse, waits explain themselves), P5 route-aware legs (`route_to` over the atlas; no-route targets written off instantly), P4 docs/teach/cleanup. 823 tests, ruff clean; worktree left UNCOMMITTED per the no-commit-unless-asked rule. Asked: (a) approve the commit (one conventional commit incl. the archive move), and (b) say go for the live gate — one run of anything (narrative log), a deliberately mixed belt (P1), a merc chip-damage moment (P2), the next patrol/survey (P5); the Drill Kit OK-gate protocol applies as always. | **(a) committed `26857c1` and pushed** (SHA backfilled into the archived plan). **(b) The user proposed a formal test** ("I'll mix the belt and injure the merc") — designed as **T54** (`drills/t54_potions_acceptance.py`, two stages: merc-aid ladder watch in the field, then the T53 patrol game onto a deliberately mixed belt, with the narrative log checked at the end); launched the same night, user at the machine. |
| R181 | decision | Add route-aware legs to the approved plan? | The user's live observation during T53 run 2 ("dawdled at dead edges, wandered into another zone — is it using our map?") checked out as an architecture gap, not a map-access failure: the atlas IS loaded (zero unsurveyed alerts, real multi-cell plans, 464 s vs 15-min timeouts) but step-level movement is straight-line hops — only each hop gets pathfinding, so far-corner targets burn no-progress budgets at fences and hops can cross zone exits. Also corrected the R174-era claim: instant write-off only happens when a hop FAILS; clamped hops still burn the slow budget. Proposed: route-aware legs — steps walk the A*-planned path's waypoints as their legs, and no-path targets are written off instantly. | **A — approved (2026-08-02): added as P5 of the potions-and-narrative-log plan.** |
| R180 | execute | Restock healing potions again (Tower walk drank them), then go for T53 run 2 | T53 run 1 halted in the preamble exactly as R178 did: "healing short 2 — manual restock" (the drill-log row says PASS — a harness blind spot, fixed in `0d08d13`: the body now raises on zero clean cycles). Second occurrence of the class in one night; the P1 type-based-belt phase of the R179 plan is what retires it (no-stock shortitude becomes log-and-continue). For tonight: put 2+ healing potions where the refill can reach them and say go. | **Done; T53 run 2 launched and PASSED** (see R177). |
| R179 | decision | Three pre-run-3 proposals: ALT policy, the potion overhaul, the narrative bot log | The user raised three areas before R177 run 3: (1) ALT item-name visibility — detection, ensuring it is on (1a), toggling it off to shrink the accidental-pickup click surface (1b); (2) a potion-logic overhaul — belt always full where possible, type minimums without column rigidity, no crashes on empty, pickup-to-fill + 2 spares, merc potions via Alt+NUM below 50% merc hp, explicitly NO vendor purchasing; (3) a human-readable narrative log with timestamps that explains apparent idleness. Assessment given (1a is a non-issue: perception is memory-read, ALT only affects rendering; 1b deferred pending run-3 audit evidence; 2 and 3 proposed as the next development cycle in one plan, run 3 first). | **A — all recommendations approved** (2026-08-02): 1a documented as a non-issue, 1b deferred pending run-3 audit evidence, potions + narrative log planned as one cycle (`docs/plans/2026-08-02-potions-and-narrative-log/`), run 3 to be prepped as a formal Drill Kit test (T53) at the user's request. |
| R178 | execute | Restock healing potions (belt refill 2 short), then say go to relaunch | R177 run 1 (survey-town) halted safely in the town preamble: "belt below minimum after refill: healing short 2 — manual restock". The character/stash is out of healing potions (the R173 fire loop drank them; its belt-full latches left replacements on the ground) and the bot has no vendor-purchase capability by design. Buy or move at least 2 healing potions where the refill can reach them (stash or inventory), then say go. | **User diagnosis supersedes mine:** the shortage was not an empty stash — the bot had placed mana potions in a healing column, so the refill counted the column as occupied-but-wrong and came up short. User refills by hand this time. **Follow-up recorded (user request): develop the potion-belt logic to handle accidental potion-type mixing, potions in the wrong slot, and similar — clarify behavior so a mixed belt cannot halt a run.** Logged in the survey plan's future-work list; awaiting the user's go to relaunch. |
| R177 | decision | Say go for the live acceptance runs (survey-town, survey-cold-plains, patrol re-run) | P4's review gate. P1-P4 implemented and committed (`a00a650`, `e1e5c1f`; 775 tests, ruff clean). Proposed sequence, each launched by the agent through the bridge with the user at the machine: (1) `survey-town` — safest possible first live exercise of the step; (2) `survey-cold-plains` — the real product; (3) a fresh `cold-plains-patrol` run to confirm the cleanse-hygiene fix and exercise the reposition rung live. | **In progress.** Run 1 attempt 1: LEFT EARLY (belt: mana potions in a healing column — R178). Run 1 attempt 2: **PASS, [CVRL]** — 7 legs, "29 room(s) recorded, 0 frontier point(s) open"; the Rogue Encampment is fully atlased. Run 2 attempt 1: **killed by the bridge's 15-min timeout after a livelock** — the combat gate had no closing-progress accounting (a monster every dash approached and never reached kept the tick for 14 min; 0 frontiers credited), compounded by nearest-first target flapping. Both fixed (`5b4b405`: survey_fight_patience=20 stale ticks with distance-or-hp progress; sticky frontier target; max legs 500) + 3 regression tests. Run 2 then completed ANOTHER way, at the user's suggestion: **T52, the manual-survey drill (PASS)** — the user walked Cold Plains with the recorder on, 57→114 rooms to 0 frontier open in ~5 minutes. Cold Plains and the Rogue Encampment are both fully atlased. **Bonus (user proposal): T52 run 2 (PASS) mapped the whole Countess route** — Black Marsh 36 rooms (~25 frontier still open on departure), Forgotten Tower 3, Cellars 1-5 (19/20/14/14/17 rooms), Cellar 5 confirmed 0 frontier — permanent capital for the M6 Countess runs. The Black Marsh partial coverage is BY DESIGN (user): a route survey of the corridor a Countess run actually uses, not an area survey — complete for its purpose. **Sanity check PASS (2026-08-02, ~03:3x): the automated survey against finished Cold Plains ran [CVRL] clean in 7 ticks** — preamble, waypoint, "survey complete: 114 room(s) recorded, 0 frontier point(s) open", done. The automated path is live-validated for future areas without costing a walk. **Run 3 = T53 run 2: PASS, [CVRL] clean, 464 s** (run 1 halted on the R180 potion shortage; drill-status honesty fixed in `0d08d13`). All steps to plan: preamble, waypoint, patrol clearance ("clear for 5s, 4 monster(s) written off as unreachable" — with the full atlas, cheaply), sweep with all 8 ring points walked, leave. **Click audit: 2 of 339 clicks landed within the avoid radius of a non-goal item, vs the R167 baseline of 31/39 accidental exemptions with 5 clicks ON items — the fix is confirmed at scale.** No chicken, no cleanse needed (the loop's precondition never formed), reposition never triggered (nothing pinned the bot — the net is in place, sim-tested, live-unexercised). R177 CLOSED; the plan's live gate is met. |
| R176 | decision | Survey + fixes plan: phase table and Q1-Q4 confirmations | The yona-plan pass for R175's approved work (plan dir: `docs/plans/2026-08-02-survey-and-post-run-fixes/`). Clarified on user challenge: manual `--survey`, passive recording, and the seed-keyed map store all exist from M3 — the new work is only the self-driving coverage walk (frontier targeting + a `survey` step + run files + coverage report). Phase table P1-P4 presented, plus confirmations: Q1 engage radius ~30 while surveying; Q2 no auto-survey mid-run, loud report instead; Q3 reposition-reflex thresholds (≥2% max HP lost in 2.5 s while moving <3 subtiles → step 10 away, 2 s cooldown); Q4 surveys run as normal engine runs (chicken/death-latch apply). | **All four approved as suggested** (2026-08-02). Q2 refinement from the user: dedicated survey runs are fine, and may even be run manually with the user controlling (the M3 `--survey` mode already covers that). Implementation proceeds P1→P4. |
| R175 | decision | Post-confirmation-run fixes and the survey question (Q1-Q3) | The user's live observations of the R173 run: (Q1) the bot visibly runs into walls and stalls — survey Cold Plains now to fill the atlas, or defer mapping? (Q2) approve the fix package for the cleanse loop: field cleanse steps away from wanted items before dropping, moves off after dropping, and `maybe_cleanse` stops resetting `inventory_full` when the freed space was created at the bot's own feet (a retry that cannot differ is not a retry); plus a damage-while-stationary reposition reflex (the character stood in fire to 49%). (Q3) item-read investigation (user: can we read ground items to avoid re-pickup?) — answered: we already read them from memory; the residual misclicks are deliberate pickup CLICKS landing on overlapping neighbours, which reading cannot prevent, only drop-hygiene can. | **Resolved as R176**: Q1 answered "survey now" (the survey-and-post-run-fixes plan), Q2's fix package and Q3's reposition reflex both approved and implemented in that plan; the whole package live-validated at R177. *(Marked closed at the M5 closeout reconciliation, 2026-08-03.)* |
| R174 | decision | Eastern dead zone: sanction cluster-level write-off inference? | Re-raise of R168(c), unblocked now that the R169 overhaul is committed (`db12114`). The first instrumented patrol run wrote off 12 monsters one at a time in a static cluster at x≈5348-5358 (55-65 subtiles east) — real terrain the patrol also skips. Proposed: when N monsters in a tight cluster are individually written off, infer the cluster (write off the region once) instead of paying the full no-progress budget per monster. Alternative: leave it per-monster — it works, it is just slow, and the expiry-on-movement rule already handles stragglers that wander out. | **B — per-monster, and the question is now largely moot (user agreed 2026-08-02).** The T52 surveys removed the ignorance the cluster heuristic would have compensated for: with Cold Plains fully atlased, a walk toward a genuinely unreachable monster fails on the FIRST attempt (A* finds no path) instead of after three futile approach legs, so the per-monster write-off is already cheap. No cluster inference will be built. |
| R173 | decision | Say go for the GOAL_EXEMPT_RADIUS confirmation run | Re-raise of R168(b). The click-audit fix (`GOAL_EXEMPT_RADIUS = 1`, commit `db12114`) predicts ~zero accidental goal exemptions where the last run measured 31 of 39. One confirmation run of `runs/cold-plains-patrol.toml` via `tools/live-run.ps1` through the bridge settles it. The bot sends input, so: be at the machine, hands near the controls, and say go (in chat or here); the run will be announced in-game per the Drill Kit first. | **Click fix CONFIRMED; run itself NOT clean ([CV-L] CHICKEN at 49%).** 491 audited clicks; 14 landed <4 subtiles of a ground item, of which 11 were deliberate collects at the item itself, 2 were the by-design 1-subtile jitter tolerance, 1 was nudge-budget exhaustion (8 nudges, settled 1 from an item). Zero wholesale accidental exemptions vs 31/39 last run. New failure mode found (user diagnosed the mechanism live): full inventory + wanted rune → field cleanse drops junk at the feet/pointer → the next pickup click scoops it straight back → `maybe_cleanse` resets `inventory_full` and re-arms the same doomed retry → infinite loop, character pinned in fire damage until chicken. Also: one monster written off (id 78 at (5352,5724), 59 subtiles — the eastern dead zone again), four ring points given up east/south-east, consistent with the same terrain. Follow-ups spun out as R175. |
| R172 | decision | The two baseline-protected inventory items: hand-clear or policy change? | Re-raise of R168(a). The cleanse report shows two items surviving every cleanse solely as "protected: predates this bot session" — the session baseline protects everything present at the first cleanse, forever. Either (A) you clear those two by hand once and the baseline stays as designed (safest: it exists so the bot never drops something you were carrying), or (B) the policy changes — e.g. re-baseline per game rather than per session, which lets the cleanse eventually judge pre-session items but weakens the "never drop the user's own stuff" guarantee. Recommendation: A. | **A — policy confirmed as designed.** Pre-session items are protected (indefinitely, if never hand-cleared — "those items get respected, and that's that"); within a session the cleanse is ruthless; the Horadric Cube is always protected (now recorded as policy in `offsets.UNMOVABLE_KINDS` and the cleanse docstring). Hand-clearing the two current survivors is optional and left to the user. |
| R170 | decision | Q3 clarified: does the done-alert fire on every terminal turn end? | "Turn end" = every time the agent finishes a terminal response and waits on the user — NOT test end (tests already announce their own CONCLUDED line in chat). The user's answer to Q3 was "if it's test end, yes; otherwise we'll see", so the general-turn alert is the undecided piece. Offered: ship it on (kill with "partyline off"), throttle to request-ending turns only, or hold it back. | **A — shipped on.** Every terminal turn end queues one `[claude] done - your turn` through the bridge; delivered only in-game + foreground + no panel, dropped when stale (>30 s) or muted. Kill switch: "partyline off". The whole overhaul landed the same turn: toolkit format pushed (`40c4505`, incl. the Windows CRLF @include fix — installed skills had carried NO protocol text on this machine), Partyline module + toggle, Stop hook (needs a session reload to arm), Drill Kit (OK gate, chat abort, scope column, drill-kit.md), request-index.md backfilled R1-R170, project-state.md, bridge single-instance mutex + `pd2bot-bridge` logon task, live-run.ps1. 752 tests, ruff clean. |
| R169 | decision | Workflow overhaul: four confirmation questions | Assessment of the user's four workflow changes (Partyline promotion + on-by-default, done-thinking chat alert, request-format change with M/P + rolling index, test formalization with chat-gated start/abort), plus the issues spotted during review: the toolkit-vs-repo instruction-source clash, bridge auto-start, the background-cwd hazard, and the off-by-default memory now contradicted. | **Q1: globally** — the M#/P# format lands in the agent-toolkit repo as an optional prefix, via the standard pull → edit → install.sh → commit/push. **Q2: yes** — the elevated bridge auto-start task is sanctioned. **Q3: split out as R170** (needed clarification). **Q4: no vetoes** — Partyline, Drill Kit, request-index.md, project-state.md all stand. |
| R168 | decision | Three follow-ups from the first instrumented patrol run | (1) clear the two session-protected items by hand vs re-baseline per game; (2) re-run to confirm the GOAL_EXEMPT_RADIUS fix; (3) whether to infer cluster-unreachability in the eastern dead zone. | _(deferred pending the R169 overhaul; re-raised 2026-08-01 20:17 as R172 / R173 / R174, one per follow-up)_ |
| R167 | verify | Supervised live run of the instrumented patrol + sweep | First live exercise of the sweep patrol, the click audit, the cleanse reporting, and the monster write-off — all fake-validated only. | **`[CVRL]`, clean and complete.** The sweep patrolled its ring and collected a mana potion it could not have seen from the clearance's endpoint. The click audit answered item 2: of 160 clicks, 30 landed inside the avoid radius and 27 of those were GOAL-EXEMPT — 31 of 39 exemptions were accidental (goal merely NEAR an item, 1-3 subtiles), 5 clicks landed exactly ON items. `AVOID_RADIUS` was never the problem; the exemption tested against the wrong constant. Fixed as `GOAL_EXEMPT_RADIUS = 1`, plus the honest-arrival rule (a walk blocked only by a hazard beside its goal stops short and reports, rather than burning five identical re-plans). The cleanse named its keeps: two items survive ONLY as "protected: predates this bot session" — the accumulation mechanism, now visible. Twelve monsters written off at 55-65 subtiles in a static eastern cluster (x≈5348-5358) — real terrain, same dead zone the patrol skips. 744 tests, ruff clean. |
| R166 | decision | Re-run T51 for a clean artifact, or move on? | Run 1 was aborted on a design fault but its telemetry survived and answered the question with seven measurements. A re-run would buy a PASS row instead of a salvaged ABORTED one, at ~3 minutes of walking. | **"Move on."** T51 was rebuilt anyway with the endpoint the user specified (16 drops all out of view, or 3 minutes, or everything out of view after >=3 drops), each drop counted back in chat, and an explicit `T51 DONE` message — the run-1 fault was that the drill knew when it was finished and the person walking did not. Left armed for whenever it is wanted. |
| R165 | execute | Run T51 — the walk-away visibility test (the user's own idea) | The user proposed dropping a series of potions at intervals and counting how many the bot perceives. Adopted with one refinement that turns it from a bracket into a decision: counting cannot distinguish **our filter** (`PERCEPTION_RADIUS` 80) from **the client's own horizon**, because both look identical from outside — the item is simply not in the snapshot. So every sample scans TWICE, at 80 and at an effectively unlimited radius, and watches which loses the item first. A dropped item is the right probe because it does not move, so its position recorded at drop time stays ground truth exactly when it becomes unreadable. | **Answered, from a run that was aborted.** The user dropped 12 potions along a walk; seven were watched vanishing at **d = 46, 46, 54, 55, 61, 63, 67** — every one far inside `PERCEPTION_RADIUS` 80, and five of the seven lost at the SAME distance whether the radius filter was applied or lifted entirely. Verdict B, measured rather than inferred: the client's loaded-room horizon is the bound. The 46-67 spread rather than a crisp circle is what room-quantised loading predicts. The bridge was closed mid-run to abort it so `.out.txt` was never written, but the bridge's `.stdout.tmp` survived with the whole trace. **Consequence nobody had priced: stage B's radius 50 was already marginal**, and the pickup sweep at 96 was seeing about half its circle. |
| R164 | execute | Get the character into Cold Plains for T50's field half | The user doubted the standing claim that perception is 80 subtiles / "over three screens" and asked for a calibration test. T50 is written and read-only (no clicks, no keys). Run once in town already: **Part A settles the screen question and my earlier claim was wrong** — a screen is 38-76 subtiles ACROSS depending on direction (19-38 from the player to the edge), not R147's "~24 subtiles", so every sizing figure derived from it understated the screen by 2-3x. Part B in town is inconclusive for the question that matters (the whole town population sat within 57 subtiles, so nothing tested the horizon). It needs a field with monsters spread out. | **PASS, and the user's skepticism was justified — in the opposite direction.** The user added the condition that made the run trustworthy (*don't conclude until I've stood still 5s with at least 1 monster on screen*), so the drill now arms on exactly that, testing "on screen" against the real projection rather than a distance. Armed in Cold Plains with **12 monsters on screen and 44 live in perception**: radius 80, 120, 160, 240, 320 and unlimited all returned **the identical 65 units, furthest at 64, nothing between 64 and infinity**. So `PERCEPTION_RADIUS` is a ceiling that never binds — the CLIENT's loaded-room horizon (~60, matching a 3x3 neighbourhood of 8x8-tile rooms) is the real limit, and raising the constant would buy nothing. Scan cost is set by unit count (~7 ms/30 units, ~19 ms/65), never by radius, which also retires R23's cost argument. |
| R163 | decision | Combat aggression numbers, after watching the patrol run | The bot fought but stood back a lot; user: *ducking in for a click and running away, and moving frequently, are good strategies*. Offered `restrike_s` 2.0→1.0, `wait_for_revives_s` 1.5→0.8, and releasing the revive-wall gate at 1 instead of 3. | **All three taken.** The third needed a new number rather than a smaller one: the gate was reusing `revive_target` (how many revives upkeep MAINTAINS) to answer a different question (how many before offense may advance), so waiting for the full wall meant two desecrates and three revive casts before the first dash. Split out as `approach_with_revives = 1`; upkeep still maintains three, and now builds the rest while the fight is on. Same message also produced the navigation unstick (below). |
| R162 | verify | Re-run the patrol with misclick recovery | After the user identified the Warriv misclick and asked for a general rule: close unexpected dialogs, move, click elsewhere. | **It fought, and then died on a wall.** Four kills, a full revive wall, the circle partly walked, a healing potion under pressure — and `NavigationError: gave up after 5 plan cycles without progress; last position (5226, 5658), target (5219, 5658)`. Seven subtiles. Combat and loot sends now absorb an unreachable target the way patrol legs already did (it costs the target, not the game), and the user's sixth observation — *a bit of dithering or wiggling in the pathfinding might solve this* — became `Navigator._shake_loose`: step aside before re-planning, since a re-plan from the same position returns the same path. **Third instance in one day of the same principle**: a retry that cannot differ from the attempt it retries is not a retry (object clicks, patrol points, pathing). |
| R161 | verify | Re-run the patrol, instrumented | The per-attempt trail added to `open_object_panel` after two identical failures the message could not explain. | **The trail worked and the user's eyes finished the job.** It showed the standoff was fine (d=7 in a 4-12 band), no panels open, the click sent with a sane projection, and the character not moving — three attempts byte-identical. The user then named the cause: a misclick on Warriv, whose dialog ate the click and was closed again at the top of each retry, so the trail truthfully read "panels none". Fixes: `_clear_stray_ui` (ESC anything open that is not the target — panels AND smaller dialogs, named or not, automap exempt), plus retries that differ (`turn` rotates where the character stands, `aim` moves where it points). Two test doubles were found lying: the fake's ESC only closed BLOCKING panels, and the fake only opened the stash on an exact tile match. |
| R160 | verify | Run T49, the waypoint click probe | Written after `_walk_near`'s verification fix failed to change anything — my second wrong theory on that failure. Measures the approach, the guard, the click and the result as four separate stages. | **PASS, first attempt**: approach 24→11, standoff OK, guard passes, click sent, panel OPENED (user: "waypoint panel flashed open"). Decisive as a NEGATIVE result — the click path is sound in isolation, so the failure only happens in context, which is what sent the instrumentation into the real path (R161). |
| R159 | verify | Re-run the patrol after the `_walk_near` fix | Verify the step-back rather than assume it. | **Failed byte-for-byte identically** — same position, same distance. The fix was correct in itself (a walk is a request, the position is the proof) but was not the cause. Recorded as a wrong theory, the second on this failure. |
| R158 | verify | Supervised live run of the patrol clearance | The patrol is implemented and validated against fakes only (713 tests, ruff clean) — the same never-run band the stage-B review was called over. One supervised run of `runs/cold-plains-patrol.toml` at radius 96 answers what no test can: **does it fight?** Both 2026-08-01 runs completed without a single `AttackUnit`. Also worth reading off the trace: patrol points reached vs skipped as unreachable (frequent skips mean the ring is landing on unread ground and the geometry needs revisiting, not the radius), and any `IdleBail` with its new context block. | **Resolved through the R159–R163 arc** (the run was supervised repeatedly that session): the first attempts exposed the Warriv-misclick and the seven-subtile wall failure, and by R162 the answer to "does it fight?" was **yes** — four kills, a full revive wall, a potion under pressure. The aggression numbers that watching produced became R163. *(Marked closed at the M5 closeout reconciliation, 2026-08-03.)* |
| R157 | decision | Is the patrol a new step, or does `clear_radius` grow it? | A separate `patrol` step is composable; folding it into `clear_radius` keeps one owner of "is this area clear". Recommended the latter, because a separate step recreates review 001's exact shape — two components measuring the same question from different points — and because patrolling and fighting interleave rather than sequence. | **A — `clear_radius` grows it.** User added the sizing constraint that matters more than the shape: *we don't need this to become enormously onerous; ideally, a simple detection of how far the character has moved, and if there are any living enemies nearby, would cover most of the requirements of the final product.* Design accordingly: a fixed ring of sample points and a visited set, no frontier exploration or coverage map. |
| R156 | decision | Patrol plan: six confirmation questions (Q1-Q6) | Radius for the new stage (96), new run file vs editing stage B, whether to fix `cold-plains.toml`'s unsound radius 150, leg length (12), skip-vs-fail on unreachable ground, and sim coverage. | *Not answered explicitly; proceeding on the suggested answers*, all of which are reversible config, and flagged Q1 (radius 96) and Q3 (changing `cold-plains.toml`) as the two worth a second look before implementing. |
| R115 | decision | Should `IdleBail` share the cycle's chicken counter? | Carried since P4. `IdleBail` is a `ChickenExit` so the cycle's handler leaves the game correctly — but its vitals backstop counted idle bails too, so a mixed sequence (one real chicken, then one idle bail) halted with a message telling the operator to heal a character whose problem was a hang. Found while answering: `TownStepFailed` and `RunFailed` are in the same position, so **three of the four** were feeding the vitals counter. | **Differentiate them.** `ChickenExit.is_vitals` (True) overridden to False on all three; the cycle counts and labels only real vitals chickens, and the report says `LEFT EARLY` rather than `CHICKEN` for the others. User added two riders: **(1) log everything before idle-bailing** — the engine now attaches step, tick, position, vitals, hostiles near vs in perception, UI panels, refusals, the last five actions sent and the last log line; **(2) the enemies-nearby rider** — *technically, idle bailing is only necessary if there are enemies nearby; however, there is something to be said for always idle bailing regardless, as we don't want the bot to get stuck.* Implemented as a longer deadline rather than an exemption: `idle_bail_s` 10 s with a hostile within 40, `idle_bail_quiet_s` 30 s otherwise, and the message says which fired. |
| R155 | verify | Re-run stage B with the waypoint fixes | Second supervised run, after four changes stacked without a live run (the object clearance, the step-off, the field panel recovery, the lateral drift). | **`[CVRL]` — a clean full run**, and the fix confirmed at exactly the point that failed: "arrived at (5268, 5713), stepped off to (5258, 5723)", first desecrate then at (5262, 5725), 12 subtiles clear of the waypoint instead of 4. Six `CastInFlight` deferrals including one on a reflex cast; nine casts across two runs with no `SkillSwitchFailed`. User: "looked great to me." **Caveat recorded rather than glossed: it never struck anything** — no `AttackUnit` in the trace, so nothing entered engage range and 001's path is STILL unexercised. Two consecutive arrivals with no fight at radius 50 is also the second data point for the deferred patrol question. |
| R154 | inform | "We got waypoint locked again" — and the recovery to build in | User, on attempt 10: when a mis-click opens an unexpected dialog the fix is **ESC, move the mouse away, click to move off** — and it should be the DEFAULT after travelling, since the waypoint is right under the pointer on arrival. Plus, again: standing still in one place for a long period is dangerous and undesirable. | **All three implemented, and the user's second point improved the 001 fix.** The root cause is theirs from the other end: `_open_ground` avoided monsters, allies and corpses but not OBJECTS, so the first desecrate went 4 subtiles from the waypoint just arrived on. Casts never went through the navigator, which has filtered `INTERACTIVE_OBJECT_KINDS` since R111, so they never got that filter — they do now, at `object_clearance` 6. The step-off is conditioned on a clickable object actually being within stepping distance, which is their reasoning stated precisely (the hazard is the thing under the cursor, not the travelling) and leaves harmless arrivals untouched. The field can now close a stray panel via the town layer's own retried ESC. On stillness: the 001 drift bound had used standing still as its fallback; it now rides `retreat_point`'s rotation ladder instead, so a step that would leave the fight becomes a LATERAL one — still moving, still engaged. Standing is left only for when every direction that keeps the fight is unwalkable. |
| R153 | verify | Supervised stage-B run, with the three review fixes in it | All of P6's open P1/P2 findings are fixed and NONE of the fixes has executed against the game — the exact band the review was called over. One supervised `--games 1 --chicken 50 --run runs/cold-plains-stage-b.toml` exercises all three at once: the clearance closing on a hostile it could not engage (001), casts no longer blocking the tick (002), and the town preamble's polling (003, whose "dithering" symptom is the user's own report and can only be judged by watching it). If `SkillSwitchFailed` recurs, it now arrives with the state that would explain it attached. | **Ran; `[CV-L] CHICKEN` on a waypoint lock-out, and the fixes it did reach all worked.** The preamble and the waypoint trip completed, bone armor was cast as the first action, and desecrate -> revive x3 built the wall. Then a desecrate placed 4 subtiles from the waypoint just arrived on opened its menu, which blocks input, and 10 s of refusals became a `NavigationError` -> chicken. **002's fix is live-verified**: three `CastInFlight` deferrals in the trace, each a MoveTo correctly waiting out an animation instead of spending it, and the armor rung did not re-fire — no `SkillSwitchFailed` in five verified casts either. 001's fix was never reached; 003's preamble ran but was not timed. Root cause and the user's recovery both under R154. |
| R152 | execute | Run T47, the hotkey audit (the offer R146 declined) | Presses F1-F6 and reads back which skill each selects, to separate stale bindings from a keypress that never lands. | **PASS — all six agree with the config**, and that is the useful half: it kills BOTH standing theories at once. F1 68 bone_armor, F2 367 blood_warp, F3 220 tp_tome, F4 78 bone_wall, F5 83 desecrate, F6 95 revive. Do not edit `necro.toml`. What the three failures have in common is what T47 does not do — every one was a switch attempted immediately after a cast — which is what T48 went on to test. Also confirmed R149's reboot worked: the client launches, character alive in town, can-act yes. |
| R151 | verify | Watch T48 — cast animation length, and when input is accepted again | T47 came back **all six OK**, which kills both standing `SkillSwitchFailed` theories: the bindings are right and the presses land. What the three failures share is what T47 does not do — every one was a switch attempted **immediately after a cast** (95←83, 68←83, 68←95). T48 tests that directly and measures finding 002's guessed 0.4 s settle at the same time: it casts bone armor on itself in town, samples the player's unit MODE to time the animation, then casts again and presses ONE hotkey at delays of 0.0–0.8 s to find when the client starts accepting input. Nine casts, no monsters, no walking, no panels. User at the machine, hands near the controls. | **PASS, and it refuted the theory it was written to test.** The animation is **610-640 ms** (mode 5 -> 10 at ~110-125 ms -> 5 again), so the guessed 0.4 s settle never covered it. But every hotkey press LANDED, at every delay including 0.0 — one sent 110 ms into the animation registered within 62 ms. So the client does not eat following input, `SkillSwitchFailed` is still unexplained, and my hypothesis was wrong. Both halves were worth having: 002 is now fixed against a measured number instead of a guess, and — because presses are unaffected while clicks are not — the ladder's potion rungs are explicitly exempt from the wait, which the old blocking sleep never allowed. |
| R150 | execute | Start the elevated bridge for this session | Review finding 001 (the permanent hang) is fixed against fakes and needs no game. What comes next does: T47's hotkey audit settles the unexplained `SkillSwitchFailed` in ~30 s, and finding 002 wants a MEASURED bone-armor cast rather than the guessed 0.4 s settle. Both send input, so the user must be at the machine with hands near the controls (mouse or ESC). The bridge also lets the agent check the R149 reboot's outcome — whether the client launches again — with a read-only probe. | **Done**, and the probe answered R149 too: attached to pid 16852, MaqiuDoubing 1224/1234 hp in Rogue Encampment, nothing open, can-act yes, 22 objects. The client launches again after the restart. |

## M6 — Countess flagship

### P4 — the countess run and the Cellar 5 endgame

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R235 | decision | Fix the cleanse gap before the Countess run, or run first? | Issued 2026-08-08 03:05, numbered R235 at closeout because a parallel session took R234 in the meantime — the counter is per project, not per conversation, and two sessions were issuing against it at once. Worth noting as a hazard the protocol did not anticipate. The operator observed live that the bot skipped items for a full inventory without ever running the inventory cleanse. Diagnosed: `TraverseStep` collects (it uses `_PickupMixin`) but was the ONLY collecting step that never called `maybe_cleanse` — `ClearRadiusStep`, `PickupStep` and `SurveyStep` all do. A descent is `town_preamble, waypoint, traverse x6`, so the cleanse was queued on the first cellar floor and served on none of them; and because `_mark_inventory_full` suppresses non-potion pickups for the REST OF THE GAME, the Countess's own drops were being skipped by a flag raised twenty minutes before she was reached. **Correction issued in the same breath:** the agent had also claimed `inventory_full` is never cleared — it is, at `steps.py:1399`, as post-cleanse hygiene; that claim came from a grep that only searched for `= True`. Asked: (a) fix then run, (b) run then fix. | **(a), then (b)** — *"a, then b. Make a note to investigate unwanted NPC misclicks again."* Fix applied to `TraverseStep` with two tests, the first demonstrated red without it. The NPC-misclick note went into `docs/project-state.md` with its measured lead and was spun out as a parallel session, which **fixed it the same day** (`docs/plans/2026-08-08-npc-click-clearance/`, T83 3/3 live). The Countess run was launched twice and did NOT succeed — see `p5-preflight.md` → "Where the battery actually stands". |
| R234 | execute | Re-run the Akara probe to confirm the NPC classification fix | The operator asked (2026-08-08) for a re-investigation of unwanted NPC misclicks, with two candidate fixes named. **Both were weighed and neither was the cause.** Travel clicks are aimed at `simplify()` waypoints capped at 12 subtiles, so nothing was ever aimed 59 subtiles out — that is the approach TARGET, not the click — and the probe's misclick happened with the NPC 4–9 subtiles away, well inside the 46–67 subtile horizon; so capping the first leg would have cost mobility and bought nothing. The real cause is a classification regression: review 002 (`46c83a2`, 2026-08-06 02:36) hoisted `scan_units`' combat-rated test above the ally test to get it above the corpse test, and **town NPCs carry no combat stats**, so they were filed as decorative scenery. `clickable_hazards` and `town._find_ally` both read `snap.allies`, which is why travel clicks stopped avoiding NPCs AND the heal step stopped being able to see Akara. Measured from the run event log, no live time spent: town ticks at 02:26 that day report 11 allies / 2–5 critters, every run after 02:36 reports 1 ally / 12–15 critters. Fixed (scenery must be non-combat-rated **and not friendly**; the town hazard rule no longer depends on whether an NPC carries an HP stat), 1160 tests, ruff clean. Asked: with the operator tabbed in, run the probe — it creates its own game, walks the Akara approach, and leaves. Expect ~10 allies at spawn instead of 1, and the walk to arrive. | **SELF-RESOLVED — the operator asked "can you run it yourself?", and the bridge could.** Ran read-only first (client was at the menu), then the probe (T82): **allies 1 -> 11**, the classification fix proven live. The walk still failed on `npc_menu` 4 subtiles from Kashya, so a second drill was written and run (T83, `drills/town_click_clearance.py`) to measure the clearance instead of guessing at it. It found the rule is the wrong SHAPE: three nudged clicks at world gap 6, two harmless (drawn 120 px to the side), one fatal (drawn 0 px sideways / 120 px straight up her sprite). No live time was spent on a human round trip. Both runs unattended via the bridge, both games left cleanly. Remaining work recorded in `project-state.md`; deliberately NOT improvised, since it changes the navigator's click rule for every walk. |
| R233 | execute | Launch the P5 Stage A descent run (the agent's launch path is permission-blocked) | The battery is prepared (`p5-preflight.md`) and the bridge answers — a read-only probe returned `screen: in_game, MaqiuDoubing 1316/1316`. Preparing to launch, a re-read of `tools/live-run.ps1` found a **real defect that would have wasted the run**: it started the watchdog with `Start-Process` from its own UNELEVATED context, so the watchdog would die on its first memory read (admin needed for both `OpenProcess` and UIPI). Fixed by moving the whole guarded sequence into a new `tools/guarded-run.ps1` that the bridge executes elevated as one queue entry — start the watchdog detached, wait for a real heartbeat, run the bot with `--require-watchdog`, then kill the tree and verify. Accidentally demonstrated the old failure while syntax-checking: an unelevated watchdog exited with code 1 and the launcher **refused to start the run** — fail-safe confirmed. The agent's own attempts to launch (both the queue file and `live-run.ps1` directly) were then denied by the harness permission classifier, so the operator must run the command. Asked: run the Stage A descent (`m6-descent.toml`, chicken 50, one game) and paste the output. | **RESOLVED by granting the permission instead** — the operator added the allow rules to `.claude/settings.json` themselves (the agent is blocked from widening its own permissions, which is correct), and the agent then drove every subsequent run itself. **Stage A PASSED**: full descent to the bottom cellar floor, operator-verified, no incident. Getting there took four attempts and exposed four defects in the 2026-08-07 safety diff — the launcher starting the watchdog unelevated (fixed by `tools/guarded-run.ps1`), the heartbeat's swallowed non-atomic writes, a **false death latch** from a torn `hp 0 / max_hp 921 / mode 5` read on a loading screen (now needs 3 confirmations), and the walk cap masking `NavigationError` so the NPC-dialog recovery never ran. Measured on the passing run: `nav.capped` 90, `safety.interrupt` 0, `watchdog.fired` 0, watchdog 2850 ticks / 570 s clean. |
| R232 | inform | What the unattended canary cost, and the production bug it found | Recorded rather than asked — no decision owed, but three findings are worth keeping. **(1) PRODUCTION: `bring_to_foreground` cannot work unattended.** `SetForegroundWindow` only obeys a process that already owns the foreground or the most recent input — true of every interactive run and NO unattended one, so a bridge-launched child could not take the window at all. Fixed in `pd2bot/window.py`: `force_foreground()` sends one inert ALT to qualify, attaches to the foreground thread's input queue, asks again, detaches in a `finally`, and still VERIFIES. `bring_to_foreground` escalates to it once. This was blocking any unattended run, not just the canary. **(2) The canary's own mana threshold could not fail.** 99 % against a character at 356/356 mana: `pct <= threshold` is false at exactly 100 %, so rounds 2 and 3 silently proved nothing. Now 100, with the reasoning in the code — the same "a test that can pass without doing the thing it tests" shape this repo has paid for repeatedly, this time caught by the run rather than by review. **(3) A stub snapshot crashed round 5** before it reached the check it existed to make, and took four passing rounds' results down with it. The round now uses a real `Perception` snapshot, and every round is wrapped so a crash becomes a FAIL instead of a lost report. | *(informational — no action owed)* |
| R231 | verify | Run the two safety canaries — T80 (in-process interrupt) and T81 (watchdog) | Both safety layers are code-complete and validated offline (P1–P4 of `docs/plans/2026-08-07-safety-starvation/`, 1128 tests, ruff clean). Nothing has touched the game since the death, and per the operator's R230(b) choice both layers land before the character re-enters a Cellar. **T80** (`drills/t80_safety_interrupt.py`) proves the in-process half against the real client in two rounds: the wall-clock cap with nothing watching, then a real `SafetyMonitor` armed 1 s into a walk, measuring how long the interrupt takes to arrive. **T81** needs no new code — the watchdog CLI is the instrument: `--probe` first (reads vitals, arms nothing, zero risk), then armed on mana in town so the operator can watch the game pause, then the half that is the whole claim of Track B — kill the bot mid-walk and confirm the watchdog still fires. Both run in TOWN on MANA, the live-test path `safety.py` was built with; life chicken is explicitly disabled in T80 and the interrupt is caught by the drill, so the game is not even left. Asked: be at the machine, tabbed in, and run the four commands in order. | **SUPERSEDED then PASSED, unattended.** The operator instead asked for the canaries to run *without supervision*, with the agent taking the client itself from character select. Feasible and done: `drills/safety_canary.py` (new) takes the window, creates its own game, runs five rounds, tears down in a `finally`, and leaves. Run twice through the elevated bridge. **Run 1 found three defects, two of them in the canary and one in production** — see R232. **Run 2: PASS 5/5.** Cap returned in 2.08 s (budget 2.0); `SafetyInterrupt` raised from inside a walk that had already moved 12 subtiles; a separate watchdog process with no bot running paused the game; `GatedInput` refused world input for the latch while `MenuInput` stayed open; a stale heartbeat made the engine refuse to step. Client left at `main_menu`, no latch, no stray process. |
| R230 | decision | P2 review gate — Track A is done; may the acceptance battery resume? | The in-process half of the safety fix is implemented and validated offline (P1+P2 of `docs/plans/2026-08-07-safety-starvation/`, results recorded in the phase files). The gate's four questions, answered: (1) the wiring is real — `build_bot` builds the monitor first and hands `safety_poll_for(monitor, should_stop)` to `live_navigator`, pinned by a test that the navigator holds that exact callable; (2) latency MEASURED, not derived — worst case **0.100 s** from crossing to `ChickenExit` across 40 phase offsets with the real monitor and real rate limiter, against **21.8 s** for the same scenario pre-fix (the real death: 24 s); conservatively ≤0.2 s live; (3) the next run proves it via two new log kinds, `safety.interrupt` and `nav.capped`; (4) the residual is the leave itself — ESC → Save-and-Exit still takes its own time, and that is exactly the window the watchdog (P3/P4) closes. Two deviations reported: the give-up ladder now counts across calls (or a capped walk would retry a hopeless target for ever — traverse relies on `NavigationError`, and traverse is what died), and the budget also bounds `_click` (a blocking panel could otherwise hold the tick loop for 10 s, which is what an operator's own ESC creates). Asked: approve resuming the M6 P5 acceptance battery on Track A alone, or wait for the watchdog. | **(b) CHOSEN** (2026-08-07 05:1x) — build the watchdog first; both layers live before the character goes back into a Cellar. The agent's recommendation was (a); the operator's read of the same facts is that the death happened on a supervised run too. Battery stays halted until P3–P5 land. |
| R229 | decision | Approve the safety-starvation phase table + five design questions | Discovery for the R228(b) fix is done (`docs/plans/2026-08-07-safety-starvation/notes.md`). It found the thing that shapes Track A: `ChickenExit`/`DeathHalt` are `RuntimeError`s, and the path out of `walk_to` runs through at least four broad `except Exception` handlers (`navigate._shake_loose`, the click audit, `town.py:672`, `town.py:2381`) — each correct about its own concern, every one of them able to **swallow a chicken** the moment safety is raised from inside a walk. Proposed a 5-phase `md` plan (P1 interruptible+capped `walk_to`; P2 wiring + telemetry, the gate that unblocks live running; P3 the watchdog process; P4 latch/dead-man/narration/launcher; P5 live canary + ADR + closeout) and five questions: Q1 `SafetyInterrupt(BaseException)` converted at the engine boundary vs re-basing `ChickenExit`; Q2 the 2 s wall-clock cap on the whole `walk_to` call; Q3 watchdog poll 0.2 s with a threshold 5 points below the bot's so the graceful chicken normally wins; Q4 dead-man heartbeat required for unattended runs; Q5 the watchdog as its own process/CLI started by the launcher. | **ALL YES** (2026-08-07 04:2x) — *"yes all. Execute p1&p2."* Phase table approved as proposed and P1+P2 (the offline half, Track A) authorized for immediate execution. Plan written to `docs/plans/2026-08-07-safety-starvation/`. |
| R228 | decision | Choose the chicken-starvation fix: interruptible `walk_to`, the watchdog process, or both | The T71 run-5 death (chicken starved by a 24 s blocking `walk_to`, `docs/reviews/2026-08-07-chicken-starvation-death/`) owes a safety-critical fix decision; the operator asked for a serious assessment of their idea — a separate chicken process polling HP at ~0.2 s and pressing ESC independently of the bot loop. Assessed (`watchdog-assessment.md` in the review dir): feasible on every axis (multi-process pymem reads are routine — the bridge proves it; CPU is <0.1 % of a core at 5 Hz; separate-process independence beats priority tricks), and the safe design is ESC-pause-only + latch file + dead-man heartbeat, never menu clicks — `UI_ESCMENU_MAIN` is a blocking panel so the bot's own `GatedInput` disarms at send time the moment the menu opens. But it is not a substitute for the in-process fix: the same block also starved the operator's abort channel (`_should_stop`, polled per tick), and the engine's capped-legs contract is what broke. Recommended: in-process safety poll inside `walk_to` + wall-clock cap first (small, offline-testable, regression-pinned), the watchdog as defense-in-depth before the unattended battery. Options put: (a) in-process only, (b) in-process now + watchdog before the battery [recommended], (c) watchdog only [recommended against]. | **(b) CHOSEN** (2026-08-07 04:0x) — the in-process fix lands first (safety poll inside `walk_to` + a wall-clock cap, offline-testable, pinned by a starvation regression test), and the ESC-pause watchdog process is built as an independent second layer before the unattended battery. Planned in `docs/plans/2026-08-07-safety-starvation/`. |
| R227 | decision | Approve the item-acquisition plan and the command-by-GID spike | Analysis of pickup (kolbot source in-repo) found the frame-perfect pickup the operator remembered is a `0x16 PickupItem` command addressed by GID — no screen aim at all — not a fast click. Our actuator is aim-based with a measured ceiling (T76: 7/8 unpickable in a pile). Proposed a 5-phase plan (`docs/plans/2026-08-06-item-acquisition/`): separate detection from acquisition (P1), improve the click loop as the safe floor (P2), hunt BH label geometry (P3), and a GATED, dangerous spike (P4) for out-of-process command-by-GID — a new memory-WRITE capability the project has never had, which reopens the actuation ADR and can crash the client. Three questions put. | **Q1 YES** *"lets try and see. Be ready to rewind all changes we make."* — Track 2 licensed as a spike on an isolated branch. **Q2 YES** — supersedes `2026-08-06-pickup-reliability` P3/P4; its P1/P2/P5 stand. **Q3 YES** — the supervised crash-risk spike is acceptable. Exploration branch `item-acquisition-spike` created off `m6-countess`; the risky write path lives there and is rewindable wholesale. |
| R226 | execute | Drop ten different PD2 maps for T77 to read | The operator asked for the item-exception work to cover PD2 maps, and offered: *"prep a small test - I'll drop 10 different PD2 maps, and you can read the log of the dropped item."* Maps appear under no code in `config/item_codes.toml`, so their kind must be READ — R144 forbids a numeric guess, which is how six wrong elite armours were approved and a Wire Fleece was picked up as a Kraken Shell. `drills/t77_map_identity.py` is written, unit-tested and READ-ONLY (sends nothing, the character does not move). It diffs the floor before and after the drop. The question it exists to answer is not just "what kind is a map" but **one kind or many**, which decides whether the registry needs one entry or a set. Asked: stand somewhere safe, drop ten DIFFERENT maps, type GO. | **PASS** — ten maps read, and the answer is **ten DISTINCT kinds**, not one: 737/740/741/743/746/747/748/750/775 (codes t11, t21, t22, t24, t32, t33, t34, t36, t26) plus **810, which has no code at all** — `item_codes.toml` holds 800 entries, so T42's sweep ended short of it. The codes are a FAMILY, verified afterwards: the table holds exactly 30 `t\d\d` codes, kinds 737-788, with no non-map collision. So the registry should MATCH THE PATTERN rather than list kinds — that covers all 30 known maps including the 21 never sampled, and survives a season renumbering, which a kind list does not. Kind 810 stays an explicit observed member until T42 is re-run. Write-up: `t77-map-identity.md`. |
| R225 | decision | Re-run T76 after the drill's own defects were fixed | T76 run 1 FAILED INCOMPLETE and could not explain itself: `except InputRefused` printed nothing, so a solo barbarian helm reported "never came up" with zero per-offset lines and no way to tell eight refusals from eight misses — the silent-decision-path shape this project has paid for four times. Also: the census radius was 15 subtiles (borrowed from T63, where the operator drops at their feet) so "saw 1"/"saw 3" could not be distinguished from "outside the radius"; and demanding exact drop counts made the operator responsible for a number the census had to agree with, skipping both crowded rounds. All three fixed — every path prints, radius 40 with the full floor census printed, and crowding is now MEASURED per target (two loose rounds: spread, then packed) instead of asked for. Asked: re-run when convenient. | **PASS (run 2)** — 15 targets across solo/pair/pile, two classes measured both ways, and every path prints now. Headline: in a dense pile **7 of 8 targets could not be picked at all**, each spending the full 8-offset schedule; **6 clicks landed on a neighbour** (T71's inference, now measured directly); and the first offset won only **1 of 15** targets, winners scattered across four different offsets. The hypothesis is therefore only half right — it is not that the winning offset MOVES predictably, it is that for a crowded item often NO offset in the schedule works. This selects Direction C (unstack the pile behaviourally) over Direction B (derive the offset). The run also caught a defect in the drill itself: `classify()` ignored PD2's `s` family (`r05s`, `gzvs`) that R126 had already flagged as the one that actually drops, filing two runes and a gem as "other" — fixed, and both runes had in fact FAILED, one of them at crowd 1. Write-up: `t76-calibration.md`. |
| R224 | decision | Approve the pickup-reliability plan: phase table + the measure-first gate | The operator asked for a process to fix pickup accuracy and offered both calibration runs and a mechanics redesign, suggesting we track item coordinates and the mouse pointer. Discovery answered the suggestion: item coordinates are already tracked (and every wanted drop is now logged, fixed 2026-08-06), but the pointer path is measured shut — T60 run 3 put 421 synthetic cursor moves across a potion with the label highlighting and got ZERO pointer flips (it tracks a real hand), and T63 settled that clicks resolve against cursor POSITION, not pointer state, which also retro-answers T61's unanswered Q2. `units.hovered_item_id` is therefore production-ready code that would verify nothing. New evidence from T71 run 4 gives one hypothesis covering all four findings — PD2 displaces item labels when items are close, and our aim schedule is 8 FIXED offsets, so a displaced label means all 8 miss (bimodal), worse in dense fields (26.3 vs 15.1 items on screen), landing on a neighbour's label ("clicked A got B", 9 of 13), and worst for label-only classes like runes and gems (the Nef rune and the flawless emerald). Asked: approve the 5-phase table, the measure-before-fixing gate at P2, and the recommendation that this precedes the P5 acceptance stages. | **Q1–Q3 all YES** (2026-08-06 05:2x). Operator added a working constraint: *"Pause before we need any game time; we'll leave that for later. Do as much as possible without needing access to the game."* So P1 (instrument gaps, no game) runs immediately, and the P2 calibration drill is to be **written and unit-tested but not launched** — the plan is executed up to the live boundary and stops there. |
| R223 | verify | Launch the first live Countess run (T71 run 3) and judge the north staging approach | Everything upstream is live-proven (T72 run 2: both transitions clean, the Tower crossed in 4.3 s); the endgame is sim-proven only. Pre-launch, the state doc's ordered re-check found review 001's shape one layer down: a chamber sweep whose points were all skipped as unreachable still concluded "provably absent", so a bot pinned short of the chamber could complete the run objective having seen nothing — fixed (skips now void the absence proof and stop loudly, ce3ba82), 1001 tests, ruff clean. Bridge probed up, client at the menus, no stale cancel file. Asked: tab into the game and say go; the drill creates its own game, chicken 35 (R212 Q8). Watch the whole run, and specifically the R219(b) judgement still owed: the north staging walk to (12531, 11036) before the advance — does the approach read right on the room you know by sight? | **Launched and PASSED — T71 run 4, 930 s, `[CVRL]`, the Countess is dead** (corpse at (12541, 11088), drop zone swept, two runes collected). **But the judgement could not be collected, and re-asking would be futile: the staging never happened.** `engage` is consulted before the phase dispatch in `ClearCountessStep.step`, so from the neighborhood clearance to her death every tick was owned by the fight — 22 of 27 `clear_countess` decisions are the bare `acted` the engage branch alone produces, `notes["countess"]` was never written, and the walk to (12531, 11036) was never made. The Countess always has a court, so this is structural, not luck. **R219(b) therefore remains open as a DESIGN question rather than an observation request** — does staging-before-contact need to pre-empt engage, or is it a beat that only fires on an empty approach? Deciding that comes before any re-run. **Protocol lesson, second time on this same request:** R219 already recorded that a `decision` framed around coordinates was really a `verify`; this run shows the `verify` was unanswerable because the behaviour is unreachable. Before asking a human to watch for something, check that the code can actually produce it. |
| R222 | execute | Second census with real monsters (T73 run 2) | The first census sampled the Forgotten Tower (2 "hostiles", both the decorative bats). The discriminator needed real monsters in the same frame: stand in Tower Cellar Level 1. | **Done — T73 run 2 PASS** (backfilled 2026-08-06 02:51): 39 hostiles in Cellar 1, ALL missing the alignment stat exactly as the bats do — the "did it prove it is friendly" test was unsalvageable. Stat counts by verdict (hostile 5-10 stats, ally 76) pointed at the real discriminator, measured next in T74: a combatant carries combat stats (`offsets.COMBAT_RATED_STATS`). |
| R221 | execute | Stand in the Forgotten Tower for the hostile census (T73) | T72's log proved the hostile detector is broken: it counted 2 then 5 "hostiles" in the **Rogue Encampment**, which has none, and spent 173 s in the Forgotten Tower attacking two units (650119, 650120) 23 times each that never died. The user confirms that floor never contains hostiles. Two candidate faults found in the code, both real: hostility is inferred from the ABSENCE of a friendly-alignment stat (a torn read reads as an enemy), and locality is an 80-subtile radius with NO area check, while the hash table holds "units from other levels, expired summons" by its own docstring. kolbot filters `m.area === me.area && m.attackable`; we do neither. Asked: walk the character to the Forgotten Tower floor (merc and revives up if convenient — that is the state T72 failed in), say go, and let T73 run. It is READ-ONLY: no input is sent, the character does not move, ~5 s. It prints every monster-type unit with its level id, its mode, and — the decisive field — whether the alignment stat is PRESENT, ABSENT, or the stat list unreadable. | **Done — T73 run 1 PASS** (backfilled 2026-08-06 02:51): in the Forgotten Tower, exactly 2 "hostiles" persisting across all samples, kind 159 (MonStats `B9` — the decorative bats), both MISSING the alignment stat, none in a different level, none unreadable. The phantom targets identified; the second census with real monsters raised as R222. |
| R220 | decision | Run-log design: confirmations Q1-Q11 | The rigorous-logging upgrade needed eleven decisions before phases could be written: JSONL + renderer (Q1); centralized `logs/runs/<stamp>-<runname>/` keeping every run (Q2); three coordinate frames on every spatial event (Q3); dual wall-clock + monotonic stamps (Q4); item names from the existing code-anchored tables with an honest `kind <n>` fallback (Q5); enemy-death events on alive->corpse transitions (Q6); "accidental" defined as an unrequested state change (Q7); narrative log kept alongside (Q8); per-tick timing instrumentation (Q9); revert the `exit_block_radius` contested-staircase rule (Q10); logging MANDATORY rather than opt-in (Q11). | **Q1-Q5, Q7-Q11 YES. Q6 NO — deferred** (*"might be too complicated"*): enemy-death logging is out of scope and moves to future work in the plan's notes.md, with the design recorded for a later revival. Q10 confirmed the contested-staircase rule dies with the hypothesis behind it — the user states the Forgotten Tower never contains monsters. Q11 makes instrumentation non-negotiable, which is the direct lesson of T71. Plan written: `docs/plans/2026-08-05-run-event-log/` (7 phases, md). |
| R219 | decision | Go/no-go: the countess run (sim trace, run file, north staging) | P4 is implemented sim-first and gated before any live run (the M5 P5 precedent). Presented for judgment: (1) `runs/countess.toml` — the proven descent spine + `clear_countess` (aggressive) + chamber pickup; (2) the sim traces — `docs/plans/2026-08-03-m6-countess/p4-sim-trace-countess.md` (85 ticks: straggler fought en route, restaged Thul collected mid-traverse, neighborhood cleared, staged north, struck, poison kill, in-and-out sweep, both drops lifted) and `p4-sim-trace-absent.md` (the blinded-read path concluding provably absent); (3) the staging derivation — `p4-staging-derivation.md`: with screen-north = the world (-1,-1) diagonal, strictly-north-outside ground is solid rock on this seed, so the derived staging is the chamber's own NW band at (12531, 11036), advance running screen-SE into her T68 anchor (12548, 11036). Asked: (a) is "approach from the north" the screen-up convention as assumed; (b) is the staging point acceptable on the map you know (name a better coordinate if not); (c) go for the first live countess run (chicken 35, R212 Q8)? | **(a) CONFIRMED** — screen-north is the world (-1,-1) diagonal, as assumed; the derivation stands. **(b) DEFERRED by the user** — *"a little unsure what this means... perhaps we can run a test and I can respond afterwards"*: coordinates on a plotted grid are the wrong instrument for a judgment about a room the user knows by sight. Judged live instead — T71 walks the derived (12531, 11036) and the user watches the approach, so the answer comes from the thing itself. **Lesson for the protocol:** a `decision` request whose evidence is a coordinate map is a `verify` request wearing the wrong hat; prefer showing the behavior to describing it. **(c) GO** — T71 launched with the user tabbed in, chicken 35. |

### P2 — waypoint tabs and traverse

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R218 | execute | Clear stash space — the preamble halts every run until it exists | T70 run 2 started clean and the town layer halted on its own guardrail: 1 item (kind 534, tome_of_identify) would not go into EITHER stash tab after both deposit passes, with 420 carried items on the character. The run never reached the waypoint, so nothing about traverse was tested. This is not a bug — the halt is the R48 no-vendor policy working (the bot cannot sell, so it cannot make room) — and it will stop EVERY M6 run, since every preamble deposits. Asked: free up regular-stash space (a few columns at least; Countess runs generate rune/gem loot steadily), then say go for the T70 rerun. | **WITHDRAWN — the request was wrong, and the user corrected it from what they watched: the stash is NOT full. The bot MISCLICKED the tome** (plain right-click instead of shift+right-click), the cursor changed to identify mode, and every retry fed that instead of the stash — the R112/R113 failure recurring verbatim on the same item. R113 fixed the same-frame shift RACE (a settle) but never the blast radius, and `StashFull` was asserted from a failed click with no evidence, which is the belt-full bug (T56/T57) in a different container. Fixed in four layers (tomes now unmovable like the Cube; retries clear the cursor by close+reopen; the diagnosis is evidence-checked against what else deposited; one stubborn item is notice-and-continue). 904 tests. Full post-mortem: `docs/reviews/2026-08-05-tome-misclick-feedback.md`. |
| R217 | decision | Standing mandate for the P2 live batch | GRANTED ("go", user seated 2026-08-05). The R207-shape mandate for P2's calibrations and drills, run when the user says go (they were mid-Cellar-5 at issue time): (1) the waypoint calibration battery — user opens the waypoint panel in town and hovers on chat prompts (act tabs II/V + rows Black Marsh / Arcane Sanctuary / Halls of Pain, ~2 min); (2) T-exit-read in Black Marsh (area ids asserted live, Q1); (3) T-traverse-one (Black Marsh → Tower); (4) T-descent (full chain to Cellar 5, chicken 50, super-unique catalogue en route). Per-drill OK gates waived within the batch; abort paths standing; the user tabbed in for calibrations and hands-near-controls for the traversal drills. | **Granted and in flight.** T69 PASS 4/4 (all six fractions baked, tab-aware travel shipped, Black Marsh id verified, Halls of Pain read as 123 against classic lore). T70 run 1 NOT STARTED (announcement undeliverable — the game was not foreground while the user read app-side instructions; the drill protocol needs the person IN the game before launch, recorded as a lesson). T70 run 2 hit the tome misclick (fixed, post-mortem in docs/reviews/2026-08-05-tome-misclick-feedback.md). Run 3 PASS (first autonomous area change). Run 4 FAILED in Cellar 1 and exposed the preset-loading limit -> seek-and-remember built. **Run 5 PASS: THE FULL DESCENT, town -> Cellar 5, 796s, all 7 transitions proven, 11 staircases banked in maps/exits.json. The batch is COMPLETE and P2 is DONE.** User observations logged for later: the ignored Thul rune (traverse has no pickup logic) and residual dithering (optimization pass). |
| R216 | verify | Confirm the Countess candidate with your eyes | T68's read-only probe (run while the user stood in Cellar 5) found the exit read fully working and two boss-flagged units: kind 734 unique_no 6 at (12548, 11036) — ~56 subtiles WEST of the player, and unique_no 6 is superuniques.txt's Countess row — and kind 21 unique_no 0 nearby. Asked: is the Countess visibly alive on/near your screen to the west? A yes pins (kind 734, unique_no 6) as her named constants with T68 as provenance. | **CONFIRMED** — the user verified her by eye; (kind 734, unique_no 6) pinned as `COUNTESS_KIND`/`COUNTESS_UNIQUE_NO` in offsets.py with T68 provenance. The kill condition (P4) matches on exactly this pair. |
| R215 | inform | Hold-left-click moves without interacting (user discovery) | User, 2026-08-04: holding the left button down moves the character to the destination WITHOUT picking up items — movement that does not touch the game world, usable to prevent misclicks; possibly replicable by the bot. Asked (implicitly): note it, plan it, test it, implement at an appropriate juncture. | **Recorded and planned** (M6 plan notes.md future work + behavior.md future notes): drill-first (hold LMB across dropped junk, PASS = zero pickups), then a `hold_move` input primitive whose guard design must handle an input that SPANS ticks (release-on-guard-false — new territory for the gate), then adoption in the navigator's travel legs. Natural juncture: alongside the P5 battery or a spare live-session moment; not scheduled into a phase yet. Also this exchange: the closed bridge was restarted agent-side via `Start-ScheduledTask pd2bot-bridge` (probe: elevated, alive) — the restart no longer needs the user at all. |

### P1 — transit and boss perception

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R214 | decision | Commit P1 and continue with P3? | P1 implemented on `m6-countess` (873 tests, ruff clean; details in the phase file's Implementation Result): exit perception (BH-cited RoomTile chain, cross-checked against d2mapapi_mod), boss identity (unique_no + name, provisional is_super_unique), area constants 6/20/21-25, the `dump --exits` probe, 9 new tests. Asked: (a) approve the P1 commit + push; (b) continue directly with P3 (postures, parking, revive urgency, review fixes — sim-only, no live time needed), leaving P2's calibrations and traversal drills for when the user is at the machine. | **Yes to both.** P2 reserved for when the user is ready; standing guidance for the stretch: do everything possible WITHOUT the bridge or game client — in-game testing comes later. P1 committed and pushed; P3 proceeds sim-only. |

### Planning

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R213 | decision | Merge PR #1 into main? | Follow-up to R211 at the user's request: a patient explanation of commits/branches/PRs/merging delivered in chat (main still ends at M4; all 89 M5+plan commits live on `m5-trial-run`, committed and pushed; merging = accepting the proposal so main becomes the live-proven M5 state; no conflicts, no CI). Recommendation: merge now, then a fresh `m6-countess` branch — one branch per milestone, merged at acceptance. Agent merges on the word "merge", or the user clicks the green button to learn the flow. | **"Merge please" — merged 2026-08-04 02:43 UTC** (the git/GitHub explanation landed: "that makes perfect sense"). main fast-forwarded 9ed9a0e → 33b3688 (244 files, +74160): the official line now carries all of M5 and the M6 plan. Fresh branch `m6-countess` created off main for the milestone's work — the one-branch-per-milestone rhythm adopted. Four glossary entries added from the exchange (branch, merge, pull request, push/pull). |
| R211 | decision | Approve the M5 closeout commit | The closeout (items 1–10) is complete on `m5-trial-run`: behavior.md, the ADR accepted, pointer docs/README/CLAUDE.md/roadmap, teach + glossary, sweep (864 tests, ruff clean), instruction log reconciled, both finished plan dirs archived with the M5 `_DONE.md`, project-state at M6/R211/T68. Per the M4 precedent: approve one conventional commit of the closeout (and a push to the open PR #1 — the merge itself stays your decision). | **Approved ("yes, please go ahead"), with yona-review/yona-push left to the agent's judgment.** Committed `f1c6276` (34 files, +926/−72) and pushed; PR #1 updated, still OPEN awaiting the user's merge. The repo has no CI, so yona-push's check-watching had nothing to watch. A formal yona-review was SKIPPED, reasoned: the closeout is documentation plus one code-comment path fix; the full suite + ruff gate it, and a review artifact over prose would be ceremony. |
| R212 | decision | M6 plan: confirmations Q1–Q9, exit-discovery Q10, and the phase table | The yona-plan pass for the Countess run (plan dir `docs/plans/2026-08-03-m6-countess/`, discovery in notes.md). Q1–Q9 batched (area-id verification, Black Marsh waypoint calibration drill, navigator-owned doors, corridor-only traversal combat, brisk-as-first-preset, executor-level right-skill parking with ~2 s grace, no-hostiles-in-chamber as the kill condition, chicken staging, folding the carried P3 review issues); Q10 discussed (exit discovery via memory-read RoomTile warps vs data-driven transition recording — memory-read suggested, ADR candidate); phase table P1–P6 proposed alongside. | **All answered 2026-08-03; plan written to the answers.** Q1/Q6/Q8/Q9 yes as suggested. Q2 yes EXPANDED: also calibrate Act II Arcane Sanctuary + Act V Halls of Pain rows, which means calibrating the act tabs. Q3 **NO — doors deferred** (no doors need opening on the route; cellar connections are single-click staircases); future-work note: bundle doors with chests/boxes/barrels and teleporter gates as "interactive objects". Q4 yes with the Cellar-5 exception (kill all nearby before the Countess — no gaggle). Q5 yes and sharpened into a posture SCHEDULE: brisk Black Marsh → Cellar 4, **aggressive** (new posture too) on Cellar 5 — backs off from closing groups, attacks when safely feasible, especially isolated enemies. Q7 yes plus a <15 s chamber sweep against blind corners AND investigate reading the Countess directly (boss/super-unique perception — P1). Q10 memory-read confirmed; user designed the fallback calibration survey (manual walk, "Exit" + hover per transition). Phase files 01–06 written; plan committed. |

## Ad hoc — CI setup (2026-08-09)

| ID | Type | Title | Asked | Outcome |
|---|---|---|---|---|
| R236 | decision | CI plan: confirmations Q1–Q5 | The yona-plan pass for adding GitHub Actions CI (plan dir `docs/plans/2026-08-09-ci-github-actions/`, discovery in notes.md). Discovery settled the one real technical question: `pd2bot/window.py:20` and `pd2bot/input.py:42` evaluate `ctypes.windll` at import time and the tests import those modules, so CI must run on a Windows runner — a Linux runner cannot even collect the suite. The repo is PRIVATE, so Windows minutes bill at 2× (~4–6 billed min/push against a 2,000-min/month free allowance — comfortable). Locally green today: 1169 tests, ruff clean. Q1 `windows-latest` runner; Q2 trigger on push (all branches) + PRs to `main`; Q3 pin Python 3.12; Q4 install per the README recipe (`pip install pymem pytest ruff` — deliberately no editable install, per pyproject's own comment); Q5 ruff failures fail the build alongside pytest. | **ALL YES** (2026-08-09 02:4x) — *"yes all."* Plan written as `sm` (one workflow file); implementation left for a `yona-implement` run. |

| R237 | decision | Approve the CI commit + push (the push IS the validation) | The plan is implemented: `.github/workflows/ci.yml` written exactly as planned (windows-latest, push + PRs-to-main triggers, concurrency cancel, pip install per the README recipe, pytest then ruff as separate steps), README's Development setup section gained the CI paragraph with the honest offline-only boundary. Local validation green: 1169 tests, ruff clean. But acceptance criteria 1–2 can only be met by GitHub running the workflow, and that requires a commit and push to `m6-countess`. Asked: approve (a) one conventional commit of the workflow + README + glossary/log updates + plan dir, pushed to origin, with the agent watching the first run via `gh run watch`; then (b) after the run is green, a small follow-up commit with `_DONE.md` and the archived plan dir. | **ALL YES** (2026-08-09 03:1x) — *"yes all."* Commit `220aa2a` pushed; **first CI run GREEN in ~40 s** (both events: push 34 s, pull_request 44 s — an open PR re-checks on every push, as the plan predicted). Cost far under the 4–6 min estimate. One deviation folded into the closeout commit: the run's Node-20 deprecation annotation on `checkout@v4`/`setup-python@v5` → both bumped a major, validated by the closeout push's own CI run. Plan archived; teach explainer written (`docs/learning/2026-08-09-continuous-integration.md`). |

| R238 | decision | Dependency-pinning plan: confirmations Q1–Q5 | The yona-plan pass for reproducible installs (plan dir `docs/plans/2026-08-09-dependency-pinning/`). Discovery: the venv holds stowaways (`cmake`, `ninja` from the C++ map-generator work), so a naive `pip freeze` lockfile would pin build tools into the bot — the lockfile must be curated; the real graph is 8 packages (pymem, ruff, pytest + five pytest transitives), all green today. Proposed: a curated exact-pin `requirements.txt` used by README and CI; pyproject keeps loose floors as intent; and a weekly scheduled "canary" CI job that still installs UNPINNED latest to preserve the early-warning signal pinning would otherwise silence (repo is public now, so the extra job is free). Q1 curated requirements.txt with all 8 exact pins; Q2 README + CI switch to `pip install -r requirements.txt`; Q3 pyproject floors stay as intent with a pointer comment; Q4 weekly unpinned canary job in ci.yml (its failure does not block merges); Q5 documented update ritual (bump in venv → checks → edit pins → push). | **ALL YES** (2026-08-09 03:4x) — *"yes all, then /yona-implement."* Implemented and validated the same session: scratch venv from ONLY the lockfile → exactly 8 packages, 1169 tests green, ruff clean; pushed `fa56adb`; CI `checks` green installing from the lockfile on the fresh runner, `canary` correctly skipped on the push event. No deviations. Known dormancy recorded in `_DONE.md`: the scheduled canary cannot fire until `ci.yml` reaches `main` at the M6 merge (GitHub runs schedules from the default branch only). Plan archived; teach explainer + three glossary entries written. |

| R239 | decision | Package-restructure plan: timing (Q-A) + confirmations Q1–Q7 | The yona-plan pass for the structural refactor (plan dir `docs/plans/2026-08-09-package-restructure/`). Discovery: `steps.py` splits cleanly by class (each step is independent; `_PickupMixin` alone is ~780 lines); `town.py` is one 60-method class, split via the repo's own mixin precedent, not redesigned; 760 `from pd2bot` imports across tests/drills/sims would be touched; 13 documented `python -m pd2bot.X` commands must keep working verbatim (root shims); `offsets.py` staying at root preserves 38+ hub imports; `input/` and `runlog/` as re-exporting packages preserve current import spellings. Proposed: subpackages named after the architecture docs (perception/, input/, nav/, safety/, runlog/, behavior/ with town/ and steps/ inside), root = commands + shared vocabulary, per-package READMEs, tests mirrored to the tree, ADR expected. Discussion Q-A: execute now on a dedicated branch (with a live smoke run before merge-back) vs wait for the M6 boundary. | **NOW + ALL YES** (2026-08-09 04:1x) — *"now; all yes, then /yona-implement."* Plan written as `md`, 7 phases, executed on branch `restructure`; live smoke run gates the merge-back to `m6-countess`. Outcome of the implementation recorded when the cycle closes. |

| R240 | execute | Clear the client for the restructure smoke run | All seven phases of the package restructure are complete on branch `restructure` (tests green after every phase, CI green, docs/READMEs/ADR done). The merge-back gate is one live Cold Plains smoke run through the guarded launcher. The bridge probe found the client IN A GAME with monsters engaged (MaqiuDoubing, act 5, one target at 1% hp) — the run would take the window and cycle games, so it is not being launched over active play. Asked: when convenient, leave the game to the main menu (or character select) and say "go" — the agent runs the smoke itself via the bridge (guarded-run, cold-plains, 1 game, watchdog up) and reads the event log after. | **"go" — SMOKE PASSED, unattended.** One false start (the queued command's path was mangled by a printf octal-escape collision — caught before any game contact, requeued via heredoc), then the guarded run played a full Cold Plains game on the restructured code: 515 ticks, waypoint sequence clean, clearance fought, stash deposit, clean leave, exit 0, **monitor silent** (no chicken/death/interrupt events), watchdog heartbeat clean throughout. Pickup 5/11 with the pre-existing miss signatures (arriving short, pile ambiguity) — known behavior reproduced, no regression. `restructure` merged into `m6-countess` (`410b7b1`), 1169 tests green on the merge. |

| R241 | decision | Combat & logistics plan: confirmations Q1–Q8 + phase table | The yona-plan pass for the user's seven-item bundle (plan dir `docs/plans/2026-08-09-combat-logistics/`; item 4 dropped by the user as unwise). Discovery: vendor UI is a policy boundary, not a capability cliff (the Charsi repair flow already drives dialog row → shop screen, effect-verified — the Akara buy adds only shop-grid geometry + one calibration drill); the line recorder is new-but-small (T52 harness + existing simplify(), stored per seed in maps/ beside the atlas); postures are number-override tables with skills excluded by design, so a fight-STYLE selector is a deliberate line-crossing (ADR); berserk's survival needs are already covered by the reflex ladder sitting above the class module; mandatory pickup must poll item-gone (T51 expiry) apart from item-unreachable, and the walk-away cleanse must exceed the return scan radius. Ordering constraint: potions leave pickit only in the same commit that lands the proven restock chore. Q1 config trio first; Q2 line storage in maps/; Q3 posture-style crossing; Q4 reopen R48 (buy only); Q5 potion-removal ordering; Q6 berserk supervised acceptance; Q7 pickup pilot flagged on Cold Plains; Q8 standing mandate for the live batch. Phase table P1–P6 proposed alongside. | **ALL YES** (2026-08-09 21:4x) — *"all yes, then /yona-implement."* One refinement on Q4 from the user: potions may not sit at fixed shop grid cells, so the chore must (a) read the actual cell per visit and (b) aim there. Folded into P4's design: vendor stock = carried items owned by Akara's unit (same reads as the player's inventory, grid cell included), read fresh per visit; the hover calibration covers only the panel's fixed grid origin + cell size. Implementation authorized; branch `combat-logistics`. |

| R242 | execute | The R241 live batch: line, leash, berserk, shop, pilot | All six phases' offline halves are implemented on `combat-logistics` (P1 fully done; 1221 tests, CI green; ADRs written). The standing mandate (Q8) covers this batch once the operator is at the machine with the client up. Sequence: (1) **T84** — walk the Cold Plains route once, END in chat (records the line); (2) the **leash run** — one cold-plains game, agent-driven, route.stray events expected; (3) **berserk supervised** (Q6) — one clearance with posture=berserk, operator watching, hands near ESC; (4) **T85** — open Akara's trade, hover three named cells (calibrates the shop grid); then the agent implements the buy chore against the measured grid, runs the restock drill from a depleted belt, and ONLY on success removes the potion pickit rules in that same commit (Q5); (5) the **order-book pilot** — one cold-plains-pilot-orders game, census reviewed by the operator before the flag goes anywhere else. Merge to m6-countess after the batch; teach step closes the cycle then. | (pending) |

| R243 | decision | Chat may type while the TRADE screen is open | User-initiated during the R242 batch: T85's prompts never reached them because `Chat`'s blocking-panel guard refuses while any panel is up — *"likely the bot cant chat to me while akara's shop is open. Let's remake the test, with a special permission to chat while the trade screen is open."* Analysis concurs with the narrow form: the guard's hazard is the dialog LIST (Enter selects a row — the M1 shape); the shop GRID has no Enter-activated default, and the verify-by-effect check (console verified open before any typing) remains regardless, so a wrong allowance costs one inert Enter. | **Implemented as an opt-in constructor allowance** (`Chat(allow_panels=...)`, default empty — nothing changes anywhere else); T85 passes `{UI_NPCSHOP}`. The same debugging pass caught a second, unrelated T85 bug: Akara's unit ID was passed where her unit ADDRESS belongs (the `MemoryReadError` at address ~110). Both fixed; drill requeued. |

| R244 | decision | Resume live testing: the verification batch + P5 census | Both R242 leftovers were solved OFFLINE from the 2026-08-10 logs (see `docs/plans/2026-08-09-combat-logistics/notes.md`, 2026-08-13 section): the order "discrepancy" was the run log clobbering its own event kind (orders booked fine all along), the cleanse starvation was a two-cause branch gap, and the stash misclick was the T83 sprite rule missing from deliberate interact clicks — all fixed, instrumented, tested (1239 green), committed (`7a2f7d9`, `b20bce4`). The decisive pilot-orders run is no longer needed for diagnosis. What live testing is still for: (a) 2–3 cold-plains pilot games WITH preambles — each preamble exercises the misclick fix (base rate was 2 aborts in 6), and the runs produce the order/cleanse event stream under real kinds plus the P5 census for operator review; (b) whitelisted drops are rare in Cold Plains post-Q5, so the operator-drop drill (drop a gem/rune mid-run for a deterministic order lifecycle) is re-offered as the fast path. The standing mandate was paused 2026-08-10 ("Let's pause live testing"), so resumption needs the word. | **(a) approved 2026-08-13** — cold-plains pilot games with preambles, full per-run logs preserved for later review/query. **(b) DECLINED in the offered form**: no operator drop-drill; the item-pickup/whitelist drill is DEFERRED until after (a), and the operator will specify the test battery for it themselves. Batch outcome recorded here when it closes. |

| R245 | execute | Relaunch R244 game 1 after the restock over-buy fix | Game 1 aborted by the operator: the restock station bought healing/mana into the inventory for 127.5 s until the client's can't-carry popup. First diagnosis (station holes: no `_belt_accepts` room check, clicks-not-purchases bound) was CORRECTED by the operator's observation (belt visibly full, 2 mana in it, bought mana entering the belt): the ROOT cause is the item-chain walk cap — the player's chain is 560 items (the expanded stash rides it) and `MAX_CARRIED_ITEMS = 512` silently truncated the read, so belt/worn/inventory read empty (`repair: 0 worn` vs 08-10's `8 worn` is the same lie). Fixed: cap 4096, `CarriedItems.truncated`, preamble refuses truncated reads; the station hardening ships too as defense in depth. Live re-probe: 560 items, truncated False, belt 15, equipped 8. 1245 tests green. Asked: say "go" when ready to relaunch game 1. | **"go" (2026-08-13 02:28)** after the operator reviewed the corrected plain-language explanation ("that makes sense"). Game 1 relaunched via the bridge; outcome under R244's batch record. The optional belt question was superseded: the operator volunteered the observation unprompted and it broke the wrong diagnosis. |

| R246 | verify | P5 census gate: review the batch and rule on mandatory_pickup | The R244/R245 verification batch is complete: game 1 verified the truncation/restock/misclick fixes live before a (known, third-occurrence, now-instrumented) watchdog freeze ended it at 310 s; games 2 and 3 ran COMPLETE (451 s / 644 s, no freeze). Game 3 exercised the full order design end-to-end: open → resight-through-combat → id-churn rebind → gone-on-expiry (diamond, ~285 s on the floor while combat held priority) → full-inventory → queued cleanse → mid-field cleanse (6 dropped) → post-cleanse retry → budget close → convergence across a second id churn → the rune collected anyway by the ordinary sweep. Census: game 2 = 1/3 (belt-full honest), game 3 = 2/3 with the miss's item ultimately collected under its churned id. Logs: `logs/runs/20260813-022855/-024533/-025430-cold-plains-pilot-orders`, all preserved and queryable (`python -m pd2bot.runlog <dir> --pickup`). Asked: review and rule — promote `mandatory_pickup` beyond cold-plains, keep piloting, or adjust (e.g., the order budget vs the T51 expiry window; combat priority let the diamond expire). | (pending) |

| R247 | decision | Hotkey-config plan: read the client's .key file, consolidate all bindings | The yona-plan pass for the operator's detour (plan dir `docs/plans/2026-08-13-hotkey-config/`). Discovery pinned the .key format empirically (per-character file, self-validating 20-byte records, labels cross-checked against live bindings + default.key). Plan md, P1 parser / P2 registry+dedupe / P3 consumers+wiring+verification / P4 live proof+ADR+docs. Design calls recorded in notes.md: toml keeps skill→key shape but is VERIFIED against the file (loud drift refusal); non-skill bindings come from the file; defaults as fallback with notice; per-game re-read. | **PRE-APPROVED at issue** — the operator authorized planning AND implementation in one instruction ("unless there are any questions for me, go ahead… I trust you"); no pressing design decisions found, so no questions were issued. Implementation proceeding same session. |

| R248 | inform | Specify the pickup-calibration detour | Session resumed from the 2026-08-13 handoff (branch `combat-logistics`, 1275 tests green, R244–R247 resolved, R246 standing). The operator announced a small detour before the standing queue: **item pickup setting calibrations**, specifics to be supplied by them — the HANDOFF's first OPEN item says to hear the proposal before touching anything else. The deferred R244 (b) item-pickup/whitelist battery (declined in its offered form; operator to define it themselves) likely folds into this. Asked: describe the detour — which settings/calibrations, what the battery should measure, and what "done" looks like. | **Resolved 2026-08-13 05:35** — the operator specified a three-mode name-tag accuracy battery: (1) no tags (ALT off — current state), (2) loot-filter tags (ALT on), (3) default non-filter tags ("F", currently off); bindings to be double-checked against the known hotkey list. Method proposed: drop all inventory (NEVER the Horadric Cube), pick up whitelisted items only, ~30 s per round, ~5 rounds per mode, back-to-back in one game, town chores suspended, gather EVERYTHING at the end (no whitelisted losses). Measure accuracy, speed, and non-whitelisted pickups per round; tabulate and rule. Agent authorized to review/edit the proposal and run it if reasonable, query only if trouble. |

| R249 | execute | Launch the tag-mode battery: T91 F-probe, then T90 (15 rounds) | P1 of `docs/plans/2026-08-13-pickup-tagmode-calibration/` is complete offline (1289 tests green, ruff clean): the `tagmode_battery` step + `runs/t90-tagmode-battery.toml` (no town_preamble — chores suspended by construction), the executor's `enforce_label_display` knob (default unchanged, pinned), `drills/t91_filter_flag.py` (T66's 4-press memory diff on key F), and `runlog --tagmode`. Hotkey double-check delivered: ALT = Show Items (keyfile entry 37, toggle via BH.json `always_show_items`), state readable (T66 flag); **F is bound nowhere on disk** — hence the T91 probe first; blind-parity fallback with chat-announced abort windows if no flag is found. Protocol edits accepted at R248: undroppables (cube/tomes/scrolls/maps/potions) sit out via the exception registry; interleaved (1,2,3)×5 cycles; rounds end early when clean; gather has NO deadline (holds the round open, nags, retries — losing an item outranks finishing). Named risks: a client crash mid-round loses that round's floor items (~1-2 min exposure); the battery anchors at town spawn, so NPC traffic may interfere (abort cheaply if round 1 shows it); the battery refuses if <2 whitelisted droppables are in the inventory. Asked: confirm presence + client at menu, enter a town game for T91 first (agent runs it via the bridge), then say "go" for the battery launch. | **Resolved 2026-08-13** — "ready for the tests". T91 PASS on the first probe: exactly ONE alternating byte in 116 modules, `BH.dll+0x14D1CC` (1 = filter styling, 0 = default names) — wired as `offsets.BH_FILTER_STYLE` + `default_tags_on()` + the battery's `filter_state`; all three modes machine-verified. T90 launch 1 ABORTED by the operator (drill-log row 1): round 1A COMPLETE and reconciled (4/4 whitelisted in 5.7 s, labels off — the first data point), then round 1B exposed two defects live: walk clicks re-collect labeled drops during the drop phase, and a stash misclick wedged the drop-walk (panel recovery covered only the inventory; no progress budget; every wedge tick read "acted" so no engine tripwire could fire). A THIRD defect surfaced during the abort itself: runs never wire `should_stop`, so the agent had no abort channel and the stop needed the operator's ESC. Nothing was lost (the wedge-round floor was re-collected by the bot's own walk clicks; census intact). The operator then ordered the R250 redesign — see R250. |

| R250 | execute | Relaunch the rebuilt tag-mode battery (the operator's redesign) | The operator's edit spec, implemented in full (1293 tests green, ruff clean): (1) **"abort the test" now stops RUNS immediately** — `wiring.run_stop_channel` wires the chat abort words ("abort" / "abort test" / "abort the test") and the drill-cancel file into `should_stop`, polled every tick and from inside every walk; standing for all future runs. (2) **Misclick recovery everywhere**: any stray blocking panel (stash, NPC dialog) is closed and the battery carries on; the chat console is exempt (the human is typing abort). (3) **The R250 structure**: drop ALL inventory except the cube and both tomes (18 items — potions/scrolls/maps now drop, deliberately overriding the registry's no-drop protections inside this battery; the R113 ctrl settle + census reconciliation carry the slip risk) in ONE PILE at the feet (no scatter walking — the orbit/NPC-nudge defect is gone by construction); blocks in order 1A..5A NO NAME TAGS → one ALT press → 1B..5B LOOT FILTER TAGS → one F press → 1C..5C DEFAULT TAGS; round announced before, score announced after ("round 1A score: 3/4 whitelisted, 1 unwanted, 12.3s"); gather retries then ASKS the operator to lift stragglers ("resuming." when clear), hands-off while they do; census reconciles over inventory+belt (gathered potions route to the belt). Everything event-logged with wall clocks; `runlog --tagmode` groups by block. Asked: say "go" when at the machine and ready — same launch method (guarded-run, one game, from wherever the client is). | **"go" (2026-08-13 07:10); INTERRUPTED at 351 s by the watchdog silent-freeze (4th occurrence)** — drill-log T90 row 2. The rebuild itself worked exactly as specified: 18 dropped in one pile, rounds announced, scores announced, operator-assist gathers ("resuming.") — and **4 clean A-rounds banked: 20/20 whitelisted (100%), mean 14.9 s, 22 junk pickups, assists declining 5→4→2→0**. The ending was NOT vitals: the watchdog process froze (heartbeat stale, process alive, its own log stops at 07:15:54) and `--require-watchdog` enforcement ended the game. ~8 junk items on the floor were lost to the auto-exit (all whitelisted items were already collected). The armed faulthandler dead-man wrote NOTHING because guarded-run's cleanup taskkilled the frozen process ~5-8 s into the freeze while the dump matures at 10 s — the instrument could never fire under its own launcher; fixed (12 s grace on a stale heartbeat before the kill). Relaunch gate: R251. |

| R251 | execute | Relaunch the battery after the watchdog freeze (attempt 3) | Nothing in the battery needs changing — run 2 proved the R250 design end-to-end (4 clean rounds, every announcement and assist behaving as specified). The freeze is the standing watchdog mystery, now instrumented properly: guarded-run waits 12 s before killing a frozen watchdog so the faulthandler dump can mature — the NEXT freeze finally writes its stack. Two freezes today clustered at ~5.3-5.8 min of watchdog life (310 s on game 1 of the R244 batch, ~345 s here), which smells like a pattern worth the dump. Loss note for this launch: ~8 junk items (potions/maps/ring-class) went down with run 2's auto-exit; the battery's begin census will simply show the smaller jumble. Asked: say "go" to relaunch; if the freeze recurs, the run ends the same safe way and we finally get the stack. | **"go." (07:29); SELF-STOPPED after round 1A** (drill-log T90 row 3): the round itself was clean (6/6 whitelisted in 11.3 s — the operator had restocked the jumble to 18 with new whitelisted kinds), but the census caught a real discrepancy and ended the battery: 18 items verified out of the inventory, only 17 landed on the floor — **a healing potion was DRUNK by a slipped ctrl during the drop phase**, the exact accepted risk of the R250 drop-everything rule. Right detection, wrong severity (one wasted potion ended 14 remaining rounds). The operator's follow-up feedback (their merge theory + "ask me to do the pickup immediately after each round") became the R252 rebuild — see R252. |

| R252 | execute | Relaunch: operator-gathered rounds + loss-tolerant census (attempt 4) | The operator's follow-up implemented (1294 tests green, ruff clean): (1) **the between-round gather is now the operator's job, immediately** — the moment a round's score is announced the battery says "please pick up all the items (N on the floor); resuming when it is clear", goes hands-off (never clicks alongside them), nags every 20 s, and announces "resuming." when the floor clears; (2) **per-drop floor-landing confirmation** — an item that leaves the inventory but never reaches the ground (launch 3's drunk potion) is announced, logged (`battery.consumed`), excluded from the census, and the battery continues; (3) **census mismatches are announced and recorded, never fatal** (`battery.loss` is a record now, not a stop) — covering the operator's stack-merge theory: merged stacks read as count losses, and a battery under live observation should note them and keep testing. Asked: say "go" for attempt 4 — expect a prompt to pick up the pile after every round's score. | **"go" (07:40); blocks A and B COMPLETE, block C eaten by watchdog stall #5** (drill-log T90 row 4). The R252 design ran flawlessly: 10 rounds, zero losses, operator gathers 6–9 s, one ctrl-slip potion in 4B caught and excluded by the floor confirm. **The measurement is dramatic: A (no tags) 30/30 (100%), mean 12.3 s; B (filter tags) 3/30 (10%), all five rounds timed out.** The stall killed the run 1 s after block C's F-verified mode set (floor clean, nothing lost) — and the grace-fix TEACHING MOMENT: the heartbeat was FRESH again at teardown and the watchdog's own log never gapped, so the "silent freeze" is a TRANSIENT ~4–10 s stall with recovery, invisible to the 10 s dead-man and fatal only because the engine declares the watchdog down after 3 s of staleness. Blocks param added for a C-only rerun; the staleness-grace policy question became R253. |

| R253 | decision | Watchdog staleness grace (safety policy), then the C-only run | Five "freezes" are now one diagnosis: the watchdog transiently stalls ~4–10 s and recovers (evidence: run 4's heartbeat fresh at teardown, its log gap-free, every faulthandler dump empty because the stall never reaches the 10 s dump trigger). The engine declares the watchdog down after 3 s of staleness and ends the run — so a transient in the BACKSTOP kills healthy runs (~1 per 6–8 run-minutes today). Proposal: the engine requires the heartbeat to read stale CONTINUOUSLY for 15 s before declaring the watchdog down. Safety analysis: Track A (in-process poll, 0.100 s chicken latency, ADR 2026-08-07) carries the primary guarantee and is untouched; the watchdog is the independent backstop, and a backstop that reacts to a REAL death of its process within 15 s (instead of 3) still bounds the unguarded window to seconds — versus today, where the backstop's own hiccups end runs. This touches a safety invariant, so it is the operator's call, not the agent's. Q1: approve the 15 s stale-grace (yes/no/other number)? Q2: after implementing (offline + tests), launch the C-only run (`runs/t90-tagmode-battery-c.toml`) to finish the battery — go? | **"OK, go" (08:1x) — both approved.** The 15 s grace implemented (engine `watchdog_stale_grace_s`, `watchdog.stale`/`recovered` events, no-channel case still refuses instantly; 1298 tests green) and the C-only run COMPLETED cleanly (drill-log T90 row 5, zero stalls — the grace went untested): **C (default tags) 11/30 (36%), all rounds timed out.** THE BATTERY IS COMPLETE: A (no tags) 100% @ 12.3 s mean; B (filter tags) 10%; C (default tags) 36%. Two more ctrl-slip potion drinks (3C, 5C) caught and excluded by the floor confirm — 3 healing potions total were the session's tax. The ruling request is R254. |

| R254 | verify | Rule on the tag-mode results: which mode becomes the standing pickup policy | The R248 objective is answered by 15 measured rounds (logs 20260813-074021 + -082022, `runlog --tagmode`): **A (no tags) 30/30 (100%), mean 12.3 s, zero timeouts; B (loot-filter tags) 3/30 (10%), all timeouts; C (default tags) 11/30 (36%), all timeouts.** The implication cuts against standing code: `execute.py::_pick_up` FORCES labels ON before pickup clicks (the T63/T66 "labels must be showing for the small classes to be clickable" policy) — the data says that policy pins the bot in its WORST mode; the jumble's small classes (rings/amulets/gems-class kinds) went 30/30 with labels OFF. Proposed ruling: the executor ensures labels OFF for pickup (invert the enforcement, same parity-safe flag mechanics), field-validated by one ordinary cold-plains run's census. Also for the record: the drop-everything rule cost 3 healing potions to ctrl slips across the session (caught by the floor confirm; acceptable test tax, but worth knowing before any future battery). Asked: confirm the reading and approve the policy inversion (or rule otherwise); the change lands as its own commit with the battery data cited. | **APPROVED 2026-08-13 08:3x** — *"approved - invert the label policy and validate it. Glad we did this."* Inverted (`execute.py::_pick_up` now ensures labels OFF before pickup clicks, same parity-safe flag mechanics; the battery suspends enforcement in all modes since it owns the display; keys.py's stale labels-on-to-pick note corrected; 1299 tests green). **Field validation PASSED**: one ordinary cold-plains run (log 20260813-083614), COMPLETE at 480 s, monitor silent, census 1/1 collected first-click, zero misses/write-offs — a thin sample (one wanted drop rolled), with the battery's 30/30 carrying the statistical weight. The operator also ordered: commit all work, /yona-review, /yona-push. |

## Observations so far (for the reduction analysis)

- 12 of 15 resolved requests are `execute`/`verify`, and all of those
  exist because of one boundary: **live checks need an elevated terminal
  + physical presence**. The single highest-leverage change would be an
  elevated execution path for the agent (e.g. the user launches one
  elevated session the agent can drive), which would collapse R3–R15
  into agent-side work with the user only supervising input-sending
  steps.
- The two `decision` points were both genuine — architecture direction
  and a blocked dependency — and would not benefit from automation.
- Several `execute` rounds were repeats caused by tool defects the run
  itself exposed (R4→R5→R6→R7, R12→R13). That is the loop working as
  designed (reality-testing), but better first-run instrumentation
  (watch-until-stop, per-bit diagnostics) would have halved the round
  trips; both fixes are now permanent tooling.

### M5 observations (added at closeout, 2026-08-03)

- The predicted remedy worked at scale: the elevated bridge (R31)
  collapsed what would have been ~60 elevated-terminal round trips into
  agent-side work. M5's ~85 requests are dominated by the residue the
  bridge cannot remove — *game-side presence*: "be at the machine and
  say go" gates, by-eye verification, and restaging test materials.
- The next reduction was proven late and should be the M6 default where
  stakes allow: **the standing mandate** (R207 — OK gates waived for a
  bounded campaign, the game at the agent's disposal). The pickup
  accuracy arc ran T65–T67 with near-zero human round trips once
  granted; the same arc gated per-run would have been 10+ requests.
- A small `provision` cluster repeated three times in one week:
  restock healing potions by hand (R178, R180, R204), a direct
  consequence of the no-vendor-UI decision (R48). Remedies, in
  increasing cost: restock discipline before sessions; a bigger
  inventory reserve via pickit; vendor purchasing (still deliberately
  out of scope).
- `verify` requests increasingly resolved by instrumentation the
  failures themselves bought (narrative log, click audits, per-attempt
  trails, arrival telemetry) — the "tool judges instead of the human"
  conversion the M3 observations predicted. The remaining human
  verifies are the ones only eyes can do (does it LOOK right; what did
  the client render), which is irreducible from outside the process.
