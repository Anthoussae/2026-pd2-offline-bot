"""Moving through the world: routes, walking, and the explored-map atlas.

A* over remembered collision grids (pathing, mapstore), the guarded walk
loop (navigate), waypoint travel, survey mode, and the dormant offline
map generator (mapdata). See docs/architecture/navigation.md.
"""
