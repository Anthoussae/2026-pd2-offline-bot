"""Seeing the game: out-of-process memory reading decoded into game state.

Every module here READS; none sends input. The capstone is snapshot.py,
which bundles one consistent picture per tick. See
docs/architecture/perception.md.
"""
